# کار بعدی

## وضعیت نسخهٔ ۱.۹.۲۲ (۲۰۲۶-۰۹-۲۲)

### انجام شد

1. قیمت زنده از کش و سوکت، با تلاش دوبارهٔ اتصال. یک شکست REST یا افتادن
   سوکت، تا وقتی تیک تازه هست، آفلاین حساب نمی‌شود.
2. معاملهٔ خودکار هم پروفایل سریع دارد (۱۰ دلار، اهرم ۲۰۰، سود خالص
   ۳/۴/۵ بعد از کارمزد) و هم تنظیم دستی. حالت واقعی با دکمهٔ خودکار
   عوض نمی‌شود.
3. زیرساخت سفارش واقعی وصل است و بدون اجراکنندهٔ تأییدشده شکست می‌خورد.
   payload صرافی ساخته نشد.
4. پروفایل سرعت هوش مصنوعی: سریع / متعادل / عمیق. پیش‌فرض متعادل.
5. دفترچهٔ نتیجه زمان‌بند ساعتی دارد و تا تیک کاربر خاموش است.
6. `AI_HANDOVER.md` برای ادامهٔ کار هوش مصنوعی بعدی نوشته شد.
7. پیشنهادهای پیشرفته در `ADVANCED_FEATURES_FA.md` است.

### هنوز باز — بدون کاربر تمام نمی‌شود

1. **`doctor_report.txt` نرسیده.** تشخیص شکست سازنده روی ویندوز بدون آن
   حدس است و نباید تکرار شود.
2. **اسکرین‌شات بخش تولید سیگنال نرسیده.**
3. **اجراکنندهٔ واقعی LBank/Toobit.** تا سند رسمی فیلدهای سفارش نرسد،
   payload ساخته نمی‌شود. اگر اندازهٔ قرارداد نبود، حجم حدس زده نمی‌شود.

### بعد از تأیید کاربر

کامیت و پوش فقط وقتی کاربر همین نسخه را تست کرد و صریح تأیید کرد.
تا پیش از آن، تحویل فقط زیپ روی صفحهٔ دانلود است.
