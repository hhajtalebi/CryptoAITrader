## وضعیت جاری ۲.۵.۴ — بسته‌شدن خودکار EXE، معاملهٔ خودکاری که باز نمی‌شد، اسکالپ فوق‌سریع

گزارش کاربر: (۱) EXE پس از چند دقیقه/ساعت خودش بسته می‌شود؛ (۲) معاملهٔ خودکار هیچ معامله‌ای باز نمی‌کند؛
(۳) «اسکالپ فوق‌سریع» با اهرم بالا، بستن در سود خالص کاربر (مثلاً ۲–۳ دلار پس از کارمزد) و ~۱۰۰ معاملهٔ هم‌زمان
با ۱۰۰۰ دلار. پاسخ‌های کاربر: فعلاً کاغذی با قیمت زنده؛ سقف هم‌زمانی تا ۲۰۰ قابل تنظیم.
- بسته‌شدن: هر `TaskHandle` فرزند Qt ‏`AsyncRunner` بود و هرگز پاک نمی‌شد (چند کار در ثانیه ← ده‌ها هزار QObject).
  حالا `_on_handle_finished` ← صف `_graveyard` ← `purge_finished` (نابودی هم‌گام با `shiboken6.delete` پس از
  `FINISHED_HANDLE_GRACE_SECONDS=2`؛ `stop()` همه را پس از join نابود می‌کند). `deleteLater` مستقیم آزمون‌ها را
  با segfault می‌کشت (GC چرخه‌ای runner/نابودی هم‌زمان با emit) — تکرار نشود. کار زمان‌بندی‌نشده (runner متوقف)
  دستهٔ بی‌والد می‌گیرد. `app/diagnostics.py`: `data/logs/app.log` (چرخشی ۵MB)، `crash.log` (faulthandler +
  excepthook نخ‌ها)، `session.json` (`SessionMonitor`: ضربان ۶۰ث، `clean`؛ نشست ناتمام قبلی ← اعلان
  `common.unclean_exit`)، پیام‌های Qt در لاگ، `MainController.health_counters/log_health`. نوشتن DB پایش معامله
  محدود شد (`PERSIST_INTERVAL_SECONDS=2`، تغییر حد ضرر فوری). رویدادهای موتور: اعلان‌ها محدود (۲٫۵ث) و تازه‌سازی
  جدول‌ها یکجا (۴۰۰ms: `_schedule_auto_refresh`/`_flush_auto_refresh`).
- باز نشدن: حد ضرر دورِ سیگنال در اهرم بالا همیشه `risk_exceeds_loss_budget` می‌داد. حالا `_open_trade_locked` هدف
  و حد ضرر را به بودجهٔ دلاری کاربر محدود می‌کند (`stop_source`/`target_source` در extra رکورد). `scan_stats()`
  (نامزد/بازشده/دلیل‌های رد) در پنل با `trades.auto.scan_summary` و `trades.auto.reject.*` هر ۳ ثانیه دیده می‌شود.
- فوق‌سریع: `trading/ultra_scalp.py` (`momentum`، `UltraScalpSource` از کش تیک کل بازار + فیلتر نقدینگی)، حالت موتور
  `ultra` (`ULTRA_MIN_SCAN_INTERVAL=1`، `ULTRA_MIN_HOLD_SECONDS=10`، بدون ابطال سیگنال)، `HARD_MAX_CONCURRENT=200`،
  دکمهٔ «⚡ اسکالپ فوق‌سریع» (`TradesPage.ULTRA_PRESET`: ۱۰$×۵۰، هدف ۲$، ضرر ۲$، ۱۰۰ هم‌زمان، ۱۸۰ث، پویش ۱ث)، فیلد
  «بیشترین زمان باز ماندن»، تنظیم‌های `scalp.ultra_*`. کش روزانهٔ سود/زیان ۱ث و یک عکس پرتفوی برای هر ورود.
  حالت سفارش (کاغذی/واقعی) با پیش‌تنظیم عوض نمی‌شود؛ درگاه واقعی همچنان کاغذی است. سوکت حداکثر ۸۰ نماد
  (`MAX_STREAMED_SYMBOLS`)، بقیه با REST هر ۳ ثانیه.
آزمون: `tests/test_v254_ultra_and_stability.py`؛ کل مجموعه 2564 پاس + 2 skip. شبیه‌سازی ۳۰۰ نماد: ۱۰۰ معامله در
کمتر از ۸ ثانیه، بستن در سود خالص ≈ ۲ دلار (دادهٔ مصنوعی؛ نشانهٔ سودآوری نیست). گزارش: `docs/RELEASE_2.5.4_FA.md`.
**commit نشده** (آخرین commit: `511d2b5` = 2.5.1+2.5.2؛ 2.5.3 هم commit نشده است).

## وضعیت قبلی ۲.۵.۳ — ربات‌های ساخت فایل نصبی ویندوز و APK

گزارش کاربر: ساخت ویندوز «پنجره‌های زیادی از بخش‌های مختلف نرم‌افزار باز می‌کند و فایل نصبی نمی‌سازد»؛ APK با
«APK ساخته نشد» (RESULT=1) تمام می‌شود. علت ویندوز: `tools/build_installer.py` کل آزمون‌ها را روی دسکتاپ واقعی
(بدون offscreen) اجرا می‌کرد و هر شکست PyInstaller را متوقف می‌کرد؛ BOM در ابتدای bat جلوی `@echo off` را می‌گرفت؛
`.iss` بدون BOM بود. رفع: `tools/build_common.py` (`run_streaming` خروجی زنده + `build_logs/*.log`، `headless_env`)،
گام «بررسی سلامت کد» (compileall + import بی‌پنجرهٔ `SMOKE_CODE`)، آزمون کامل فقط با `--with-tests`/`RUN_TESTS=1`
(بی‌پنجره)، `upx=False`، `tests/conftest.py` پیش‌فرض offscreen، `scripts/build_windows.bat` ← `build_installer.bat`.
علت APK: `mobile/assets/` (آیکون/پیش‌نمایش/قلم) نبود، پذیرش مجوز SDK، `python-bidi` بدون پین (Rust)، آزمون ریاضی
با پایتون بدون numpy/pandas، ساخت روی `/mnt/c`، `pip --user` مسدود در اوبونتو ۲۴. رفع: `mobile/assets/*`،
`android.accept_sdk_license = True`، `python-bidi==0.4.2`، `warn_on_root = 0`، `check_toolchain`/`probe_script`
(buildozer در `~/.cai-buildozer`، فرمان apt دقیق)، `build_script` (کپی به `~/cryptoaitrader-apk`، ساخت روی
فایل‌سیستم لینوکس، برگرداندن APK به `mobile/bin`)، ردکردن آزمون ریاضی در نبود بسته‌ها. موبایل 1.9.13.
آزمون: `tests/test_v253_build_tools.py`. ساخت واقعی ویندوز/APK در sandbox ممکن نیست. گزارش: `docs/RELEASE_2.5.3_FA.md`.
**commit نشده** (آخرین commit: `511d2b5` = 2.5.1+2.5.2).

## وضعیت قبلی ۲.۵.۲ — فیوچرز LBank: HTTP 403 از Cloudflare

گزارش کاربر پس از 2.5.1: اسپات ✓ (۳ دارایی)، فیوچرز ✗ `NetworkError: HTTP 403 … /cfd/openApi/v1/prv/account`.
`lbkperp.lbank.com` پشت Cloudflare است (`api.lbkex.com` نه). رفع: `BROWSER_HEADERS` در `_contract_client`،
`LBankRestClient._classify_forbidden` + `_cloudflare_code`/`CLOUDFLARE_REASONS` ← `AccessBlockedError`
(`app/exceptions/errors.py`, زیرکلاس `NetworkError`) در `no_retry_on`، شکستن حلقهٔ دارایی‌های فیوچرز پس از
مسدودی، `wallet.hint.blocked/blocked_region` در `_wallet_error_hint`. کد Cloudflare واقعی کاربر هنوز نامعلوم؛
گزارش بعدی کیف پول آن را نشان می‌دهد. گزارش: `docs/RELEASE_2.5.2_FA.md`. commit شد در `511d2b5`.

## وضعیت قبلی ۲.۵.۱ — نمایش دارایی کیف پول، واچ‌لیست، ترتیب و قیمت بازارها

خواسته‌های کاربر: کیف پول هیچ دارایی (کل/اسپات/فیوچرز) نشان نمی‌داد؛ ساخت واچ‌لیست با کلیک راست روی ردیف/نماد
بازار؛ «افزودن به واچ‌لیست» مودال کار نمی‌کرد؛ ترتیب نمادها بر پایهٔ ارزش و حجم (BTC، ETH بالا)؛ قیمت دقیق تتری.
پیاده‌سازی: `LBankRestClient.signed_headers()` (امضا در سرآیند + بدنه)، `LBankEndpoints.USER_INFO_ACCOUNT/
USER_INFO_LEGACY`، `LBankProvider._spot_rows`/`last_sync_report`/زنجیرهٔ مسیرهای اسپات، `ExchangeAccountService`
(شکست اسپات با فیوچرز سالم غیرکشنده، `details["report"]`)، `WalletPage` (`status_panel`/`set_sync_report`،
`auto_sync_checkbox`، `spot_search`/`hide_small_checkbox`)، `MainController.start_wallet_auto_sync`/`sync_wallet(silent=)`/
`_fill_wallet_report`/`_wallet_error_hint`؛ `SymbolRepository.add_to_watchlist(symbol, exchange=None)` رکورد را می‌سازد،
`is_in_watchlist`، `MainController.set_watchlist_membership`/`_sync_symbol_table`؛ `market/market_rank.py`
(`sort_by_market_value`, `price_decimals`, `quote_usdt_prices`)، `MarketsPage.build_context_menu` و حالت
`market_cap`. آزمون: `tests/test_v251_wallet_watchlist_markets.py`. گزارش: `docs/RELEASE_2.5.1_FA.md`.
**این تغییرها commit نشده‌اند** (دستور صریح کاربر)؛ آخرین commit: `982cce2` (2.5.0).

## وضعیت قبلی ۲.۵.۰ — معامله از سیگنال، کیف پول سه‌زبانه، موجودی کاغذی

خواسته‌های کاربر: اشتراک سیگنال در شبکه‌های اجتماعی؛ جست‌وجوی بالا کشیده و گرد؛ «اقدام به معامله» با
ورود/SL/TP1–3 و بستن با اهداف (پاسخ کاربر: پلکانی ⅓/⅓/باقی، SL به ورود پس از TP1)، بستن مودال و رفتن به جدول
باز؛ بازسازی جدول تاریخچه و عنوان‌ها؛ ماشین‌حساب پرشده از سیگنال؛ بی‌درنگ؛ کیف پول سه‌زبانه (نمای کلی/اسپات/
فیوچرز) با اعداد دقیق فیوچرز؛ موجودی کاغذی جعلی برابر کیف پول واقعی. پیاده‌سازی: `trading/staged_targets.py`
(`next_step`)، `trading/trade_monitor.evaluate_staged`، `PaperTradeRepository.partial_close/realized_pnl_since`،
`trading/paper_account.py`، `MainController._on_trade_requested(signal, dialog=)`/`_apply_trade_step`/
`_paper_account`/`sync_paper_balance_with_wallet`/`_fill_wallet_tabs`، `WalletPage` (سه زبانه، `MetricStrip`،
`paper_sync_requested`)، `TradesPage.HISTORY_COLUMNS` (۱۶ ستون، عنوان هنگام ساخت، به‌روزرسانی درجا)،
`PositionCalculator` (TP2/TP3، `signal_entry`, `trade_values`)، `SignalDetailDialog` (`share_to`, `trade_payload`,
`trade_opened`)، `LBankRestClient._handle_response(contract=True)` (پاسخ `success:true`/`result:""` قبلاً «LBank
error 0» می‌شد — علت نمایش‌ندادن فیوچرز)، `LBankProvider._parse_futures_details`، `wallet_details` در
`extra_config`. گزارش: `docs/RELEASE_2.5.0_FA.md`. commit/push فقط با دستور صریح.

## وضعیت قبلی ۲.۴.۲ — سبکی رابط و کپی سیگنال

گزارش کاربر پس از 2.4.1: «هنوز خیلی سنگین است و هنگ می‌کند» + «امکان کپی نام و اطلاعات
سیگنال در جدول یا مودال». علت‌های یافته: (۱) کاروان GIL — محاسبهٔ pandas پویش روی نخ
پس‌زمینه نخ رابط را پس از هر فراخوانی Qt معطل می‌کرد (پروفایل: ~۹۵٪ زمان هر نماد در
`compute_timeframe_analysis`)؛ (۲) تایمر ۳۰ ثانیه‌ای پیگیری نتیجه روی نخ رابط
`outcome_repository.performance()` (تا ۵۰۰۰ ردیف) را اجرا می‌کرد، حتی وقتی صفحه پنهان بود؛
(۳) پویش‌های پیاپی همان سیگنال را بی‌وقفه ذخیره و پیگیری می‌کردند (رشد پایگاه داده)؛
(۴) ترمینال معاملات هر ثانیه حتی پنهان رسم می‌شد. اصلاح: `signals/compute_pool.py`
(`ComputePool` با spawn، ≤۲ کارگر، اولویت پایین، fallback محلی، بستن در بیکاری) فقط در
`is_bulk_fetch()`؛ `SignalEngine.set_compute_pool`؛ `Application.compute_pool()`
(`performance.process_pool`، متغیر `CRYPTOAI_NO_PROCESS_POOL` که conftest روشن می‌کند)؛
`Application._should_store_scanned` (۳۰ دقیقه/۵ واحد)؛ `_refresh_outcome_view` تنبل با
`run_blocking` و `ShowWatcher`؛ `_terminal_timer_tick`؛ `UiStallWatchdog`؛
`multiprocessing.freeze_support()` در main.py. کپی: `ui/signal_share.py`، دکمه‌های
`SignalDetailDialog.copy_symbol/copy_info`، منوی راست‌کلیک و Ctrl+C در `SignalsPage`
(`copy_notice` → toast). آزمون: `tests/test_v242_lightweight_and_copy.py`. گزارش:
`docs/RELEASE_2.4.2_FA.md`. 2.4.0، 2.4.1 و 2.4.2 هنوز commit نشده‌اند.

## تحویل قبلی ۲.۴.۱ — سبک‌سازی پویش کل بازار

گزارش کاربر پس از 2.4.0: «بخش سیگنال خوب شد ولی سیستم خیلی سنگین است، هنگ می‌کند
و کل سیستم را درگیر می‌کند.» علت‌ها: (۱) هر کندلِ پویش (~۴۰۰۰ درخواست برای کل صرافی)
با ORM در SQLite نوشته می‌شد (دیسک و CPU)؛ (۲) کندل‌های پویش حافظهٔ نهان ۵۰۰تایی را پر
و تیکر/قیمت داغ را بیرون می‌انداختند؛ (۳) محاسبهٔ هم‌گام اندیکاتورها روی تنها حلقهٔ
asyncio تا ۴۰۰ms قفل می‌ساخت و با GIL رابط را کند می‌کرد؛ (۴) هر اندیکاتور DataFrame را
از نو می‌ساخت (~۳۵٪ CPU)؛ (۵) جدول پویش با صدها ردیف و ستون «به اندازهٔ محتوا» هر ۸۰۰ms
از نو رسم می‌شد. اصلاح: `market.engine.bulk_fetch()` (ContextVar)، `signals.scanner.CpuGovernor`
(دستی ۰٫۶، خودکار ۰٫۳۵)، انتظار در مکث نرخ، دروازهٔ محاسبه در `SignalEngine`،
`IndicatorEngine._calculate(frame_holder)`، numpy در `BaseIndicator.calculate`، سقف ۲۰۰ ردیف
و تازه‌سازی تطبیقی. آزمون: `tests/test_v241_performance.py`. گزارش: `docs/RELEASE_2.4.1_FA.md`.
2.4.0 و 2.4.1 هنوز commit نشده‌اند.

## تحویل قبلی ۲.۴.۰ — پویش کل بازار و منبع سیگنال‌گیری خودکار

درخواست کاربر: پویش سیگنال‌ها فقط روی تعداد محدودی نماد (پیش‌فرض ۶۰) اجرا می‌شد؛
تعداد دستی بماند ولی گزینه‌ای برای پویش همهٔ نمادهای صرافی باشد. سیگنال‌گیری
خودکار باید «چرخش کامل» را روی همهٔ نمادها انجام دهد و منبعش بین سیگنال‌های
پیداشده و کل بازار قابل‌انتخاب باشد. پیاده‌سازی: `signals/scan_universe.py` (جهان
پویش، پالایش هوشمند، اولویت نقدشوندگی/نوسان)، `MarketScanner.scan(universe, filters,
on_signal)`، `Application.scan_market(universe, min_turnover, smart_filter, on_signal)`،
`AutoScanConfig.source/universe/min_turnover/smart_filter` + `seed_focus`، کنترل‌های
تازه در `ui/pages/signals_page.py` و نتیجهٔ زنده/پیشرفت در `MainController`. کلیدهای
تازه: `signals.auto_scan_{source,universe,min_turnover,smart_filter}` و
`signals.scan_{universe,limit,min_turnover,smart_filter}`. آزمون:
`tests/test_v240_scan_universe.py`. گزارش: `docs/RELEASE_2.4.0_FA.md`. 2.3.2 با دستور
کاربر commit/push شد (`80ce0ab`)؛ 2.4.0 هنوز commit نشده است.

## تحویل تاریخی ۲.۳.۲ — اتصال LBank

گزارش کاربر: روی LBank حدود ۲۰ ثانیه آنلاین و بعد آفلاین؛ وب‌سوکت LBank هرگز وصل
نمی‌شد (صرافی‌های دیگر سالم). علت‌ها: جدول خطای LBank جابه‌جا بود و 10004
(«Request too frequent») خطای احراز هویت شمرده می‌شد → REST «مرده» و بدون مکث؛
provider.ping هم خطا را می‌بلعید؛ وب‌سوکت فقط `www.lbkex.net` (نشانی قدیمی) را
می‌شناخت. اصلاح: `market/providers/lbank/{constants,rest_client,provider,websocket_client}.py`،
`retry_async(no_retry_on=...)`، آمار عیب‌یابی در `RedundantStream.stats()`؛ تست
`tests/test_v232_lbank.py`. گزارش: `docs/RELEASE_2.3.2_FA.md`. stubهای Qt اکنون در
`.cache/qtstub` داخل مخزن (نادیده در snapshot) ساخته می‌شوند. با دستور کاربر commit/push شد (`80ce0ab`).

## تحویل تاریخی ۲.۳.۱ — اتصال پایدار و داشبورد

رگرسیون 2.3.0 (خواندن بدون کش همهٔ تیکرها هر ۲ ثانیه و کش مشترک ۰٫۱ ثانیه‌ای)
باعث محدودیت نرخ صرافی و «چند ثانیه آنلاین، بعد قطع» می‌شد. اصلاح: کش مشترک،
مکث سراسری 429/418، آنلاین از REST یا WS، snapshot هر ۳۰ ثانیه. داشبورد مرکز
فرمان گرفت (`ui/widgets/dashboard_widgets.py`، `summarise_market` در کنترلر).
گزارش: `docs/RELEASE_2.3.1_FA.md`. Qt در محیط لینوکس بدون libGL با stubهای
ساخته‌شده در `/home/user/qtstub` (خارج مخزن) اجرا شد. هیچ commit/push انجام نشده.

## نسخهٔ ۲.۳.۰ — معامله، اتصال افزونه و تاریخچه

به درخواست کاربر، ورود/خروج هم‌زمان و پایش اسکن از هم جدا شدند؛ محاسبات
خالص کارمزد و سر‌به‌سر، محافظ هزینه/ریسک و سقف زیان روزانهٔ پایدار اصلاح شد.
دو client مستقل از همان صرافی، watchdog، REST تازه و حفظ اشتراک نمودار اضافه شد.
ورود دستی از فرصت‌ها پس از موفقیت به تاریخچهٔ Open می‌رود؛ جدول کامل‌تر،
مودال جزئیات و خروج مشترک دارد. انتخاب قبل از تحلیل و چرخش بازار/علاقه‌مندی‌ها
اصلاح شد. هیچ سود، زیان صفر یا اتصال همیشگی تضمین نمی‌شود.

گزارش دقیق: `docs/RELEASE_2.3.0_FA.md`. آزمون انتخابی **۷۵۸ پاس + ۴ skip**؛
۵۰ رگرسیون تازه. ۳۳ آزمون قدیمی غیرنمایشی جداگانه نیز پاس شدند. full suite
هنوز ۳۰ خطای collection کتابخانهٔ Qt دارد؛ native GUI/ویندوز تأیید نشده است.
بسته: `CryptoAITrader-v2.3.0-2026-09-23.zip`؛ درگاه `tools/serve_downloads.py`.
هیچ commit/push/Release انجام نشده؛ پس از تست ZIP منتظر دستور صریح کاربر بمان.
R6 (feed/tick/trading) و R10 (دامنهٔ چرخش) فقط بخشی رفع شدند؛ agentها و
استقلال آستانهٔ watch هنوز بررسی می‌خواهند. R4/R5/R7/R8/R9 دست‌نخورده‌اند.
بخش‌های نسخه‌های قدیمی پایین، **تاریخی** هستند.

## نسخهٔ ۲.۲.۲ — ۲۰۲۶/۰۹/۲۳

**رفع سه ایراد بازتولیدشده + صفحهٔ دانلود محلی به درخواست کاربر.**

- کش پیش‌بینی با کلید تایم‌فریم جدید بدون TypeError؛ تست force/TTL/آخرین گزارش.
- انتقال هر ۲۸ فیلد AutoTradeConfig؛ صفر کارمزد و False حفظ می‌شوند؛ سن مجاز
  داده در cache متصل هم با ذخیرهٔ config به‌روز می‌شود.
- بررسی snapshot و برچسب درست حساسیت backup؛ حفظ ciphertext و بازیابی کلید.
  settings.json قدیمی محافظه‌کارانه حساس فرض می‌شود؛ backup خصوصی است.
- ۲۰ تست رگرسیون هسته + ۱۴ تست سرور موفق؛ تست اتصال Qt اضافه ولی محیطاً skip.
- ترکیب با هستهٔ قبلی: ۷۰۸ پاس + ۳ skip، صفر شکست. full collection به علت
  libGL مسدود است؛ نتیجهٔ تاریخی تست‌های کامل به این مرحله نسبت داده نمی‌شود.
- درگاه فارسی و واکنش‌گرا؛ فقط ZIP/metadata/checksum/font، بدون فایل دلخواه مخزن.
- گزارش و دستور اجرای سرور: docs/RELEASE_2.2.2_FA.md.
- نسخهٔ بسته: CryptoAITrader-v2.2.2-2026-09-23.zip؛ دادهٔ واقعی و .git/.venv ندارد.
- هیچ commit/push/Release گیت‌هاب انجام نشده؛ منتظر تست و دستور صریح کاربر.

---

## نسخهٔ ۲.۲.۱ — ۲۰۲۶/۰۹/۲۳

**شناخت مخزن و مستندسازی، بدون تغییر منطق نرم‌افزار.**

- تطبیق HEAD با main گیت‌هاب: `a66e61a2d45721aff46d1d08a804012e3c907084`.
- ZIP اولیهٔ دست‌نخورده: ۴۸۲ فایل؛ CRC و SHA-256 تک‌تک فایل‌ها تأیید شد.
- ثبت قواعد کار در AGENTS.md و نقشهٔ اتصال اجزا در docs/PROJECT_MEMORY_FA.md.
- فهرست کامل فایل‌ها/کلاس‌ها/توابع/واردسازی‌ها در docs/CODE_MAP.md؛ نمایهٔ
  ایستا است، نه ادعای ممیزی خط‌به‌خط یا صحت کامل همهٔ مسیرهای اجرایی.
- گزارش آزمون و اختلاف مستندات با کد در docs/REVIEW_VALIDATION_FA.md.
- اجرای هسته: ۶۷۴ پاس + ۲ skip (LightGBM/PyTorch اختیاری)؛ اجرای کامل
  هنگام جمع‌آوری با ۳۰ خطای محیطی libGL متوقف شد. UI/ویندوز تأیید نشده‌اند.
- سه اشکال با دادهٔ ساختگی بازتولید شدند و **عمداً اصلاح نشده‌اند**:
  cache پیش‌بینی، config تنظیم‌های جدید معامله، و manifest پشتیبان رازها.
- تنها تغییر فایل Python مقدار APP_VERSION است؛ وابستگی‌ها، تست‌ها، UI،
  منطق مالی، دیتابیس و تنظیمات کاربر تغییر نکردند.
- بستهٔ پایان مرحله: CryptoAITrader-v2.2.1-review-2026-09-23.zip.
- commit/push/PR/Release انجام نشده؛ منتظر بررسی و دستور صریح کاربر.

---

## نسخهٔ ۲.۲.۰ — ۲۰۲۶/۰۹/۲۳

**ترمینال حرفه‌ای — دور دوم بر اساس فهرست مشکلات کاربر.** فقط صفحهٔ
معاملهٔ خودکار؛ بقیهٔ صفحات دست‌نخورده.

### کارت‌های سرمایه (مشکل ۱)

هر ۹ کارت آیکون + عنوان + مبلغ دارد (سبک تصویر مرجع)؛ سود/زیان
روز بر اساس علامتِ خودِ داده سبز/قرمز می‌شود.

### نمودار حرفه‌ای (مشکل ۲)

- **اندیکاتور**: منوی تیک‌خور EMA 21 / SMA 50 / باندهای بولینگر —
  محاسبه از همان کتابخانهٔ اندیکاتورهای تحلیل (منبع واحد) روی
  کندل‌های واقعی؛ دادهٔ کم = پیام صادقانه.
- **خط‌کشی**: خط افقی (هر کلیک، قابل‌جابه‌جایی) و خط روند (دو
  کلیک) + پاک‌کردن همه — مثل تریدینگ‌ویو؛ خطوط با تازه‌شدن کندل
  نمی‌پرند.
- **منوی نماد با آیکون**: دیلیگیت سفارشی — دایرهٔ رنگی ثابت برای
  هر نماد (رنگ از نام نماد).
- **عکس‌لحظه‌ای**: کل نمودار (قیمت+حجم) با یک دکمه → PNG.
- **نوع نمودار** (کندل/خط/ناحیه) و **بازنشانی زوم** در همان نوار.
- **رال‌تایم‌تر**: بازخوانی کندل هر ۵ ثانیه (قبلاً ۱۵) + خط قیمت
  زندهٔ هر تیک + تعویض نماد/تایم‌فریم همان لحظه نمودار را عوض
  می‌کند (زوم کاربر محفوظ می‌ماند).

### پنل تصمیم + هوش مصنوعی (مشکل ۳)

دکمهٔ «🤖 تحلیل هوش مصنوعی» → همان عامل خودمختار صفحهٔ تحلیل روی
نماد/تایم‌فریمِ همین نمودار؛ نتیجه با جهت/اطمینان/مراحل/دلیل در
پنل می‌نشیند. مدل فقط توضیح می‌دهد — عددسازی ممنوع (قواعد سند
حاکم). بدون هوش مصنوعیِ پیکربندی‌شده، پیام روشن.

### جدول فرصت‌ها همیشه زنده (مشکل ۴)

- **پویش پس‌زمینه**: حتی وقتی موتور معامله خاموش است، همان پویش
  AI هر `scalp.watch_scan_interval` ثانیه (پیش‌فرض ۲۰) اجرا
  می‌شود و نامزدها را با احتمال/حرکت انتظاری/MTF/ریسک/سطوح در
  جدول نگه می‌دارد. موتورِ روشن همچنان غنی‌تر است و اولویت دارد.
- **تنظیمات مودال**: ⚙ روی کارت فرصت‌ها → روشن/خاموش، فاصله،
  حداقل اطمینان، سقف اسپرد، سقف ردیف — همهٔ کلیدهای
  `scalp.watch_*` با پیش‌فرض (بدون مهاجرت تنظیم‌های موجود).
- «قابل ورود» = اطمینان ≥ آستانه و اسپرد در محدوده؛ ورودِ واقعی
  همچنان همهٔ دروازه‌های موتور را می‌گذرد.

### جدول موقعیت‌ها (مشکل ۵)

- **ستون اکشن (۱۹مین)**: دکمهٔ «بستن» در هر ردیف → همان سیگنال
  بستن موتور؛ مرتب‌سازی این جدول خاموش شد چون ویجت سلولی با
  جابه‌جایی مرتب‌سازی ناسازگار است (جدول هر ثانیه تازه می‌شود).
- **مودال جزئیات**: کلیک روی نماد → همهٔ ۱۸ فیلد + دکمهٔ بستن.
- بقیهٔ ستون‌ها (Bid/Ask/سن داده و …) لحظه‌ای از قبل بودند.

### نمادهای مورد علاقه (مشکل ۶)

- «💱 انتخاب نمادها» در تنظیمات → مودال جست‌وجو + تیک‌خور با
  آیکون هر نماد.
- تأیید → ذخیره در `scalp.selected_symbols` **و همگام‌سازی با
  فهرست پیگیری دیتابیس** — یعنی همین لیست همه‌جای برنامه (داشبورد/
  تحلیل/سیگنال) به‌عنوان «نمادهای مورد علاقهٔ من» در دسترس است.
  ورودی متنی فقط-خواندنی شد (منبع حقیقت: مودال).

### فنی

- `PriceChart`: `set_draw_mode`/`clear_drawings`/`screenshot_pixmap`
  + نشانگر نقطهٔ اول خط روند؛ در حالت خط‌کشی پن‌زوم خاموش.
- `ui/dialogs/trading_dialogs.py` (تازه): SymbolPickerDialog،
  PositionDetailDialog، ScannerSettingsDialog، SymbolIconDelegate.
- کنترلر: `on_chart_indicators_changed`، `on_ai_opinion_requested`،
  `on_scanner_settings_saved`، `on_selected_symbols_saved`،
  `_run_watch_scan`/`_watch_rows_from_candidates`؛ ورود دستی با
  پشتیبانِ نامزد پویش؛ پیام روشن وقتی موتور خاموش است.

**آزمون‌ها:** مجموع ۲۲۷۰: ۲۲۶۹ پاس + ۱ skip مربوط به LSTM؛ فایل
تازهٔ `test_v22_terminal_pro.py` با ۱۹ آزمون (کارت‌ها، نوار ابزار
نمودار، خط‌کشی، عکس، دیلیگیت، هوش مصنوعی، آستانه‌های پویش،
دکمهٔ بستن، مودال‌ها) + حذف تعریف تکراری قدیمی.

## نسخهٔ ۲.۱.۱ — ۲۰۲۶/۰۹/۲۳

**اصلاح v2.1.0 بر اساس بازخورد کاربر: چیدمان ترمینالی + دو باگ واقعی.**

### چیدمان — «ترمینال حرفه‌ای» به‌جای پشتهٔ کارت

- حذف `addStretch` انتهایی زبانهٔ خودکار: پیش از این فضای اضافهٔ
  پنجره بلااستفاده می‌ماند و هیچ بخشی بزرگ نمی‌شد؛ حالا نمودار
  (کشش ۳) و زبانه‌های پایین (کشش ۲) با پنجره بزرگ می‌شوند — در
  ۱۰۸۰p صفحهٔ تمام‌ترمینال بدون اسکرول؛ در ۷۲۰p اسکرول BasePage.
- هدر یک‌ردیفی فشرده (عنوان + قرص‌ها + تنظیمات + شروع/توقف).
- ستون راست = پنل پیش‌بینی (بالا) + پنل ریسک (پایین)؛ نمودار غالب
  با نسبت ۵:۲.
- زبانه‌های پایین به سبک صرافی‌ها: «فرصت‌ها (n)» | «موقعیت‌های باز
  (n)» با شمار زندهٔ ردیف‌ها در عنوان زبانه.
- پنل تنظیمات پیش‌فرض جمع‌شده (دکمهٔ «تنظیمات ▸» در هدر).
- برچسب نمادِ نمودار در سربرگ پنل پیش‌بینی — پیوند «نمودار ↔
  پیش‌بینی» همیشه دیده می‌شود.

### دو باگ واقعی (گزارش کاربر: «نمودار کار نمی‌کند / نمادی
قابل انتخاب نیست»)

1. **کمبوی نماد همیشه خالی**: کنترلر هرگز `set_auto_symbols` را صدا
   نمی‌زد. حالا همراه همان جایی که تحلیل/سیگنال پر می‌شوند
   (`_on_symbols_loaded`) کمبوی ترمینال هم با فهرست کامل پر می‌شود.
2. **بوت‌استرپ نماد فقط روی تیکِ اول**: اگر بازار هنوز بارگذاری
   نشده بود، نمودار برای همیشه خالی می‌ماند. حالا هر تیک تا
   نخستین موفقیت امتحان می‌کند (تحلیل ← فهرست پیگیری).

### تله‌های Qt که در این مسیر پیدا شد

- `addItem` روی کمبوی خالی ایندکس را خودش ۰ می‌کند؛
  `setCurrentIndex(0)` بعدی **سیگنال نمی‌دهد** (همان ایندکس) —
  باید اول ایندکس را در حالت بی‌صدا به ‎-1‎ برگرداند.
- نمادِ غایب در کمبو: افزوده + انتخاب با سیگنال صریح (اگر کمبو
  خالی باشد، مسیر طبیعی سیگنال قطعی نیست).
- تست قدیمی «جعبهٔ جمع‌شده ارتفاع نمی‌گیرد» باید پنل تنظیمات را
  اول باز کند — پیش‌فرضِ جمع، سنجش درونش را بی‌اثر می‌کرد.

**آزمون‌ها:** مجموع ۲۲۵۱: ۲۲۵۰ پاس + ۱ skip مربوط به LSTM؛
‎+۳ آزمون تازه (زبانه‌ها با شمار، پرشدن کمبو با سیگنال، نماد
غایب) و به‌روزسازی دو آزمون قدیمی.

## نسخهٔ ۲.۱.۰ — ۲۰۲۶/۰۹/۲۳

**بازطراحی کامل صفحهٔ «معاملهٔ خودکار» به ترمینال حرفه‌ای معامله.** مرجع
طراحی، تصویر دلخواه کاربر (چیدمان/تراکم بصری/تجربهٔ کاربری) بود؛ هیچ
عددی از تصویر به برنامه منتقل نشد — همهٔ داده‌ها از موتورهای واقعی
پروژه می‌آید. محدودهٔ کار: فقط همین صفحه و داشبورد داخلی‌اش؛ بقیهٔ
صفحات دست‌نخورده.

### ساختار جدید ترمینال (۱۴ بخش مرجع)

1. **هدر ترمینال** — عنوان، حالت موتور (اجرا/توقف)، قرص Paper/Live،
   وضعیت وب‌سوکت، آخرین تیک (با میلی‌ثانیه)، تأخیر کل، دکمهٔ شروع/توقف
   و دکمهٔ «تنظیمات» که پنل تنظیمات را جمع/باز می‌کند.
2. **۹ کارت اطلاعاتی** — موجودی، آزاد، مارجین درگیر، سود/زیان روز،
   معاملات باز، فرصت‌های فعال، نرخ برد، افت سرمایه، وضعیت ریسک.
3. **نمودار اصلی بازار** — کندل+حجم با همان `PriceChart` صفحهٔ تحلیل
   (منبع واحد، بدون موتور نمودار تازه)، خط قیمت زنده، خطوط ورود/SL/TP
   موقعیت باز و P50 پیش‌بینی؛ تایم‌فریم 1m/5m/15m/1h/4h و نماد از
   همین کارت عوض می‌شود؛ ردیف وضعیت زیر نمودار (قیمت/روند/سن داده با
   هشدار STALE).
4. **پنل پیش‌بینی کنار نمودار** — خلاصهٔ تصمیم‌محور (جهت، اطمینان،
   احتمال، حرکت انتظاری، رژیم، مرحله، روند کلان، هم‌راستایی MTF،
   توافق مدل، کیفیت داده، هشدار، P10/P50/P90 پنج تایم‌فریم) از همان
   موتور پیش‌بینی واحد صفحهٔ پیش‌بینی — موتور جدیدی ساخته نشد.
5. **پویش فرصت‌ها — ۱۷ ستون** — نماد/جهت/اطمینان/احتمال/حرکت
   انتظاری/روند/MTF/ریسک(R/R)/اسپرد/قدرت سیگنال/ورود/TP/SL/اهرم/
   مارجین/وضعیت/اکشن؛ جست‌وجو + فیلتر (همه/قابل ورود/ردشده)؛ دوبار
   کلیک روی ردیفِ قابل‌ورود = درخواست ورود دستی که همهٔ دروازه‌های
   موتور دوباره سنجیده می‌شوند؛ مقادیر غایب «—» اند نه صفر قلابی.
6. **موقعیت‌های باز — ۱۸ ستون** — ستون «مقدار» (Quantity) افزوده شد؛
   خروج همچنان تیک‌محور (میلی‌ثانیه‌ای) با پشتیبان poll.
7. **پنل تنظیمات جمع‌شونده** — همان سه حالت (نمادهای انتخابی/پویش
   بازار/هوش مصنوعی خودکار) + جعبه‌های تنظیم دستی و محافظ‌ها؛ همهٔ
   آستانه‌ها از تنظیمات کاربر، هیچ عددی هاردکد نیست.
8. **پنل ریسک ۱۰خانه** — زیان روزانه/سقف، حداکثر افت (از منحنی سهام
   واقعی)، موجودی درگیر، مصرف مارجین، تعداد باز/سقف، ریسک هر معامله،
   اهرم، ریسک اسپرد (بدترین اسپرد نمادهای فعال)، نقدینگی، ریسک داده
   (STALE) + حکم نهایی رنگی.
9. **رزولوشن‌ها** — ردیف نمودار|پیش‌بینی در عرض کم جمع می‌شود؛
   جدول‌ها با اسکرول افقی، هیچ داده‌ای له یا حذف نمی‌شود.

### تغییرات فنی هم‌زمان

- `trading/auto_trader.py`: ردیف فرصت‌ها غنی‌شد (احتمال، حرکت
  انتظاری، MTF، ریسک، ورود/TP/SL/اهرم/مارجین، پرچم قابل‌ورود) +
  نامزدِ ذخیره‌شده و `opportunity_candidate(symbol)` برای ورود دستی.
- `trading/ai_decider.py`: چهار فیلد تازه روی `AICandidate`
  (probability/expected_move_percent/mtf/risk_reward) که در پویش AI
  پر می‌شوند.
- `ui/charts/price_chart.py`: جدا شدن خطوط سیگنال از خطوط سطوح؛
  `mark_signal` حالا تکرار-امن است (پاک‌سازی فقط خطوط سیگنال) و هر
  تیک بدون محو شدن S/R قابل فراخوانی است.
- **ایمن‌سازی مرتب‌سازی Qt**: ایندکس واقعی ردیف در `Qt.UserRole`
  ذخیره می‌شود تا دوبار کلیک/انتخاب پس از مرتب‌سازی جدول به دادهٔ
  درست اشاره کند (باگ کلاسیک جابه‌جایی سطرها).
- کنترلر: تایم‌فریم/نماد → `get_candles` واقعی → نمودار ترمینال؛
  بازخوانی دوره‌ای کندل؛ پالت نمودار در هر سه نقطهٔ تغییر پوسته؛
  `_risk_panel_data()` با حکم بدترین‌حالت؛ ورود دستیِ فرصت از همان
  مسیر `open_trade` (دروازه‌ها دوباره سنجیده می‌شوند).
- بومی‌سازی: ‎+۸۸ رشتهٔ تازه (fa/en) برای ترمینال.

**آزمون‌ها:** مجموع ۲۲۴۸: ۲۲۴۷ پاس + ۱ skip مربوط به LSTM (~۵ دقیقه).
آزمون‌های ترمینال برای قرارداد جدید (۹ کارت، ۱۷/۱۸ ستون، قرص‌های هدر،
پنل ریسک، نمودار، نگاشت UserRole) بازنویسی و توسعه یافتند.

## نسخهٔ ۲.۰.۰ — ۲۰۲۶/۰۹/۲۲

**نسل ۲ — موتور هوش پیش‌بینی کامل (فازهای ۲ تا ۱۵).** شماره‌گذاری به
فرمان کاربر از ۱.۱۱ به ۲.۰ ارتقا یافت: جهش معماری (موتور احتمالاتی
تازه + جدول پایگاه + دو ابزار عامل + صفحهٔ یازدهم) با شمارهٔ فرعی
جمع نمی‌شود.

**موتور هوش پیش‌بینی — فازهای ۲ تا ۱۵ کامل و متصل.** سند حاکم:
`docs/PREDICTIVE_ENGINE_FA.md`. همهٔ قوانین آن حفظ شد: هیچ عددی بدون
داده ساخته نمی‌شود؛ افقِ بدون تاریخچه با دلیل غیرفعال است؛ مهر زمانی
فیچر = بسته‌شدن کندل؛ LLM توضیح می‌دهد نه عدد‌سازی.

### لایهٔ محاسباتی (زیر `signals/prediction/`)

- **horizons.py** — برنامه‌ریز ۱۳ افق (۱m تا 7d) با انتخاب هوشمند
  تایم‌فریم پایه و الزامات حداقلی دادهٔ هر افق.
- **regime.py** — شش رژیم + مرحلهٔ چرخهٔ بازار + ماتریس گذار
  (`_samples` به تفکیک مرحله، نه رژیم).
- **distribution.py** — چندک‌های تجربی از بازده‌های پیش‌روی واقعی؛
  fallback تحلیلی σ√n با ATR و برچسب صادقانه.
- **fusion.py / uncertainty.py** — ترکیب چهارجزئی (توزیع، آنسامبل،
  مومنتوم، رویداد) و اطمینان مؤثر با کسر بهای دادهٔ ناقص.
- **models/** — آنسامبل آماری+GBM(+LSTM اختیاری)، walk-forward،
  برچسب `log(ahead/base)`؛ LightGBM/XGBoost با API بومی (sklearn لازم
  نیست). `gbm.py`: MIN_TRAIN_ROWS=120، num_leaves=15، ۸۰ دور.
- **scenarios.py / breakout.py / anomaly.py / warning.py / events.py /
  crossasset.py / leadlag** — سناریوی گاوی/پایه/خرسی، شکست و شکست
  قلابی (مقایسه با `_ANALYSIS` موجود)، آنومالی آماری، هشدار زودهنگام،
  فشار رویداد از تقویم دستی، کراس-asset فقط کریپتو داخلی، سرپیشی
  BTC/ETH با lag آزمایش‌شده.
- **engine.py** — `PredictiveIntelligenceEngine.assess()`: خط لولهٔ
  کامل + کش ۶۰ ثانیه + ضدهرزنامهٔ ثبت (فقط تغییر جهت یا |Δp|≥۳ یا
  کهولت ≥ max(۱۵دقیقه، افق/۴)).
- **scoring.py / timeline.py / store.py** — بریر، دقت جهت/بازه،
  کالیبراسیون ۶-سطلی، سلامت مدل، تایم‌لاین و «چه عوض شد»؛ حکم
  neutral با بازه P25–P75.

### داده و یکپارچه‌سازی

- جدول `predictions` + مهاجرت Alembic؛ `PredictionRepository` با
  `due_for_resolution` (مقایسهٔ عددی — رشته در SQLite همیشه True
  می‌شود) و `accuracy_group`.
- `app.application.prediction_engine` (تنبل؛ با تعویض صرافی باطل
  می‌شود) + `PredictionStore` با قیمت‌یاب از کندل‌های واقعی DB.
- دو ابزار عامل: `get_prediction_report`، `get_prediction_accuracy`
  (+ نام فارسی برای گام‌های چت).
- صفحهٔ «پیش‌بینی» (یازدهمین صفحه) + `localization/{fa,en}/prediction.json`
  + `nav.prediction` در هر دو زبان.

### رفع اشکال واقعی (کشف‌شده از دل آزمون‌ها)

1. **دکمهٔ تحلیل له‌شده با قلم واقعی**: متریک وزیرمتن sizeHint=۳۳
   می‌دهد و خانهٔ جدول ۳۰ پیکسل جا داشت — `BUTTON_ROW_HEIGHT`
   ۴۶→۵۴. در آزمون‌ها دیده نمی‌شد چون قلم هنوز بار نشده بود؛ هر
   کاربر واقعی آن را می‌دید.
2. **نشت فونت سراسری بین آزمون‌ها**: `apply_default_font` فونت
   QApplication را عوض می‌کرد و هیچ آزمونی برنمی‌گرداند؛ v193 و
   v172 فقط در اجرای کامل مجموعه می‌شکستند. فیکسچر `_isolate_qt_app_look`
   (conftest) فونت+شیوه‌نامه را بعد از هر آزمون برمی‌گرداند.

**آزمون‌ها:** ‎+۱۵۷ (مجموع ۲۱۷۹: ۲۱۷۸ پاس + ۱ skip مربوط به LSTM که
به torch اختیاری نیاز دارد؛ ۵ دقیقه — پیش‌تر ۱۴ دقیقه).

## نسخهٔ ۱.۹.۲۳ — ۲۰۲۶/۰۹/۲۲

هیچ قابلیت تازه‌ای ندارد. کار این نسخه صفر کردن هفت آزمون مردود و
برطرف کردن یک علت واقعی شکست سازندهٔ ویندوز است.

### ۱. پایان خط فایل‌های `.bat` — علت واقعی، پنج آزمون

پنج فایل `.bat` در مخزن پایان خط LF داشتند و پروژه `.gitattributes`
نداشت. از خود بلاب گیت، نه درخت کاری:

```
$ git ls-files --eol '*.bat'
i/lf    w/lf    attr/     scripts/doctor.bat

$ git cat-file -p HEAD:scripts/doctor.bat | tr -cd '\r' | wc -c
0
```

`i/lf` یعنی خودِ مخزن LF دارد، پس **هر کلون روی هر سیستم‌عاملی** LF
می‌گرفت — از جمله ویندوز کاربر. `cmd.exe` با پایان LF رفتار عجیبی
دارد و برچسب‌های `goto` و بلوک‌های `if` ممکن است ناقص خوانده شوند.

نتیجهٔ عملی‌اش این بود که عیب‌یاب سازندهٔ خودِ برنامه
(`tools/build_doctor.py`) اسکریپت‌ها را «ناموفق» گزارش می‌کرد. چون
`AI_HANDOVER.md` می‌گوید «بدون `doctor_report.txt` نمی‌شود گفت چرا
سازنده روی ویندوز شکست می‌خورد» و `docs/HANDOVER_FA.md` می‌گوید «سه
بار حدس زده شد و هر سه بار غلط بود»، این یافته ارزش دارد که یک **علت
اثبات‌شدنی** است، نه حدس چهارم.

`.gitattributes` با قاعدهٔ `*.bat text eol=crlf` اضافه شد: مخزن LF
می‌ماند و هنگام checkout روی هر سیستم‌عاملی CRLF نوشته می‌شود. پس از
آن `git ls-files --eol` مقدار `i/lf w/crlf attr/text eol=crlf` می‌دهد
و بلاب‌های گیت دست‌نخورده ماندند — تنها تغییر مرحله‌شده خودِ
`.gitattributes` بود.

### ۲. دو آزمونی که نام را می‌جستند، نه رفتار را

**`test_controller_uses_user_settings_not_hardcoded_numbers`** روی منبع
`_auto_trader` دنبال رشتهٔ `build_trader_config` می‌گشت. آن متد کار را
به `_auto_trade_config()` واگذار کرده (خط ۴۹۹۰) که خودش در خط ۵۷۳۱
`ScalpService(self.app).build_trader_config()` را صدا می‌زند. پس **رفتار
درست بود** و عددها از تنظیم کاربر می‌آمدند؛ آزمون شکست می‌خورد چون
دنبال یک نام می‌گشت. `docs/HANDOVER_FA.md` §۵ صریحاً همین تله را
هشدار می‌دهد. حالا آزمون عددهای کاربر (۴۲ / ۷٫۵ / ۴٫۵ / ۳۳) را در
پیکربندیِ واقعاً ساخته‌شده می‌بیند.

**`test_a_slow_model_does_not_block_forever`** شیء تنظیماتی می‌ساخت که
فقط `get_int` داشت. از ۱.۹.۲۲ مهلت از راه پروفایل سرعت
(`ai/speed_profile.py`) و با `settings.get()` خوانده می‌شود، پس فیک به
پیش‌فرض پروفایل «متعادل» (۴۵ ثانیه) برمی‌گشت و آزمون ۱۰ ثانیه طول
می‌کشید. اندازه‌گیری:

```
فیکِ قدیمی (فقط get_int) → مهلت = 45.0 ثانیه
فیکِ با get              → مهلت = 1.0 ثانیه
```

یعنی **کد محصول درست کار می‌کرد**؛ مهلت همیشه اعمال می‌شد، فقط عددش
از جای دیگری می‌آمد.

هر دو آزمون پس از تغییر روی کدِ عمدی خراب‌شده هم اجرا شدند: با
برداشتن `asyncio.wait_for` آزمون نخست در ۱۰٫۲۴ ثانیه شکست خورد، و با
hard-code کردن `target_profit` آزمون دوم شکست خورد. پس واقعاً
رگرسیون را می‌گیرند.

### ۳. پاک‌سازی قطعی پنجره در آزمون‌ها

`test_new_pages.py` در اجرای دسته‌ای از سقف ۶۰۰ ثانیه رد می‌شد، در
حالی که هر آزمونش به‌تنهایی سه ثانیه است. تفکیک اندازه‌گیری نشان داد
ساخت `MainWindow` ثابت است (~۱ ثانیه) و آنچه رشد می‌کند
`ThemeManager.apply()` است:

```
نوبت     apply   MainWindow   همه‌ویجت   سطح‌بالا
   1      0.01         0.98       1942         57
   2      1.92         0.99       3885        115
   4      6.56         1.20       7771        231
   6      8.93         0.85      11657        347
```

`QApplication.setStyleSheet` هر ویجت برنامه را دوباره پولیش می‌کند، پس
هزینه‌اش با تعداد کل ویجت‌ها رشد می‌کند و ویجت‌های پنجرهٔ آزمون قبلی
آزاد نمی‌شدند. علتش این است که `deleteLater()` فقط یک رویداد
`DeferredDelete` در صف می‌گذارد و `processEvents()` آن را پردازش
نمی‌کند — Qt پردازش حذف معوق را به سطح حلقهٔ رویداد گره زده است.
`tests/test_exchange_login_flow.py` دقیقاً همین پاک‌سازی ناقص را داشت
و برای ۹ آزمون ۲۱۵ ثانیه می‌گرفت.

`tests/conftest.py::destroy_window` اضافه شد که صریح
`sendPostedEvents(None, QEvent.DeferredDelete)` صدا می‌زند. اندازه‌گیری
پس از آن: رشد ویجت در هر نوبت از ۱۹۴۲ به ۱۱۰ رسید و زمان ساخت ثابت
ماند. نتیجهٔ عملی:

| فایل | پیش | پس |
|---|---|---|
| `test_new_pages.py` | رد شدن از سقف ۶۰۰ ثانیه | ۴۶ ثانیه |
| `test_exchange_login_flow.py` | ۲۱۵ ثانیه | ۵۸ ثانیه |

**صادقانه: این اشکال کاربر را نمی‌زند.** `main.py:109` فقط یک بار
`MainWindow` می‌سازد و تعویض پوسته از نو نمی‌سازدش
(`apply_theme_tokens()` صدا زده می‌شود). مشکل از تکرار ساخت پنجره در
یک فرایند بلند می‌آید، پس راه‌حلش در `tests/conftest.py` است نه در کد
محصول.

### ۴. ناسازگاری مستندات

- `docs/HANDOVER_FA.md` §۶ «۱۳۶ کلید تنظیمات» می‌گفت؛ برنامه **۱۴۲**
  کلید دارد.
- سرآغاز همان سند روی «۱.۹.۱۹ — ۲۹۷ فایل پایتون، ۷۶ فایل آزمون»
  مانده بود؛ واقعی ۱.۹.۲۳، ۳۰۴ و ۷۹ است.
- نسخه در `README.md` خط ۷ و `README_EN.md` خط ۵ هم ذکر می‌شود که در
  فهرست «چهار جا» نبود. `README_EN.md` هنوز «Version 1.0.0» و
  «Python 3.12+» می‌گفت. چون جداکنندهٔ رقم در README فارسی `٫` است نه
  `.`، جست‌وجوی `1.9.22` آن را پیدا نمی‌کرد.
- همین فایل (`PROJECT_PROGRESS.md`) مدخل ۱.۹.۲۲ نداشت؛ بالاترین مدخل
  ۱.۹.۲۱ بود.

**نتیجهٔ آزمون‌ها: ۲۰۵۰ قبول / ۰ مردود / ۰ خطا.**

دقت شود که چرا ۲۰۵۰ و نه ۲۰۲۶: پیش از این نسخه ۲۰۱۹ قبول و ۷ مردود
گزارش می‌شد، ولی `test_new_pages.py` از سقف زمانی رد می‌شد و **۲۴
آزمونش هرگز شمرده نمی‌شدند**. ۲۰۱۹ + ۷ + ۲۴ = ۲۰۵۰. زمان کل مجموعه هم
از ۱۰۰۴ ثانیه به ۲۸۳ ثانیه رسید.

---

## نسخهٔ ۱.۹.۲۲ — ۲۰۲۶/۰۹/۲۲

> این مدخل در زمان خودش نوشته نشده بود و `PROJECT_PROGRESS.md` روی
> ۱.۹.۲۱ مانده بود. شرح زیر **اختراع نشده**؛ از دو منبع موجود در
> همان نسخه بازسازی شده است: بخش «تغییرهای نسخهٔ ۱.۹.۲۲» در
> `BUILD_INFO.txt` و `AI_HANDOVER.md` (نگارش ۲۰۲۶-۰۹-۲۲). آنچه در آن
> دو سند نیست، اینجا هم نیامده است.

### ۱. اتصال و قیمت زنده

اگر پینگ اول شکست بخورد، سوکت باز هم تلاش می‌کند (`ensure_streaming`).
تیک سوکت `_mark_rest_alive(True)` می‌زند و نگهبان هر ۱۵ ثانیه
`keepalive` را صدا می‌زند. سقف نماد سوکت ۸۰ است، کف نظرسنجی خوراک
۰٫۵ ثانیه و پیش‌فرض ۲ ثانیه.

قیمت صفحه از کش می‌آید و `set_live_price` صدا زده می‌شود؛
`set_chart_data` هر ثانیه صدا زده نمی‌شود تا کل کندل‌ها بی‌دلیل
دوباره کشیده نشوند. تیک قیمت ۱ ثانیه، پایش معامله ۲ ثانیه، کندل
۸ ثانیه و تازه‌سازی جدول ۰٫۵ ثانیه است.

### ۲. معاملهٔ سریع و کارمزد

دکمهٔ «تنظیم خودکار سریع» مارجین ۱۰، اهرم ۲۰۰، هدف و حد ضرر ۳/۴/۵،
همزمانی ۱، پایش ۰٫۴، نگهداری ۹۰ و کارمزد ۰٫۰۰۰۶ را ذخیره می‌کند.
**حالت و عبارت تأیید را پاک نمی‌کند** — یعنی معاملهٔ واقعی با یک دکمه
روشن نمی‌شود. ذخیرهٔ تنظیم، موتور در حال اجرا را با `apply_config`
به‌روز می‌کند.

کارمزد ورود روی رکورد ذخیره می‌شود و کارمزد خروج فقط به `close_trade`
داده می‌شود؛ سود خالص = ناخالص − هر دو طرف. برای ۱۰×۲۰۰ کارمزد
رفت‌وبرگشت ۲٫۴۰ دلار است، پس هدف خالص ۳ دلار یعنی ناخالص ۵٫۴۰ دلار.

### ۳. درگاه سفارش واقعی

درگاه خالی به موتور وصل است تا حالت زنده داخل `open_position` شکست
بخورد، نه اینکه بی‌صدا کاغذی بماند در حالی که کاربر فکر می‌کند واقعی
است. **payload هیچ صرافی حدس زده نشد.**

### ۴. پروفایل سرعت و دفترچهٔ نتیجه

پروفایل سرعت (سریع / متعادل / عمیق) در زبانهٔ کارایی تنظیمات است.
متعادل عددهای ذخیره‌شده را نگه می‌دارد؛ سریع یک گام و یک ابزار چت است.
دفترچهٔ نتیجه تیک ساعتی دارد و پیش‌فرض خاموش است
(`signals.scorecard_auto`).

### ۵. یک تصحیح در خودِ `AI_HANDOVER.md`

آن سند صریحاً می‌گوید شرحش جایگزین شرح تفصیلی `BUILD_INFO.txt` برای
۱.۹.۱۷ تا ۱.۹.۲۱ نیست و «آن بخش‌ها عمداً اختراع نشده‌اند». همین
قاعده اینجا هم رعایت شد.

---

## نسخهٔ ۱.۹.۲۱ — ۲۰۲۶/۰۹/۲۱

سه خواستهٔ کاربر.

### ۱. «سیستم خیلی قطع می‌شود و بازار آفلاین می‌زند»

دو علت پیدا شد، هر دو با آزمایش زنده اثبات شدند:

- **یک درخواست ناموفق بی‌درنگ وضعیت را آفلاین می‌کرد.** روی اینترنت
  ناپایدار این یعنی چشمک‌زدن دائم. حالا `REST_FAILURE_TOLERANCE = 3`:
  آفلاین فقط پس از سه شکست پشت‌سرهم، ولی **یک** موفقیت فوراً برمی‌گرداند
  (بدبینی کند، خوش‌بینی سریع). شمارنده با هر موفقیت صفر می‌شود.
- **فقط `get_all_tickers` سلامت اتصال را گزارش می‌کرد.** `get_candles`
  با موفقیت داده می‌گرفت ولی وضعیت آفلاین می‌ماند؛ یعنی کاربری که در
  صفحهٔ تحلیل بود، با وجود دادهٔ زنده پیام آفلاین می‌دید. اثبات زنده:
  پس از یک قطعی ساختگی، `get_candles` ده کندل گرفت و وضعیت
  `DISCONNECTED` ماند. حالا این مسیر هم سلامت را اعلام می‌کند.

### ۲. تنظیم دستی معاملهٔ خودکار

پنل «معاملهٔ خودکار» در صفحهٔ معاملات حالا جعبهٔ «تنظیم دستی عددها»
دارد: سرمایهٔ هر معامله، هدف سود، حد ضرر، اهرم، تعداد هم‌زمان، حداقل
اطمینان و روش انتخاب ارز. پیش‌فرض **قفل** است تا عددهای پول واقعی
تصادفی عوض نشوند.

همان کلیدهای `scalp.*` نوشته می‌شوند که صفحهٔ تنظیمات می‌نویسد، پس دو
صفحه از هم جدا نمی‌افتند.

### ۳. معامله بر پایهٔ اطمینان بالای ۷۵٪

**این رفتار قبلاً وجود نداشت.** معاملهٔ خودکار از موتور **اسکالپ** نامزد
می‌گرفت که بر پایهٔ نوسان و حجم پنج‌دقیقه‌ای کار می‌کند و اصلاً مفهوم
«درصد اطمینان» نداشت.

`trading/confidence_source.py` اضافه شد: کل بازار را با موتور سیگنال
پویش می‌کند و هر نمادی که اطمینانش از حد کاربر بالاتر باشد به نامزد
معامله تبدیل می‌شود، مرتب‌شده بر پایهٔ اطمینان نزولی.

- پیش‌فرض‌ها: `scalp.candidate_source="confidence"`،
  `scalp.min_confidence=75`، `scalp.scan_symbols=40` ⇒ **۱۳۹ تنظیم**.
- کف ایمنی ۵۰٪ که دور زدنی نیست.
- «انتظار» هرگز معامله نمی‌شود.
- هوش مصنوعی عمداً صدا زده نمی‌شود (پرهزینه و کند روی ده‌ها نماد).

**دو باگ که آزمون‌ها گرفتند:** `TradingSignal` فیلد `entry_price` ندارد
(بازهٔ `entry_min`/`entry_max` است) و `reasons` هم ندارد (`reason`
تک‌رشته‌ای است). خواندن نام اشتباه، همهٔ نمادها را بی‌صدا رد می‌کرد.

**آزمایش زنده:** با حد ۶۰٪ هشت نامزد (SOL ۷۴٪، AVAX/BTC ۷۳٪)، با حد
۷۵٪ **صفر نامزد** — یعنی حد واقعاً اعمال می‌شود.

- **افزوده:** `tests/test_v1921_online_and_autotrade.py` — ۲۳ آزمون.
  با برگرداندن تحمل به ۱، سه آزمون می‌افتند.

---

## نسخهٔ ۱.۹.۲۰ — ۲۰۲۶/۰۹/۲۱

### گزارش کاربر: «تنظیمات آشنا در تنظیمات نیست، چرا برداشتی؟»

**چیزی برداشته نشده بود.** بررسی کامل مسیر نشان داد:

- `ai/providers/catalog.py` — سرویس `ashna` ثبت است (مورد پنجم فهرست).
- فهرست کشویی تنظیمات ۱۶ سرویس دارد و آشنا داخلش هست.
- کادر کلید با انتخاب آشنا **مرئی و فعال** است و حالت رمز دارد.
- نشانی پایه خودکار پر می‌شود: `https://api.ashna.ai/v1/api`.
- کلید با نام `ai.ashna.api_key` به انبار **رمزگذاری‌شده** می‌رود.
- درخواست واقعی به سرویس زده شد: پاسخ ۴۰۱ «Invalid API key» —
  یعنی نشانی درست است و فقط کلید معتبر لازم دارد.

**ولی یک کمبود واقعی پیدا شد:** `signup_url` برای هر ۱۶ سرویس در
کاتالوگ ذخیره شده بود ولی **هیچ‌جای برنامه نمایش داده نمی‌شد**. کاربر
باید حدس می‌زد کلید را از کجا بگیرد. حالا زیر کادر کلید می‌آید:

    🔑 دریافت کلید از: https://app.ashna.ai/account?tab=api

این برای همهٔ سرویس‌ها کار می‌کند، نه فقط آشنا.

**چرا این اشتباه رخ داد:** یک بار سنجش اولیه گفت کادر کلید نامرئی است.
علتش این بود که صفحه هرگز `show()` نشده بود و زبانهٔ «هوش مصنوعی» فعال
نبود؛ در آن حالت `isVisible()` همیشه `False` می‌دهد. با فعال کردن زبانه،
مقدار درست `True` شد. در آزمون‌ها این نکته مستند شد.

- **افزوده:** `tests/test_v1920_ashna_settings.py` — ۱۶ آزمون.
  با برگرداندن اصلاح، ۲ آزمون می‌افتند (یعنی واقعاً چیزی را می‌گیرند).
- **افزوده:** کلید ترجمهٔ `settings.ai_get_key_at` در fa و en.

---

## نسخهٔ ۱.۹.۱۹ — دفترچهٔ نتیجه و سامانهٔ هشدار (۲۰۲۶/۰۹/۲۰)

دو مورد از سه پیشنهاد اولویت‌دار سند `NEW_FEATURES_V1918_FA.md` ساخته شد،
به‌علاوهٔ بستن یک کار باز از `NEXT_TASK.md`.

### ۱. دفترچهٔ نتیجهٔ پیش‌بینی‌ها — `score_forecast` دیگر کدِ مرده نیست

`score_forecast()` در ۱.۹.۱۷ نوشته شد ولی `grep` نشان داد **هیچ‌جا صدا
زده نمی‌شد**. یعنی ادعای «بازهٔ ۸۰ درصدی» هیچ‌وقت سنجیده نمی‌شد.

حالا `signals/scorecard.py` + `Application.score_forecasts()` + زبانهٔ
سومِ صفحهٔ گزارش‌ها این کار را می‌کنند:

| افق | بررسی‌شده | داخل بازه | نرخ اصابت |
|---|---|---|---|
| ۱۵ دقیقه | ۴۰ | ۲۶ | ۶۵٪ |
| ۱ ساعت | ۲۲ | ۱۶ | ۷۳٪ |

و حکم صریح می‌دهد: بازه‌ها **تنگ**‌اند، **پهن**‌اند، یا **درست تنظیم**
شده‌اند — به‌همراه پیشنهاد تغییر `SIGMA_MULTIPLIER` (سقف ±۰٫۲ در هر دور
تا تنظیم نوسان نکند).

سه تصمیم برای اینکه آمار **صادقانه** بماند:

- **قیمت تاریخی، نه قیمت امروز.** پیش‌بینیِ «یک ساعت بعد» با کندل همان
  یک ساعت بعد سنجیده می‌شود. سنجیدنش با قیمت فعلی، آمار را بی‌معنا
  می‌کرد.
- **افقِ نرسیده شمرده نمی‌شود.** نه «درست» است نه «غلط»؛ در ستون
  «در انتظار» می‌رود. شمردنش در هر دو حالت دروغ بود.
- **زیر ۲۰ نمونه حکمی صادر نمی‌شود.** تغییر ضریب بر پایهٔ سه نمونه،
  تنظیم را خراب می‌کند.

**یک باگ واقعی هم پیدا شد:** آزمونِ «دادهٔ خراب» نشان داد `score_forecast`
روی ردیفِ غیر-دیکشنری با `AttributeError` می‌ترکید. چون این داده از
پایگاه داده می‌آید و ممکن است از نسخه‌های قدیمی ناقص مانده باشد، حالا
ردیف خراب رد می‌شود.

### ۲. هشدار قیمت و هشدار سیگنال

`signals/alerts.py`. دو نوع هشدار:

- **قیمتی** — «هر وقت BTC/USDT از ۸۵۰۰۰ رد شد خبرم کن». از صفحهٔ
  بازارها با دکمهٔ 🔔 ساخته می‌شود؛ قیمت فعلی پیش‌فرض است و جهت
  (بالاتر/پایین‌تر) خودکار از مقایسه با قیمت روز تشخیص داده می‌شود.
- **سیگنالی** — «هر وقت سیگنالی با اطمینان بالای ۷۰٪ ساخته شد».

سه تصمیم طراحی:

- **هشدار پس از فعال‌شدن خاموش می‌شود، نه حذف.** اگر هر تیک دوباره
  فعال شود، کاربر زیر بار اعلان دفن می‌شود و برنامه را می‌بندد. کاربر
  می‌تواند دستی دوباره مسلحش کند.
- **سیگنال «انتظار» هرگز هشدار نمی‌دهد** — حتی با اطمینان ۹۵٪. انتظار
  توصیهٔ معاملاتی نیست.
- **فقط برای نمادهای دارای هشدار مسلح قیمت گرفته می‌شود.** کاربری با
  دو هشدار نباید باعث گرفتن ۱۳۷۱ قیمت شود.

### آزمون‌ها

‎+۷۲ آزمون تازه (۳۶ دفترچهٔ نتیجه، ۳۶ هشدار). هر دو ماژول روی نسخهٔ
۱.۹.۱۸ اصلاً وجود ندارند (`ModuleNotFoundError`)، پس تازگی‌شان قطعی است.

## نسخهٔ ۱.۹.۱۸ — هوش مصنوعی سریع، نمودار زنده و معاملهٔ خودکارِ قابل‌دیدن (۲۰۲۶/۰۹/۲۰)

کاربر شش ایراد تازه گزارش کرد. مهم‌ترینشان: «هوش مصنوعی در قسمت تولید
سیگنالِ بالای صفحه کار نمی‌کند، ولی بخش پویش سریع و درست است.»

### ۱ و ۲. چرا بالای صفحه کار نمی‌کرد — کند بود، نه خراب

با یک مدلِ ساختگیِ عمداً کند (هر فراخوانی ۲۰ ثانیه) اندازه‌گیری شد:

| حالت | زمان | فراخوانی مدل |
|---|---|---|
| پیش از اصلاح | **۸۱٫۰ ثانیه** | ۴ (پشت‌سرهم) |
| پس از اصلاح | **۲۱٫۱ ثانیه** | ۱ |

علت: `AutonomousAgent._loop` برای هر ابزار یک رفت‌وبرگشت جداگانه به مدل
می‌زد. بخش پویش سریع بود چون **اصلاً مدل را صدا نمی‌زند** و فقط موتور
ریاضی را اجرا می‌کند. روی کارت گرافیک GT 740 هر فراخوانی چند برابر کندتر
است، پس عملاً به نظر می‌رسید هنگ کرده.

**اصلاح:** `_preload_evidence()` سه ابزار پایه (`get_ticker`،
`get_multi_timeframe_data`، `calculate_multiple_indicators`) را **موازی**
با `asyncio.gather` می‌گیرد و نتیجه را در همان پیام نخست به مدل می‌دهد،
با این دستور که فقط اگر چیز اساسی کم بود ابزار صدا بزند.

**نگهبان مهلت:** مدلِ خیلی کند می‌توانست کل ۱۸۰ ثانیه را بسوزاند و در
پایان «انتظار ۰٪» بدهد — یعنی چند دقیقه انتظار برای هیچ. حالا اگر وقتِ
یک رفت‌وبرگشت دیگر نمانده باشد، یک جمع‌بندی اجباری خواسته می‌شود. با
مدل ۶۰ ثانیه‌ای: پیش از این ۱۸۰ ثانیه و WAIT 0٪، حالا **۶۱٫۱ ثانیه و
LONG ۷۱٪**.

### ۴. معاملهٔ خودکار — موتورش بود، دکمه‌اش نبود

کاربر پرسید «معامله خودکار چی شد؟». بررسی نشان داد `trading/auto_trader.py`
و ۱۶ کلید تنظیماتش از نسخهٔ ۱.۹.۱۴ وجود داشتند، ولی `grep` در کل `ui/`
هیچ ارجاعی پیدا نکرد: **هیچ راهی برای روشن‌کردنش وجود نداشت.** قابلیتی
که کاربر نتواند پیدایش کند، عملاً وجود ندارد — حق با کاربر بود.

حالا در صفحهٔ «معاملات» یک پنل هست: دکمهٔ شروع/توقف، چراغ وضعیت، و خطی
که پیش از شروع نشان می‌دهد با چه چیزی معامله می‌شود (حالت، هدف سود، حد
ضرر، اهرم، سرمایهٔ هر معامله، حداکثر معاملهٔ همزمان). همهٔ عددها از
تنظیمات کاربر خوانده می‌شوند و حالت پیش‌فرض **کاغذی** است.

آزمون زنده: با تنظیمات پیش‌فرض، موتور ۳ معاملهٔ واقعی روی داده‌های زندهٔ
بازار باز کرد (`C/USDT`، `BR/USDT`، `ENA/USDT`) و درست متوقف شد.

### ۵. نمودار زنده، شبیه تریدینگ‌ویو

- **خط قیمت زنده** با برچسب رنگی، هر **۳ ثانیه** به‌روز می‌شود (تیک سبک
  و جدا؛ کشیدن دوبارهٔ ۳۰۰ کندل هر ۳ ثانیه هم گران است هم به صرافی فشار
  می‌آورد).
- **سه نوع نمودار:** کندل، خطی، ناحیه‌ای.
- **حفظ زوم** — مهم‌ترین اصلاح. پیش از این نمودار هر ۱۵ ثانیه
  `enableAutoRange` می‌زد و زومِ کاربر پاک می‌شد؛ کار کردن با نمودار
  عملاً ممکن نبود. حالا اگر کاربر خودش زوم کند، دامنه دست‌نخورده می‌ماند.
- **دکمهٔ «تناسب صفحه»** برای بازگشت به نمای خودکار.

### ۶. تحلیل دوباره روی سیگنالِ ساخته‌شده

دو اشکال پیدا شد:

- **یک باگ واقعی:** `show_signal_analysis` نتیجهٔ `get_with_analysis()` را
  با `**` باز می‌کرد، ولی آن متد **تاپل** برمی‌گرداند نه دیکشنری. نتیجه
  `TypeError: 'tuple' object is not a mapping` بود، پس تحلیلِ ذخیره‌شده
  هرگز نمایش داده نمی‌شد. `analysis_payload()` تازه، شکل تختِ آماده می‌دهد.
- **قابلیت نبود:** پنجرهٔ تحلیل فقط متنِ ذخیره‌شده را نشان می‌داد. حالا
  دکمهٔ «تحلیل دوبارهٔ هوش مصنوعی» دارد؛ نتیجه در همان پنجره می‌نشیند و
  با `update_analysis()` در پایگاه داده ذخیره می‌شود تا دفعهٔ بعد بدون
  هزینهٔ دوباره دیده شود. کارت «سیگنال جاری» هم دکمهٔ تحلیل گرفت.

برای فعال‌شدن آن دکمه، شناسهٔ سیگنالِ ذخیره‌شده لازم بود؛ `to_dict()`
شناسه ندارد چون `TradingSignal` اسلات‌محور است، پس `Application` حالا
`_last_signal_id` را نگه می‌دارد.

### ۳. پیشنهاد قابلیت‌های تازه

`docs/NEW_FEATURES_V1918_FA.md` — ۱۲ پیشنهاد در چهار اولویت.

### آزمون‌ها

‎+۳۶ آزمون تازه (`tests/test_v1918_live_and_auto.py`). اثبات بازگشتی:
همین ۳۶ آزمون روی کد نسخهٔ ۱.۹.۱۷ اجرا شد ⇒ **۳۳ شکست، ۳ قبول**.

دو آزمون قدیمی به‌روز شدند چون رفتار عمداً عوض شده (پیش‌بارگذاری موازی
سه ابزار): `test_agent_uses_tools_before_deciding` و
`test_repeated_identical_call_is_blocked`. هر دو حالا «قصد» را بررسی
می‌کنند نه فهرست دقیق فراخوانی‌ها؛ و با خاموش‌کردن عمدیِ نگهبانِ تکرار
تأیید شد که هنوز خطای واقعی را می‌گیرند.

## نسخهٔ ۱.۹.۱۷ — اعتماد به سیگنال، پیش‌بینی تایم‌فریم بعدی و معاملات زنده (۲۰۲۶/۰۹/۲۰)

کاربر نُه ایراد گزارش کرد و خواست همه در یک نسخه رفع شوند. مهم‌ترینشان
این بود: «سیگنال‌های ۸۰، ۹۰ و ۱۰۰ درصدی ضرر دادند.»

### ۱. عدد اطمینان دروغ می‌گفت — ریشه پیدا شد

فرمول قبلی: `min(|score|*0.6 + consensus*0.4, 1.0)`.
«اجماع» نسبت راهبردهای هم‌نظر بود، ولی در بازار روندی عملاً **فقط یک
راهبرد** رأی می‌داد (بازگشت‌به‌میانگین در روند قوی کنار می‌کشد، و
درست هم همین است). اجماعِ «یک از یک» یعنی ۱۰۰٪. نتیجه: **یک رأی تنها
عدد ۱۰۰٪ می‌ساخت.** این با اندازه‌گیری مستقیم روی خود موتور تأیید شد.

راه‌حل در سه لایه:

- **`signals/confidence.py` (تازه)** — سقف بر پایهٔ پوشش شواهد:
  ۱ راهبرد ⇒ حداکثر ۴۵٪، ۲ ⇒ ۶۵٪، ۳ ⇒ ۸۰٪. سقف تایم‌فریم:
  ۱ ⇒ ۶۰٪، ۲ ⇒ ۸۰٪. **سقف مطلق ۹۲٪** — ۱۰۰٪ وعده‌ای است که هیچ
  سامانه‌ای نمی‌تواند به آن عمل کند.
- **کالیبراسیون با نتایج واقعی** — نرخ برد ۱۸۰ روز گذشتهٔ همان بازهٔ
  اطمینان با وزن ۰٫۳۵ در عدد نهایی اثر می‌گذارد (حداقل ۲۰ نمونه؛
  نتیجه‌گیری از ۳ نمونه بدتر از نتیجه‌نگرفتن است).
  `outcome_repository.confidence_buckets(days=180)` با کش ۳۰۰ ثانیه.
- **شواهد بیشتر** — راهبردها از ۳ به ۵ رسیدند:
  `signals/strategies/momentum.py` (MACD + RSI + Stochastic با تشخیص
  واگرایی) و `signals/strategies/volatility_regime.py` (فشردگی در برابر
  انبساط نوسان؛ ورود در اوج نوسان دلیل رایج «جهت درست بود ولی استاپ
  خورد» است).

| شواهد | قبل | بعد |
|---|---|---|
| ۱ راهبرد، ۱ تایم‌فریم، امتیاز کامل | **۱۰۰٪** | **۴۵٪** |
| ۳ راهبرد، ۱ تایم‌فریم | **۱۰۰٪** | **۶۰٪** |
| ۳ راهبرد، ۳ تایم‌فریم | **۱۰۰٪** | **۸۰٪** |

هر عدد اطمینان حالا `reasons` دارد و می‌گوید چرا سقف خورده است.

### ۲. پیش‌بینی تایم‌فریم بعدی

`signals/forecast.py` (تازه). از دادهٔ ۱۵ دقیقه‌ای، بازهٔ محتمل ۱۵ دقیقه،
۱ ساعت و ۴ ساعت بعد را می‌دهد. عمداً **بازه** است نه عدد قطعی: بازه از
ATR واقعی بازار می‌آید و با جذر زمان مقیاس می‌خورد (نوسان ۴ کندل
**۲ برابر** یک کندل است نه ۴ برابر). افق دورتر ⇒ بازهٔ پهن‌تر و احتمال
کمتر. تمایل جهت‌دار فقط مرکز بازه را جابه‌جا می‌کند و اجازه ندارد
بازه را تنگ کند. روی کارت سیگنال نمایش داده می‌شود و در خود سیگنال
ذخیره می‌گردد.

### ۳. بخش معاملات اصلاً کار نمی‌کرد

- `trading/trade_monitor.py` (تازه) — منطق خالص: سود/زیان لحظه‌ای،
  بستن خودکار، تشخیص حد سود/ضرر. **حد ضرر مقدم بر حد سود** بررسی
  می‌شود (فرض محتاطانه، تا آمار عملکرد دروغ نگوید).
- درصد سود نسبت به **مارجین** حساب می‌شود نه قیمت. با اهرم ۱۰، حرکت
  ۰٫۶۲٪ قیمت یعنی ۶٫۲۵٪ بازده. نمایش قبلی عدد را ۱۰ برابر کوچک‌تر
  نشان می‌داد.
- پایش هر ۱۰ ثانیه در کنترلر؛ `trade_repository.update_live_pnl`؛
  ستون تازهٔ `last_price` روی `PaperTradeRecord` + مهاجرت `c8d5f2a91e44`.
- `_last_price` اصلاح شد (جدول بازارها، سپس کش قیمت زنده) — **بستن
  دستی معامله دیگر بی‌صدا شکست نمی‌خورد.**
- نمادی که قیمتش نرسیده **رد می‌شود، صفر نمی‌شود**؛ «۰ دلار» اطلاعات
  غلط است.

### ۴. نمودار زنده

`start_live_chart` هر ۱۵ ثانیه کندل‌ها را تازه می‌کند، فقط وقتی صفحهٔ
تحلیل دیده می‌شود. کلید `analysis.live_chart` برای خاموش‌کردن.

### ۵. بخش تحلیل و اندیکاتورها

پنل اندیکاتورها درست پر می‌شد ولی جدول مقادیر تا فشردن دکمهٔ «تحلیل»
خالی می‌ماند و صفحه بی‌استفاده به نظر می‌رسید. حالا نخستین ورود به
صفحه خودش تحلیل را اجرا می‌کند.

### ۶. پرامپت هوش مصنوعی بازنویسی شد

نقش: **تحلیل‌گر ارشد مشتقات رمزارز با ۱۵+ سال تجربه**، متخصص فیوچرز
دائمی (نرخ فاندینگ، بهرهٔ باز، آبشار لیکوئیدیشن، عدم توازن دفتر سفارش).
لنگر عددی برای اطمینان اضافه شد (۸۰–۹۲ = کمیاب) و بررسی‌های ویژهٔ
فیوچرز: رژیم نوسان، نقدشوندگی، اسپرد، شکار حد ضرر روی اعداد رند.

دو ابزار تازه برای مدل: `get_engine_signal` (پرسیدن نظر موتور ریاضی) و
`forecast_next_timeframe`. پیش‌تر مدل فقط دادهٔ خام داشت و مدل‌های
کوچک به «انتظار» پناه می‌بردند.

### ۷ و ۸. مرتب‌سازی و مستندسازی جدول‌ها

سابقهٔ سیگنال‌ها: مرتب‌سازی بر پایهٔ **بیشترین/کمترین اطمینان، جهت،
جدیدترین، نماد** + تیک «دسته‌بندی بر اساس جهت». پیش‌فرض «بیشترین
اطمینان» است چون کاربر دنبال بهترین فرصت است نه آخرین ردیف.
`docs/SIGNAL_TABLES_FA.md` (تازه) همهٔ ستون‌ها، رنگ‌ها، قواعد مرتب‌سازی
و نحوهٔ خواندن عدد اطمینان را مستند می‌کند.

### ۹. بازخورد دکمه‌ها

«ذخیره» در تنظیمات فقط نوار وضعیت را عوض می‌کرد که دیده نمی‌شد. حالا
اعلان شناور هم می‌دهد — و همین‌طور ذخیرهٔ چیدمان، حذف فهرست پیگیری،
حذف گفتگو، افزودن به فهرست پیگیری و حذف پوستهٔ سفارشی.

### ۱۰. بازار همیشه زنده نیست

پایش معاملات و نمودار زنده وقتی اتصال قطع است **عقب می‌کشند**: بستن
خودکار بر پایهٔ قیمت کهنه ضرر واقعی می‌سازد. یک‌بار هشدار داده می‌شود
(نه هر ۱۰ ثانیه) و بازگشت اتصال هم اعلام می‌گردد.

### ۱۱. ارتقای پایگاه دادهٔ کاربران فعلی (پیش از انتشار گرفته شد)

افزودن ستون `paper_trades.last_price` روی نصب تازه کار می‌کرد ولی روی
پایگاه دادهٔ موجود **نه**: `create_all` جدولِ نبوده را می‌سازد و جدول
موجود را دست نمی‌زند، و هیچ‌جای برنامه Alembic را هنگام راه‌اندازی اجرا
نمی‌کرد. یعنی هر کاربر فعلی با خطای «no such column» روبه‌رو می‌شد.

`DatabaseManager._add_missing_columns` اضافه شد: هنگام راه‌اندازی،
ستون‌های تازه به جدول‌های موجود افزوده می‌شوند. این کار فقط **افزودنی**
است — هیچ ستونی حذف یا تغییر نمی‌کند و هیچ دادهٔ کاربری بازنویسی
نمی‌شود. با پایگاه دادهٔ واقعیِ ساخته‌شده با نسخهٔ ۱.۹.۱۶ آزموده شد.

### ۱۲. سه ایراد باقی‌مانده از فهرست عقب‌افتاده

- **`signals.hide_stale` بی‌اثر بود** — ذخیره می‌شد ولی هیچ‌جا خوانده
  نمی‌شد؛ تیکی که هیچ کاری نمی‌کرد. حالا روی هر دو جدول سابقه و پویش
  اعمال می‌شود و از همان تعریف `signals.validity.NOT_ENTERABLE` استفاده
  می‌کند تا دو تعریف موازی از «سوخته» نداشته باشیم.
- **پنجرهٔ اعتبار ذخیره نمی‌شد** — ستون‌های `primary_timeframe`،
  `enter_before` و `valid_until` در پایگاه داده وجود داشتند ولی
  `save_signal` هرگز پرشان نمی‌کرد، پس سیگنالِ بازخوانده‌شده
  «بی‌تاریخ‌مصرف» به نظر می‌رسید. حالا هر چهار مقدار (به‌علاوهٔ
  پیش‌بینی) نوشته می‌شوند؛ ستون تازهٔ `signals.forecast` +
  مهاجرت `d1a7b3e05c62`.
- **سنجش دقت پیش‌بینی** — `signals.forecast.score_forecast` نرخ
  اصابت بازه را در برابر قیمت واقعی اندازه می‌گیرد. هدف ۸۰٪ است؛
  انحراف زیاد یعنی بازه‌ها باید تنگ‌تر یا پهن‌تر شوند. پیش‌بینی هم
  حالا در پنجرهٔ جزئیات سیگنال نمایش داده می‌شود.

### آزمون‌ها

`tests/test_v1917_trust_and_trades.py` (۳۱) و `tests/test_v1917_forecast.py`
(۳۷). در برابر کد ۱.۹.۱۶ بازتولید شدند: **۱۴ آزمون قرمز شدند.**

## نسخه ۱.۹.۱۶ — رفع «انتظار ۰٪» در مسیر هوش مصنوعی (۲۰۲۶/۰۹/۲۰)

- کاربر تصویری فرستاد که چند نماد پشت‌سرهم «انتظار / ۰٪» ثبت شده بودند.
  این با ایراد ۱.۹.۱۵ فرق داشت: آنجا عامل **شکست** می‌خورد، اینجا عامل
  **موفق** می‌شود ولی خودِ مدل «انتظار/۰» می‌دهد — پس از فیلتر قبلی
  (`succeeded`) رد می‌شد.
- علت ریشه‌ای: عامل بدون هیچ شاهد اولیه‌ای شروع می‌کرد و باید کل تحلیل
  را در چند گام از داده خام بازمی‌ساخت؛ مدل کوچک به امن‌ترین پاسخ
  پناه می‌برد.
- اصلاح ۱: `AutonomousAgent._format_baseline` + جای `{baseline}` در
  `SYSTEM_PROMPT`؛ نتیجهٔ موتور به‌عنوان شاهد به مدل داده می‌شود.
  کنترلر اول موتور را اجرا می‌کند و سیگنالش را به `agent.run(baseline=…)`
  می‌دهد، و همان را برای مسیر جایگزین نگه می‌دارد (بدون محاسبهٔ دوباره).
- اصلاح ۲: `_agent_outcome_is_usable` حالا «انتظار با اطمینان ≤۰» را رد
  می‌کند. «انتظار» با اطمینان واقعی همچنان معتبر است.
- آزمون‌ها: `tests/test_v1916_ai_wait.py` (۱۴ مورد؛ در برابر کد قدیمی
  بازتولید شد و ۴ آزمون کلیدی قرمز شدند).

## نسخه ۱.۹.۱۵ — رفع سه ایراد گزارش‌شده روی LBank (۲۰۲۶/۰۹/۲۰)

- **سیگنال با هوش مصنوعی همیشه «صبر» می‌داد.** اصلاح ۱.۹.۱۱ در لایهٔ
  اشتباه بود: تیک هوش مصنوعی از `Application.generate_signal` رد
  نمی‌شود و مستقیم به عامل خودمختار می‌رود. شکستِ عامل (JSON نامعتبر،
  پایان مهلت، نبود سرویس) حالا از «صبر»ِ واقعی تفکیک می‌شود و در آن
  حالت موتور ریاضی نتیجه را می‌سازد.
  فایل‌ها: `ui/controllers/main_controller.py`
  (`_agent_outcome_is_usable`، `_fallback_to_engine_signal`).
- **دکمهٔ معامله در تاریخچه دیده نمی‌شد** چون در تنظیمات ذخیره می‌شد
  نه در پایگاه داده. حالا در هر دو ثبت می‌شود.
- **موجودی فیوچرز LBank غایب بود** چون `prv/account` پارامتر `asset`
  لازم دارد. هر دارایی جداگانه پرسیده می‌شود.
  فایل‌ها: `market/providers/lbank/{provider,constants}.py`.
- آزمون‌ها: `tests/test_v1915_bugfixes.py` (۱۸ مورد؛ هر سه ایراد در
  برابر کد قدیمی بازتولید و تأیید شدند).

# Project Progress — Crypto AI Trader

**آخرین به‌روزرسانی:** ۲۰۲۶-۰۹-۱۶ — نسخهٔ ۱٫۹٫۱۰
**وضعیت:** 🎉 **پروژه کامل است.** همه فازها تمام شده‌اند؛ برنامه از ابتدا تا انتها اجرا می‌شود.
**سلامت پروژه:** ✅ کل مجموعهٔ آزمون سبز · `--check` سبز · بسته‌بندی PyInstaller با ساخت واقعی آزموده شد

---

## نسخهٔ ۱٫۹٫۱۰ — تغییر راهبرد: اندازه‌گیری به‌جای حدس

کاربر گفت «هنوز هم اولاما وصل نمی‌شود». این چهارمین بار بود.

### چرا سه نسخه شکست خورد

هر سه نسخه یک الگوی مشترک داشتند: از روی شواهد غیرمستقیم فرضیه‌ای
ساختم، کدی نوشتم که با فرضیه بخواند، و چون نمی‌توانستم روی دستگاه
کاربر بیازمایمش، سبز بودن آزمون‌های خودم را «حل شد» گرفتم.

| نسخه | فرضیه | چرا غلط بود |
|------|--------|--------------|
| ۱٫۹٫۷ | `num_ctx` متغیر | درست ولی ناقص |
| ۱٫۹٫۸ | ثبات کافی است | عدد را از رم سیستم می‌گرفتم |
| ۱٫۹٫۹ | نفرستادن کافی است | فرض کردم مشکل فقط پنجره است |

علت ریشه‌ای همیشه یکی بود: **دستگاه کاربر را نمی‌بینم.**

### آنچه از لاگ کاربر واقعاً برمی‌آید

```
name="NVIDIA GeForce GT 740"  total="2.0 GiB"  available="1.7 GiB"
OLLAMA_VULKAN:true
```

هیچ‌کدام از چهار مدل کاربر در ۱٫۷ گیگابایت جا نمی‌شود:

| مدل | حجم | جا می‌شود؟ |
|------|------|-------------|
| gemma4:e4b-it-qat | ۶٫۱G | نه |
| deepseek-r1:8b | ۵٫۲G | نه |
| llama3.1:8b | ۴٫۹G | نه |
| deepseek-coder:6.7b | ۳٫۸G | نه |

یعنی همهٔ مدل‌ها به CPU سرریز می‌کنند، از مسیر Vulkan که خود اولاما
آزمایشی می‌داندش. `ollama run` با پرامپت ۸ توکنی زنده می‌ماند؛ برنامه
با پرامپت ۱۲۲۲ توکنی نه. **مرگ اجراکننده در این پیکربندی
اجتناب‌ناپذیر است و هیچ تنظیمی در سمت ما آن را حذف نمی‌کند.**

### تغییر راهبرد

۱. **عیب‌یاب روی دستگاه کاربر** (`ai/ollama_doctor.py` + دکمه در
   تنظیمات + `tools/ollama_doctor.py`). چهار پله از سبک به سنگین؛
   اولین پله‌ای که می‌شکند علت را لو می‌دهد. دیگر لازم نیست من حدس
   بزنم — کاربر یک دکمه می‌زند و جواب قطعی می‌گیرد.

۲. **نجات به‌جای پیشگیری.** هدف از «هرگز نشکند» به «وقتی شکست، باز هم
   پاسخ بدهد» تغییر کرد. پس از هر مرگ، پرامپت واقعاً کوچک می‌شود و
   دوباره تلاش می‌کند.

### نقصی که فقط اندازه‌گیری پیدا کرد

نسخهٔ اول رفع نجات، فقط `num_predict` را نصف می‌کرد. با شبیه‌سازی
دیدم پرامپت روی ۴٬۴۱۱ نویسه **ثابت** می‌ماند و هر دو تلاش نجات همان
اندازه را می‌فرستادند. علت: پیام سیستمیِ ۱۲۲۲ توکنی هرگز کوتاه نمی‌شد
و به‌تنهایی از پنجرهٔ نجات (۱۰۲۴) بزرگ‌تر بود. اگر فقط آزمون واحد
می‌نوشتم و خروجی واقعی را نمی‌دیدم، این را نمی‌گرفتم.

### درس روش‌شناختی (چهارمین بار، و این بار ساختاری)

سه بار سعی کردم با فکرکردن به مسئله‌ای برسم که فقط با **دیدن** حل
می‌شد. وقتی یک اشکال روی دستگاهی رخ می‌دهد که به آن دسترسی ندارم،
درست‌ترین کار در **اولین** قدم ساختن ابزار اندازه‌گیری است، نه در
چهارمین. هر نسخهٔ حدسی، اعتماد کاربر را خرج می‌کند.

---

## نسخهٔ ۱٫۹٫۹ — لاگ اولاما، و سه نسخه فرض غلط

کاربر لاگ `ollama serve` را فرستاد. دو خط، سه نسخه کار را تصحیح کرد:

```
library=Vulkan name="NVIDIA GeForce GT 740" total="2.0 GiB" available="1.7 GiB"
msg="vram-based default context" total_vram="2.0 GiB" default_num_ctx=4096
```

### فرض غلطی که سه نسخه دوام آورد

کاربر ۱۶ گیگابایت **رم سیستم** دارد و من همهٔ محاسباتم را روی همان
بنا کرده بودم. ولی اولاما پنجره را از روی **VRAM** حساب می‌کند و کارت
او ۲ گیگابایت است. یعنی:

| نسخه | پنجره را از کجا می‌گرفتم | نتیجه |
|------|---------------------------|--------|
| ۱٫۹٫۷ | طول پرامپت | متغیر ⇒ بارگذاری دوباره |
| ۱٫۹٫۸ | رم سیستم (۱۶ گیگ) ⇒ ۸۱۹۲ | ثابت ولی **دو برابر ظرفیت کارت** |
| ۱٫۹٫۹ | **نمی‌گیرم؛ کار اولاماست** | درست |

روی کارت ۱٫۷ گیگابایتیِ آزاد، تفاوت ۴۰۹۶ و ۸۱۹۲ یعنی حافظهٔ KV از
۰٫۵ به ۱٫۰ گیگابایت می‌رفت — در حالی که وزن مدل هم باید جا می‌شد.
اجراکننده همان‌جا می‌مرد.

نکتهٔ دردناک: در ۱٫۹٫۸ نوشتم «هزینه‌اش فقط ۰٫۵ گیگابایت است» و همان را
دلیل بی‌خطر بودنش دانستم. آن ۰٫۵ گیگابایت را با **۱۶ گیگابایت رم**
سنجیده بودم که واقعاً ناچیز است؛ ولی باید با **۱٫۷ گیگابایت VRAM آزاد**
می‌سنجیدم که در آن مقیاس تعیین‌کننده است. عدد درست بود، مخرج غلط.

### آنچه انجام شد

* **دیگر `num_ctx` نمی‌فرستیم.** اولاما به سخت‌افزار دسترسی دارد و ما
  نداریم. این هم‌زمان هر دو ایراد را حل می‌کند: نه عدد نادرست تحمیل
  می‌شود و نه پنجره وسط کار عوض می‌شود — چون اصلاً عددی در کار نیست.
* **`ai.ollama_max_context` همچنان محترم است** برای کاربری که آگاهانه
  عددی می‌گذارد؛ آن مقدار ثابت و بدون تغییر فرستاده می‌شود.
* **کوتاه‌سازی پرامپت** بر پایهٔ محافظه‌کارانه‌ترین پنجرهٔ ممکن (۴۰۹۶).
  خطای این فرض همیشه به سمت امن است: اگر دستگاه بزرگ‌تر باشد، پرامپت
  کوتاه‌شده راحت‌تر جا می‌شود.
* **`num_predict` می‌ماند** — پارامتر زمان تولید است، نه بارگذاری.

### درس روش‌شناختی (سومین بار)

سه بار پشت سر هم فرض کردم می‌دانم منبع محدودیت کجاست، بی‌آنکه از خود
سیستم بپرسم. لاگ `ollama serve` از همان اول این را می‌گفت. **وقتی
سامانه‌ای خودش گزارش می‌دهد با چه عددی کار می‌کند، آن گزارش را بخوانید
پیش از آنکه عدد بهتری حساب کنید.**

و قاعدهٔ کلی‌تر: هر جا کد ما و یک سامانهٔ بیرونی هر دو می‌توانند یک
تصمیم را بگیرند، اگر آن سامانه اطلاعات بیشتری دارد، تصمیم مال اوست.

---

## نسخهٔ ۱٫۹٫۸ — نیمهٔ دومِ همان اشکال

کاربر تصویر مرورگر را فرستاد (`localhost:11434` ⇒ «Ollama is running»)
با این گمان که پورت را اشتباه نوشته‌ام.

### پورت درست بود — و خودِ متن خطا این را ثابت می‌کند

برنامه به `http://127.0.0.1:11434` وصل می‌شود و `localhost:11434` را هم
به‌عنوان جایگزین می‌آزماید. شاهد قاطع در متن خطاست: عبارت
«an error was encountered while running the model» را **خود اولاما**
می‌سازد و نام مدل در آن آمده است. اگر پورت اشتباه بود، پیام
«connection refused» می‌گرفتیم و اصلاً نام مدلی در کار نبود.

یعنی: درخواست رسیده، مدل بارگذاری شده، و **وسط تولید پاسخ** مرده است.

### اشکال واقعی: استدلال ناقص خودم در ۱٫۹٫۷

در ۱٫۹٫۷ نتیجه گرفتم «ترمینال `num_ctx` نمی‌فرستد، پس ما هم وقتی لازم
نیست نفرستیم» و با یک پیام کوتاه آزمودمش؛ درست کار کرد و قانع شدم.
ولی یک نوبت چت **چند دور** دارد و هر دور نتیجهٔ ابزارها را به تاریخچه
می‌افزاید. با اندازه‌گیری روی پرامپت واقعی (پیام سیستمی با ۱۴ ابزار =
۱۲۲۲ توکن):

| دور | پرامپت | فرستاده می‌شد | پنجرهٔ مؤثر |
|-----|--------|----------------|--------------|
| ۱   | ۱۲۳۲   | هیچ            | ۴۰۹۶ پیش‌فرض |
| ۲   | ۲۰۰۷   | هیچ            | ۴۰۹۶ |
| ۳   | ۲۷۰۷   | num_ctx=۸۱۹۲   | ۸۱۹۲ ← **بارگذاری دوباره** |

خطای من این بود که «نفرستادن» را با «بدون پنجره» یکی گرفتم. نفرستادن
یعنی پنجرهٔ پیش‌فرض ۴۰۹۶ — که خودش یک مقدار است. پس گذار ۴۰۹۶ → ۸۱۹۲
وسط یک نوبت رخ می‌داد و مدل چندگیگابایتی کامل تعویض می‌شد.

**اصل درست «سکوت» نیست، «ثبات» است.**

### آنچه انجام شد

* **پنجرهٔ چسبنده:** یک مقدار (سقف امن دستگاه) یک بار انتخاب و در همهٔ
  درخواست‌ها تکرار می‌شود، حتی برای «سلام». هزینه‌اش اندازه‌گیری شد:
  روی `llama3.1:8b` تفاوت حافظهٔ ۴۰۹۶ و ۸۱۹۲ فقط ۰٫۵ گیگابایت است
  (۵٫۴۰ در برابر ۵٫۹۰ از ۱۶ گیگابایت) — معاملهٔ بسیار خوبی در برابر
  حذف کامل بارگذاری‌های دوباره.
* **قفل ترتیبی:** اولاما پیش‌فرض همزمانی ندارد و هر درخواست همزمان یک
  حافظهٔ KV جدا می‌گیرد. چت کاربر و بازبینی خودکار سیگنال می‌توانستند
  هم‌زمان شوند. حالا در هر لحظه فقط یک درخواست در راه است.
* **کوتاه‌سازی آگاهانه:** با پنجرهٔ ثابت، پرامپت بلندتر را خودمان کوتاه
  می‌کنیم تا اولاما بی‌صدا ابتدای آن (شامل دستورالعمل سیستمی) را نبرد.
* **پیام خطای صادق:** دیگر اندازهٔ مدل را مقصر اعلام نمی‌کند.

### درس روش‌شناختی (دومین بار در این پروژه)

آزمون ۱٫۹٫۷ من با یک پیام کوتاه انجام شد و سبز بود — ولی اشکال در
دور سوم یک گفت‌وگو زندگی می‌کرد. **سناریوی آزمون باید شکل واقعی
استفاده را داشته باشد، نه ساده‌ترین شکل ممکن.**

همچنین آزمون سرصفحهٔ همین نسخه را اول ضعیف نوشتم (`None` را از شمارش
کنار می‌گذاشت) و وقتی کد را عمداً به ۱٫۹٫۷ برگرداندم، سبز ماند. آزمونی
که نسخهٔ معیوب را نگیرد، آزمون نیست. تقویتش کردم تا `None` را همان
۴۰۹۶ بشمارد — و آن وقت *رفع اولیهٔ خودم* را هم رد کرد، که درست بود.

---

## نسخهٔ ۱٫۹٫۷ — علت واقعی «در ترمینال کار می‌کند، در برنامه نه»

کاربر تصویر ترمینالش را فرستاد: چهار مدل نصب‌شده، `ollama run
llama3.1:8b` بی‌نقص. این تصویر دو فرضیهٔ قبلی را یک‌جا باطل کرد.

### فرضیهٔ غلطی که خودم ساخته بودم

در نسخهٔ ۱.۹.۶ نتیجه گرفتم `deepseek-r1:8b` روی ۱۶ گیگابایت جا
نمی‌شود و ضریب‌ها را طوری کالیبره کردم که این را بگویند. تصویر
`ollama list` نشان داد آن مدل روی دیسک ۵٫۲ گیگابایت است و کار می‌کند.

این یک درس روش‌شناختی است: وقتی برای توضیح یک نشانه فرضیه‌ای می‌سازم و
بعد پارامترها را طوری تنظیم می‌کنم که فرضیه را تأیید کنند، دیگر
اندازه‌گیری نکرده‌ام — فرضیه را در کد حک کرده‌ام. هشدار حاصل روی
مدل‌های سالم کاربر روشن می‌شد، که بدتر از نبودنش است: کاربر را عادت
می‌دهد همهٔ هشدارها را نادیده بگیرد.

### اندازه‌گیری به‌جای حدس

یک سرور HTTP ثبت‌کننده نوشتم که فقط چاپ می‌کند برنامه چه می‌فرستد:

| پارامتر     | `ollama run` | برنامه          |
|-------------|--------------|-----------------|
| `num_ctx`     | نمی‌فرستد    | ۲۰۴۸ یا ۸۱۹۲    |
| `num_predict` | نمی‌فرستد    | ۱۶۰۰            |
| `keep_alive`  | پیش‌فرض      | نمی‌فرستاد      |

**`num_ctx` متغیر، علت بود.** اولاما مدل را با یک اندازهٔ پنجرهٔ مشخص
در حافظه بارگذاری می‌کند؛ هر مقدار متفاوت یعنی تخلیه و بارگذاری
دوبارهٔ چند گیگابایت. برنامه برای چت ۲۰۴۸ می‌فرستاد و برای تحلیل
۸۱۹۲، یعنی تقریباً هر درخواست یک چرخهٔ کامل. ترمینال هیچ‌وقت این کار
را نمی‌کند — و دقیقاً به همین دلیل کار می‌کرد.

رفع: تا وقتی پرامپت زیر پنجرهٔ پیش‌فرض مدل بماند، هیچ `num_ctx`
فرستاده نمی‌شود. فقط تحلیل‌های بزرگ که واقعاً به پنجرهٔ بزرگ‌تر نیاز
دارند آن را صریح تعیین می‌کنند — چون بدون آن اولاما بی‌صدا ابتدای
پرامپت را می‌برد و مدل نصف داده‌های بازار را نمی‌بیند.

### نقص دومی که همان‌جا پیدا شد

روی یک پیام کوتاه چت، پنجره ۲۰۴۸ انتخاب می‌شد و برنامه ۱۶۰۰ توکن پاسخ
می‌خواست: ۷۸٪ کل پنجره فقط بافر تولید. `_reply_budget()` حالا سقف
پاسخ را به فضای واقعاً باقی‌مانده محدود می‌کند، با کفی که پاسخ را
بی‌فایده نکند.

### keep_alive

پیش‌فرض اولاما ۵ دقیقه است. برای برنامه‌ای که کاربر بین دو پرسش فکر
می‌کند کوتاه است؛ هر بار بارگذاری دوبارهٔ چند گیگابایت هم کند است و هم
پرخطرترین لحظه برای مرگ اجراکننده. حالا ۳۰ دقیقه.

### آزمون

`tests/test_v196_fixes.py` به ۵۵ آزمون رسید. مهم‌ترینشان
`test_a_short_chat_sends_no_context_override` و
`test_the_users_four_models_all_take_the_quiet_path` است — دومی
مستقیماً از تصویر ترمینال کاربر ساخته شده و هر چهار مدل نصب‌شده‌اش را
جداگانه بررسی می‌کند.

## نسخهٔ ۱٫۹٫۶ — سه گزارش از ماشین کاربر

سه چیز گزارش شد: یک کرش در ترمینال، یک شکست در چت، و یک درخواست
ظاهری. دو تای اول باگ واقعی بودند و هر دو بازتولید شدند پیش از آنکه
دست به کد زده شود.

### کرش: بی‌نهایت در شمارش معکوس

`OverflowError: cannot convert float infinity to integer` از
`seconds_until_next()` می‌آمد. `_elapsed()` برای «هرگز اجرا نشده»
بی‌نهایت برمی‌گرداند، پس `full_interval - inf` منفیِ بی‌نهایت می‌شد.

آنچه این باگ را جدی می‌کرد، محل وقوعش بود: داخل تیک تایمر. یعنی
**هر تیک دوباره می‌ترکید** و کاربر سیلی از خطا می‌دید، نه یک خطا.

رفع، یک تصمیم معنایی بود نه فنی: «هرگز اجرا نشده» یعنی «همین حالا
موعدش است»، پس صفر. هر عدد دیگری شروع سامانه را عقب می‌انداخت.
آزمون‌ها هر چهار ترکیب چرخش‌کامل/تمرکز را پوشش می‌دهند، چون کرش
دقیقاً در ترکیبی رخ داد که کسی جداگانه آزمونش نکرده بود.

### چت: نردبانی که هرگز به پله نمی‌رسید

خطا این بود: `wsarecv: An existing connection was forcibly closed`.
در نگاه اول شبیه همان مشکل نسخهٔ ۱.۹.۵ به نظر می‌رسید، ولی نبود.

آنجا پنجرهٔ متن بزرگ بود و مدل اصلاً بارگذاری نمی‌شد. اینجا مدل
بارگذاری می‌شد و **وسط تولید پاسخ** می‌مرد. دو علت متفاوت با دو
درمان متفاوت: اولی با کوچک‌کردن پنجره حل می‌شود، دومی با تلاش مجدد و
بارگذاری دوبارهٔ مدل.

مهم‌تر از آن، یک نقص منطقی پیدا شد: نردبان جبرانِ نسخهٔ قبل شرط
`num_ctx > 4096` داشت و چت پنجرهٔ ۲۰۴۸ می‌گیرد. یعنی **در چت هیچ
تلاش مجددی انجام نمی‌شد** — تنها یک درخواست می‌رفت و بی‌درنگ «همهٔ
ارائه‌دهنده‌ها شکست خوردند» برمی‌گشت. روی مقلدی که همان پیام ویندوزی
را می‌داد بازتولید شد: یک درخواست، بدون تلاش مجدد. پس از رفع: سه
درخواست با `num_predict` نصف‌شونده، و موفقیت.

### علت پشت علت

تلاش مجدد، نشانه را درمان می‌کند. علت این است که `deepseek-r1:8b`
روی ۱۶ گیگابایت جا نمی‌شود.

`ai/local_model_fit.py` حالا پیش از انتخاب هشدار می‌دهد. نکتهٔ
کالیبراسیون مهم بود: با ضریب اولیه (۰٫۷۵ گیگابایت به ازای هر میلیارد
پارامتر) آن مدل «جا می‌شود» ارزیابی می‌شد — که با تجربهٔ واقعی کاربر
تناقض داشت. ضریب از روی گزارش او اصلاح شد، نه از روی نظریه.

مدل‌های استدلالی ضریب ۱٫۵ برابری می‌گیرند چون بلوک `<think>` کش KV را
چند برابر می‌کند؛ به همین دلیل مدل استدلالی ۸ میلیاردی می‌میرد در
حالی که مدل معمولی هم‌اندازه کار می‌کند. هیچ مدل استدلالی‌ای پیشنهاد
نمی‌شود — یعنی کاربر را به همان چاه دوباره نفرستیم.

یک حالت مرزی هم اضافه شد: دستگاهی که پس از کسر سهم سیستم چیزی برایش
نمی‌ماند. «۰ گیگابایت آزاد دارید» شبیه خطای برنامه به نظر می‌رسد، پس
پیام مخصوص خودش را دارد که به سرویس ابری راهنمایی می‌کند.

### پوستهٔ پیش‌فرض

«سرمه‌ای اداری» که از قبل در کاتالوگ بود، پیش‌فرض شد. یک آزمون موجود
(`test_default_theme_is_glass_dark`) دستور پیشین را کدگذاری کرده بود و
به‌روز شد — دستور تازه، دستور قبلی را نسخ می‌کند.

مهم‌ترین بررسی این بخش، آزمون بازنویسی‌نشدن بود: برنامه اجرا شد،
پوسته به طلایی تغییر داده شد، برنامه بسته و دوباره باز شد، و انتخاب
کاربر سر جایش بود. قاعدهٔ «تنظیمات کاربر هرگز بازنویسی نمی‌شود» با
عوض‌کردن یک پیش‌فرض نقض نمی‌شود.

### آزمون

`tests/test_v196_fixes.py` — ۴۶ آزمون. مهم‌ترینشان
`test_the_users_exact_model_is_flagged_on_his_machine` است: اگر
کالیبراسیون ما دوباره خوش‌بین شود، این آزمون قرمز می‌شود.

## نسخهٔ ۱٫۹٫۵ — کار سنگین روی هوش مصنوعی، اعتبار سیگنال و توصیهٔ صریح

کاربر شش چیز گفت که در نگاه اول شش کار جدا بودند: «هنوز به اولاما وصل
نمی‌شود»، «سابقهٔ سیگنال باید تحلیل هوش مصنوعی داشته باشد»، «هوش
مصنوعی باید پیشنهاد خرید و فروش بدهد»، «باید مشخص باشد در چه زمان و
تایم‌فریمی»، «باید بدانیم تا کی اعتبار دارد»، و «بعضی سیگنال‌ها سودی
نداده‌اند». پس از اندازه‌گیری معلوم شد سه‌تای اول یک ریشه دارند و
سه‌تای دوم یک ریشهٔ دیگر.

### ریشهٔ اول: پرامپت از مدل بزرگ‌تر بود

سه رفع قبلیِ اولاما کار نکرد چون همه به نشانه‌ها زده بودند نه به علت.
اندازه‌گیری نشان داد پرامپت تحلیل ۱۷ کیلوبایت است و برنامه پنجرهٔ
۱۶۳۸۴ توکنی می‌خواهد. روی ۱۶ گیگابایت رم، اجراکنندهٔ اولاما پیش از
تولید پاسخ می‌میرد و «۵۰۰ با بدنهٔ خالی» برمی‌گرداند — که برنامه آن را
«قطع اتصال» تفسیر می‌کرد. برای همین اولاما به‌تنهایی کار می‌کرد و با
این برنامه نه.

بدتر از آن: راه جبرانیِ موجود پنجره را به ۴۰۹۶ می‌آورد ولی پرامپت
۵۸۰۰ توکنی را کوچک نمی‌کرد. اولاما بی‌صدا ابتدای پرامپت را می‌برید،
مدل نصف داده‌ها را نمی‌دید و جای خالی را با حدس پر می‌کرد. این
مستقیماً به شکایت ششم وصل است: تحلیلی که معتبر به نظر می‌رسید ولی روی
دادهٔ ناقص بنا شده بود.

`ai/prompt_budget.py` حالا حافظه را می‌خواند، پنجرهٔ امن را انتخاب
می‌کند و پرامپت را در همان بودجه جا می‌دهد. ترتیب کوچک‌کردن آگاهانه
است: اول عمق دفتر سفارش (عکس لحظه‌ای که تا رسیدن پاسخ کهنه شده)، بعد
تایم‌فریم‌های میانی. تایم‌فریم اصلی و بزرگ‌ترین تایم‌فریم هرگز حذف
نمی‌شوند — بدون تصویر کلان، تحلیل کوتاه‌مدت می‌تواند خلاف روند اصلی
سیگنال بدهد. و مهم‌تر: به مدل صریحاً گفته می‌شود چه چیزی کم دارد و
حدس نزند.

مشکل دوم اولاما جدا بود: مدل‌های استدلالی پاسخ را در `<think>` می‌پیچند
و `json.loads` روی آن همیشه شکست می‌خورد.

### ریشهٔ دوم: سیگنال تاریخ مصرف نداشت

`signals/validity.py` برای هر سیگنال دو مهلت حساب می‌کند. هر دو بر
پایهٔ تعداد کندل‌اند نه ساعت ثابت؛ مهلت ساعتی برای سیگنال ۱۵ دقیقه‌ای
خیلی بلند و برای روزانه بی‌معنا بود. در تحلیل چند تایم‌فریمی
کوچک‌ترین تایم‌فریم ملاک است — انتخاب بزرگ‌تر یعنی به کاربر دروغ
بگوییم سیگنال هنوز زنده است.

مهم‌ترین نکته این بود که **کهنگی فقط مسئلهٔ زمان نیست**. سیگنال
می‌تواند دو دقیقه‌ای باشد و باز هم سوخته، اگر قیمت پیش از ورود بیشترِ
مسیر تا هدف را رفته باشد. عددی که کاربر باید ببیند نسبت ریسک به ریوارد
**در قیمت فعلی** است: سیگنالی که روی تابلو ۲٫۲۰ نشان می‌دهد، با ورود
دیرهنگام عملاً ۰٫۲۶ است. این دقیقاً همان تجربه‌ای است که کاربر داشت
ولی نمی‌توانست ببیند.

ترتیب بررسی معنادار است: ابطال (حد ضرر خورده) ← انقضا ← حرکت قیمت ←
پنجرهٔ زمانی. سیگنالی که حد ضررش خورده، دیگر مهم نیست چقدر تازه است.

### جلوگیری، نه فقط هشدار

روی سیگنال سوخته دکمهٔ «اقدام به معامله» خاموش می‌شود. هشدار بدون
جلوگیری تشریفات است، و یکی از راه‌هایی که کاربر به معاملهٔ بی‌سود
می‌رسید همین بود. یک تناقض هم در آزمون بصری پیدا شد و رفع گردید:
توصیهٔ «خرید» با سبز پررنگ، درست بالای کارتی که می‌گفت «سوخته». حالا
توصیهٔ کهنه خط‌خورده و خاکستری است.

### توصیهٔ صریح

قالب تحلیل موظف شده در آخرین خط یک ردیف ماشین‌خوان بدهد:
`RECOMMENDATION: BUY | 72 | دلیل کوتاه`. چرا نه خروجی کاملاً JSON؟
آزمودیم — وقتی از مدل ۸ میلیاردی کل تحلیل را در JSON خواستیم، کیفیت
متن افت کرد؛ مدل انرژی‌اش را صرف ساختار می‌کرد نه محتوا.

سه تصمیم در این بخش:

۱. **حدس ممنوع.** اگر مدل آن خط را ننویسد، از متن جهت استخراج
   نمی‌کنیم. «به نظر صعودی می‌آید» را به توصیهٔ خرید تبدیل‌کردن دقیقاً
   همان کاری است که اعتماد را از بین می‌برد.
۲. **«صبر کن» پاسخ معتبر است.** مدلی که مجبور به انتخاب جهت شود، جهت
   اختراع می‌کند.
۳. **اطمینانِ نداده‌شده `None` است نه صفر.** «اطمینان ۰٪» معنای کاملاً
   متفاوتی از «عددی ندادم» دارد.

### بازبینی پس از بسته‌شدن

هر سیگنالی که بسته می‌شود خودکار به بازبینی می‌رود و درسش در یکی از
هفت دستهٔ ثابت می‌نشیند. دسته‌های ثابت عمدی‌اند: با متن آزاد، صفحهٔ
آمار پر از دسته‌های تک‌عضوی می‌شد و هیچ الگویی دیده نمی‌شد. اگر بیشتر
درس‌ها «ورود دیرهنگام» باشد، مشکل موتور سیگنال نیست — سرعت واکنش است.

بازبینی‌ها **یکی‌یکی** انجام می‌شوند نه موازی؛ چند درخواست هم‌زمان به
مدل محلی حافظه را پر می‌کند. دسته‌ها هم سه‌تایی‌اند چون روی ماشین خود
کاربر اجرا می‌شوند.

### آزمون

`tests/test_v195_ai_and_validity.py` — ۸۶ آزمون. مهم‌ترینشان
`test_no_line_means_no_guessing` است: آزمونی که مطمئن می‌شود برنامه
وقتی نمی‌داند، وانمود نمی‌کند که می‌داند.

## نسخه ۱.۳ — هوش مصنوعی، مدل‌های رایگان، و چت

- **رفع ایراد Ollama** («اجرا هست ولی وصل نمی‌شود»): سه علت — نام مدل،
  ترجمهٔ `localhost` به IPv6، و مهلت کوتاه. جزئیات در `NEXT_TASK.md`.
- **رده‌بندی مدل‌ها** (`ai/providers/free_models.py`): رایگان → کم‌هزینه →
  پولی. گزینهٔ «فقط مدل‌های رایگان» پیش‌فرض روشن.
- **بخش چت هوشمند** (`ai/agent/chat_agent.py` + `ui/pages/chat_page.py`):
  گفتگوی چندنوبتی با دسترسی به ۱۲ ابزار داده زنده. اقدام‌ها فقط پیشنهاد
  می‌شوند و نیاز به تأیید کاربر دارند.
- **کلید OpenAI کاربر** رمزنگاری‌شده ذخیره شد. کلید معتبر است ولی حساب
  اعتبار ندارد (`insufficient_quota`).
- **خطاهای واقعی سرویس** به‌جای کد HTTP خام + سازگاری با مدل‌های
  نسل جدید (`max_completion_tokens`, دمای ثابت).
- راهنمای تازه: `docs/AI_SETUP_FA.md`

---

## ✅ همه فازها تکمیل شد

### فاز ۱ — معماری و زیرساخت
- ساختار ماژولار، `pyproject.toml`، `requirements.txt`، `.gitignore`، `.env.example`
- `app/core/` — `paths` (پشتیبانی frozen)، `constants`، `models`، `events`
- `app/exceptions/` — سلسله‌مراتب کامل خطا با `message` / `user_key` / `details`
- `app/logging/` — چرخش لاگ + `SensitiveDataFilter` (ماسک خودکار کلید API — **آزموده شد**)
- `app/config/` — `AppSettings` + ۵۴ تنظیم در ۱۰ دسته + `SettingsService`
- `app/security/` — `SecretStore` با Keyring/DPAPI و جایگزین رمزنگاری‌شده (**بدون نشت متن ساده**)
- `app/database/` — ۱۳ جدول + ۸ مخزن

### فاز ۲ — لایه بازار
- `market/providers/base.py` — قرارداد عمومی `ExchangeProvider` (هسته به LBank وابسته نیست)
- `market/providers/lbank/` — REST + WebSocket (**تست زنده با API واقعی**)
- `market/timeframes.py` — ۱۳ تایم‌فریم با تجمیع خودکار
- `market/rate_limiter.py`، `market/cache/`، `market/engine.py` (کش → REST → پایگاه داده → خطا)

### فاز ۳ — موتور اندیکاتور
- **۲۴ اندیکاتور** در ۵ دسته، مستقل از کتابخانه بومی
- `IndicatorEngine.calculate_many()` با ایزوله‌سازی خطای هر اندیکاتور
- تحلیل ساختار بازار، سطوح حمایت/مقاومت، تشخیص روند

### فاز ۴ — لایه هوش مصنوعی
- `AIProvider` + Local (Ollama) / OpenAI-compatible / Custom با جایگزینی بر پایه اولویت
- `MarketToolset` با ۱۲ ابزار؛ مدل هرگز داده نمی‌سازد
- `ResponseValidator` با تعمیر و اعتبارسنجی خروجی JSON
- پرامپت‌های نسخه‌دار در `ai/prompts/` (نه در کد)

### فاز ۵ — موتور سیگنال و ریسک
- `signals/engine.py` — رأی‌گیری ۳ استراتژی با وزن تایم‌فریم؛ آستانه ۰٫۲۲
- `signals/risk_engine.py` — حد ضرر ساختاری/ATR، محدودسازی فاصله، ارزیابی و رد ستاپ
- خروجی `LONG` / `SHORT` / `WAIT` + ورود، حد ضرر، ۳ هدف، R/R، اهرم، اطمینان، دلیل، ابطال
- «اطمینان» صریحاً به‌عنوان هم‌جهتی عوامل برچسب خورده، نه احتمال سود

### فاز ۶ — پشتیبان‌گیری و مهاجرت
- `backup/manager.py` — ساخت، اعتبارسنجی، بازیابی؛ WAL checkpoint و حذف فایل‌های جانبی
- Alembic با پشتیبان خودکار پیش از مهاجرت (head: `7af2c2624f88`)
- **رمزها هرگز داخل پشتیبان نمی‌روند** (آزمون خودکار)

### فاز ۷ — بومی‌سازی و گزارش‌گیری
- ۲۵۴ کلید ترجمه در هر زبان (فارسی RTL / انگلیسی LTR)، بدون کلید گم‌شده
- `reports/` — ساخت گزارش + خروجی CSV / Excel / JSON / PDF / HTML (هر پنج قالب آزموده شد)
- بدون فیلد «نرخ برد» — چون معامله واقعی وجود ندارد

### فاز ۸ — رابط گرافیکی
- ۷ صفحه، `MainWindow` با ناوبری، پوسته تیره/روشن/سیستمی، ویزارد اولین اجرا
- تعویض زبان و پوسته در زمان اجرا بدون بستن برنامه

### فاز ۹ — یکپارچه‌سازی (تکمیل شد)
- `app/application.py` — ریشه ترکیب و تزریق وابستگی
- `main.py` — GUI + `--check` + `--signal` + `--log-level`

### فاز ۱۰ — نمودار و اتصال رابط به موتورها (تکمیل شد)
- `ui/charts/` — `CandlestickItem` (ترسیم یک‌باره روی QPicture) + `PriceChart` با حجم، خطوط
  میانگین متحرک، سطوح کلیدی، نشانگر متقاطع و علامت‌گذاری ورود/حد ضرر/اهداف
- `ui/controllers/async_runner.py` — حلقه asyncio روی نخ جدا؛ نتیجه از راه سیگنال Qt
- `ui/controllers/main_controller.py` — اتصال کامل همه صفحات به موتورها
- رابط کاربری هرگز یخ نمی‌زند؛ هیچ ویجتی از نخ پس‌زمینه دست‌کاری نمی‌شود

### فاز ۱۱ — آزمون، بسته‌بندی و مستندات (تکمیل شد)
- **۹۲ آزمون** در ۹ فایل، همه سبز
- `CryptoAITrader.spec` + `scripts/` (build_windows.bat، build.sh، run_dev.sh، run_tests.sh، migrate.sh)
- `README.md` (فارسی) + `README_EN.md` + ۷ سند در `docs/`

---

## 🧪 وضعیت آزمون‌ها

| فایل | تعداد | موضوع |
|---|---|---|
| `test_indicators.py` | ۱۰ | درستی ریاضی اندیکاتورها |
| `test_risk_engine.py` | ۱۲ | حد ضرر، R/R، اندازه موقعیت، رد ستاپ بد |
| `test_signal_engine.py` | ۹ | جهت‌گیری، جدایی هوش مصنوعی از قیمت‌ها |
| `test_ai_validator.py` | ۱۲ | مهار خروجی نامعتبر مدل |
| `test_timeframes.py` | ۷ | تجمیع و هم‌ترازی زمان |
| `test_localization.py` | ۱۱ | نبود کلید گم‌شده |
| `test_backup.py` | ۹ | اعتبار پشتیبان و نبود رمز در آن |
| `test_reports.py` | ۱۴ | هر پنج قالب، نبود ادعای نرخ برد |
| `test_async_runner.py` | ۸ | مهار خطا در نخ پس‌زمینه |
| **جمع** | **۹۲** | همه سبز |

---

## 🐞 اشکال‌های واقعی که آزمون‌ها پیدا کردند

1. **`risk_engine.calculate_stop_loss`** — شاخه «بدون سطح ساختاری» زودهنگام `return` می‌کرد و
   محدودسازی بیشینه فاصله حد ضرر را دور می‌زد. بازنویسی شد تا محدودسازی روی **هر مسیر** اجرا شود.
2. **مقایسه اعشاری در `assess()`** — اهداف سود پس از گرد شدن، R/R را ۱٫۴۹۹۹۹۹۹ می‌کردند و ستاپی که
   دقیقاً حداقل کاربر را برآورده می‌کرد رد می‌شد. `FLOAT_TOLERANCE = 1e-6` افزوده شد.
3. **جهت چیدمان پس از تعویض زبان** — ویجت‌هایی که یک بار جهتشان دستی تنظیم شده بود روی حالت قبلی
   می‌ماندند. حالا صفت `WA_SetLayoutDirection` از همه فرزندان برداشته می‌شود.
4. **محتوای پویا پس از تعویض زبان** — جدول‌ها ارقام فارسی را زیر عنوان‌های انگلیسی نگه می‌داشتند.
   `_rerender_dynamic_content()` افزوده شد.

---

## ⚠ محدودیت‌های شناخته‌شده (عمدی)

- **بدون معامله واقعی** — نسخه ۱ فقط تحلیل است؛ هیچ سفارشی ثبت نمی‌شود.
- ~~برچسب‌های PDF انگلیسی‌اند~~ — **در نسخهٔ ۱.۴ برطرف شد.** ماژول `reports/persian_pdf.py`
  با قلم Vazirmatn و شکل‌دهی حروف، خروجی PDF کاملاً فارسی و راست‌به‌چپ می‌سازد.
- **پنج تایم‌فریم در LBank بومی نیستند** (`3m, 2h, 6h, 8h, 12h`) و تجمیع می‌شوند.
- **هوش مصنوعی پیش‌فرض خاموش است** — برنامه بدون هیچ سرویس پولی کامل کار می‌کند.

---

## 📦 اجرا و ساخت

```bash
python main.py                      # رابط گرافیکی
python main.py --check              # بررسی سلامت
python main.py --signal BTC/USDT    # سیگنال در خط فرمان
scripts/run_tests.sh                # آزمون‌ها
scripts\build_windows.bat           # ساخت نسخه ویندوز
```

---

# نسخهٔ ۱.۴ — شش درخواست تازهٔ کاربر

| # | درخواست | وضعیت |
|---|---|---|
| ۱ | سرعت پایین دریافت داده | ✅ انجام شد |
| ۲ | مدال جزئیات با کلیک روی نام ارز | ✅ انجام شد |
| ۳ | مرتب‌سازی بازارها بر پایهٔ ارزش + کلیک روی ردیف | ✅ انجام شد |
| ۴ | سیگنال زیر نظر هوش مصنوعی + تحلیل فارسی + PDF | ✅ انجام شد |
| ۵ | بازطراحی چت مانند GPT + ذخیره و حذف گفت‌وگوها | ✅ انجام شد |
| ۷ | گسترش تنظیمات | ✅ انجام شد |

## ۱. سرعت

| سنجه | پیش | پس |
|---|---|---|
| نماد نامعتبر | ۲٫۶ ثانیه | **۰٫۱۴ ثانیه** |
| `signals.generate` (سرد) | — | ۰٫۶۹ ثانیه |
| `signals.generate` (گرم) | — | **۰٫۲۴ ثانیه** |
| کندل چهار تایم‌فریم | ۰٫۶۳ ثانیه (پشت‌سرهم) | **۰٫۱۹ ثانیه** (هم‌زمان) |

نکتهٔ مهمی که اندازه‌گیری نشان داد: **هم‌زمان‌سازی کورکورانه کندتر است.**
گرفتن ۸ تیکر با `gather` دو و نیم برابر کندتر از حالت پشت‌سرهم بود (۲٫۶۷ در
برابر ۱٫۱۱ ثانیه) چون صرافی درخواست‌های موازی یک حساب را صف می‌کند. به‌جای
آن از `get_all_tickers()` انبوه استفاده شد و `gather` فقط برای فراخوانی‌های
مستقل (کندل چند تایم‌فریم) به کار رفت.

همچنین: خطاهای دائمی صرافی (مانند ۱۰۰۰۸ = جفت‌ارز پشتیبانی‌نشده) دیگر
تلاش مجدد نمی‌شوند، حافظهٔ نهان روی دیسک ماندگار شد و صفحه‌ها تنبل بارگذاری
می‌شوند.

## ۲ و ۳. مدال جزئیات ارز و مرتب‌سازی بازارها

- کلیک روی هر ردیف در **داشبورد** و **بازارها** مدال `CoinDetailDialog` را باز می‌کند.
- مدال: قیمت، تغییر، بالاترین/پایین‌ترین، حجم، ارزش معاملات، معادل تومان و
  جزئیات فنی (روند، RSI، حمایت/مقاومت) که در پس‌زمینه بارگذاری می‌شوند.
- دکمه‌ها: تحلیل هوش مصنوعی، تولید سیگنال روی تایم‌فریم انتخابی (۱۴ تایم‌فریم)،
  رفتن به بازارها، گفت‌وگو دربارهٔ همین ارز، افزودن/برداشتن از واچ‌لیست.
- مرتب‌سازی پیش‌فرض بازارها **بر پایهٔ ارزش معاملات** است. هفت حالت در فهرست
  کشویی بالای جدول: ارزش، حجم، بیشترین رشد، بیشترین افت، قیمت (نزولی/صعودی) و نام.
- سرستون‌ها هم قابل کلیک‌اند و **عددی** مرتب می‌کنند (نه متنی).

## ۴. سیگنال زیر نظر هوش مصنوعی

سه حالت در تنظیمات:

| حالت | رفتار |
|---|---|
| **ترکیبی** (پیش‌فرض) | موتور ریاضی همیشه سیگنال می‌دهد، هوش مصنوعی بازبینی می‌کند و تحلیل فارسی می‌نویسد |
| **کاملاً هوش مصنوعی** | تصمیم نهایی با مدل است |
| **فقط موتور** | سریع‌ترین، بدون هوش مصنوعی |

حالت ترکیبی پیش‌فرض است چون **موتور ریاضی هرگز بی‌پاسخ نمی‌ماند**؛ همان
ایرادی که گزارش کردید («سیگنالی تولید نمی‌کند») ریشه‌اش وابستگی کامل به
سرویس هوش مصنوعی بود.

ستون هفتم جدول سابقه دکمهٔ «📄 مشاهدهٔ تحلیل» دارد که مدال تحلیل نوشتاری
فارسی را باز می‌کند و از آنجا **خروجی PDF فارسی** گرفته می‌شود.

## ۵. چت مانند GPT

- ورودی **چندخطی** که با متن بلند می‌شود (پیش‌تر یک‌خطی بود و متن بریده می‌شد).
- Enter می‌فرستد، Shift+Enter خط تازه می‌سازد.
- حباب‌ها متن را **کامل و قابل انتخاب** نشان می‌دهند؛ عرض حباب نسبتی از پنجره است.
- ستون کناری تاریخچه با جست‌وجو، دکمهٔ «گفت‌وگوی تازه»، تغییر نام، سنجاق و حذف.
- همهٔ گفت‌وگوها در **پایگاه دادهٔ برنامه** ذخیره می‌شوند (پس در پشتیبان‌گیری هم می‌آیند).

## ۷. تنظیمات گسترش‌یافته

دو زبانهٔ تازه (۷ زبانه در مجموع) و ۱۹ گزینهٔ تازه:

- **نمایش:** ترتیب پیش‌فرض بازارها، اندازهٔ قلم (۸۰–۱۶۰٪)، حالت فشرده،
  تأیید پیش از کارهای مهم، نمایش ستون تومان.
- **کارایی:** حالت سیگنال، تحلیل نوشتاری، سقف انتظار هوش مصنوعی، ذخیرهٔ
  تاریخچه، سقف گفت‌وگوها، عمر حافظهٔ نهان، درخواست‌های هم‌زمان، فاصلهٔ
  تازه‌سازی داشبورد و بازارها، سقف انتظار شبکه، بیشترین تلاش مجدد.

تنظیمات **بی‌درنگ** اعمال می‌شوند؛ نیازی به بستن برنامه نیست.

## آزمون‌ها

**۳۸۶ آزمون** (پیش‌تر ۲۰۴). آزمون‌های تازه: مخزن چت (۱۶)، تحلیل نوشتاری
(۱۸)، PDF فارسی (۱۷)، مرتب‌سازی و مدال (۲۹)، چت نسخهٔ ۲ (۳۰)، تنظیمات (۷۲).

---

# نسخهٔ ۱٫۵ — پوسته‌ها، حساب کاربری و صرافی

## ۱. تحلیل بستهٔ طراحی

۴۰ تصویر ارسالی بررسی شد. نتیجهٔ کلیدی: **۴۰ طرح جدا نبودند.** با مقایسهٔ
رنگ پس‌زمینه، شعاع گوشه، سبک کارت، نوار کناری و تایپوگرافی مشخص شد که
۴ پوسته × ۱۰ صفحه هستند. گزارش کامل (نگاشت تصویر ← پوسته ← صفحه، جدول
شکاف‌ها، معماری) در `docs/DESIGN_ANALYSIS_FA.md`.

| پوسته | نام فارسی | نشانهٔ تشخیص |
|---|---|---|
| `glass_dark` | شیشه‌ای تیره (**پیش‌فرض**) | `#0a0e1a`، پنل نیمه‌شفاف، فیروزه‌ای→بنفش |
| `minimal_light` | روشن مینیمال | سفید، بی‌حاشیه، شعاع کم |
| `corporate_navy` | سرمه‌ای اداری | نوار کناری `#0f2347`، تأکید طلایی |
| `violet_light` | بنفش روشن | `#faf9ff`، بنفش `#6d28d9`، شعاع ۱۴–۲۲ |

## ۲. معماری پوسته

`ui/themes/` شامل `tokens.py` (توکن‌های تغییرناپذیر)، `catalog.py` (چهار
پوسته)، `stylesheet.py` (تولید QSS) و `theme_manager.py`.

قاعده‌ای که در سراسر کد رعایت شده و آزمون دارد:

> **در کد قابلیت، هیچ `if theme == ...` وجود ندارد.**

برگه سبک فقط با ویژگی `role=` هدف‌گذاری می‌کند. ویجت‌های نقاشی‌شده
(نمودارها) رنگ را از `apply_theme(tokens)` می‌گیرند چون QSS به آن‌ها
نمی‌رسد. `MainWindow.apply_theme_tokens()` این را به همهٔ صفحه‌ها می‌رساند.

تغییر پوسته **هیچ ویجتی را بازنمی‌سازد**: داده‌های جدول، متن چت و نماد
انتخابی سر جای خود می‌مانند.

## ۳. صفحه‌های تازه (۸ ← ۱۰)

- `ui/pages/trades_page.py` — تاریخچهٔ معاملات با فیلتر تاریخ/نماد/جهت/
  وضعیت، نوار چهار معیار، صفحه‌بندی، خروجی CSV.
- `ui/pages/wallet_page.py` — کیف پول با سه کارت آمار، جدول دارایی،
  دونات سهم و نمودار رشد سرمایه.

## ۴. پوستهٔ بیرونی پنجره

`ui/widgets/chrome.py`: `TopBar` (جستجوی سراسری، اعلان، منوی کاربر،
کلید پوسته، حالت تمرکز)، `IconSidebar` (۲۳۶px / ۶۲px جمع‌شده، کارت
اتصال)، `FeatureStrip`.

میان‌برها در یک ثبت‌گاه متمرکز: `Ctrl+K` جستجو، `Ctrl+R` تازه‌سازی،
`Ctrl+B` نوار کناری، `Ctrl+T` چرخش پوسته، `Ctrl+Shift+F` حالت تمرکز،
`Ctrl+1..0` صفحه‌ها.

## ۵. حساب کاربری

| لایه | فایل |
|---|---|
| رمزنگاری | `app/security/passwords.py` |
| داده | `app/database/repositories/user_repository.py` |
| سرویس | `app/core/auth_service.py` |
| رابط | `ui/dialogs/auth_dialog.py` |

PBKDF2-HMAC-SHA256، ۳۹۰٬۰۰۰ تکرار، نمک ۱۶ بایتی. قفل حساب پس از ۵ تلاش
ناموفق. توکن نشست فقط به‌صورت چکیدهٔ SHA-256 ذخیره می‌شود.

**حالت مهمان پیش‌فرض است** — برنامه بدون ورود کار می‌کند تا کاربر فعلی
مجبور به ثبت‌نام نشود.

## ۶. حساب‌های صرافی

`app/core/exchange_account_service.py` + زبانهٔ حساب‌ها در تنظیمات.

`Exchange Provider → Exchange Adapter → Exchange API → Application`؛ نام
صرافی از حساب کاربر می‌آید و ارائه‌دهنده از رجیستری ساخته می‌شود — هیچ
صرافی سخت‌کد نیست.

**امنیت کلید (سه لایه):**
1. پایگاه داده فقط `secret_ref` و کلید پوشیده (`ABCD••••WXYZ`) دارد.
2. مقدار واقعی رمزنگاری‌شده در Secret Store است.
3. پیام خطای ارائه‌دهنده پیش از نمایش یا لاگ پاک‌سازی می‌شود.

## ۷. معاملهٔ کاغذی

`app/database/repositories/trade_repository.py`. ستون `mode='paper'` از
حالا موجود است تا فعال‌کردن سفارش واقعی در آینده صفحه‌ها را تغییر ندهد.
محاسبهٔ سود جهت‌آگاه (فروش با افت قیمت سود می‌دهد) و اهرم در **درصد**
ضرب می‌شود نه مبلغ خام.

## ۸. پایگاه داده

مهاجرت `ce3e52be9145` — **فقط افزایشی**. ۱۵ ← ۱۹ جدول: `users`،
`user_sessions`، `exchange_accounts`، `paper_trades`. روی یک پایگاه دادهٔ
واقعی نسخهٔ ۱٫۴ با تنظیمات دست‌کاری‌شدهٔ کاربر آزمایش شد: هر چهار جدول
اضافه شد و **هیچ تنظیمی تغییر نکرد**.

## ۹. آزمون‌ها

**۴۶۴ آزمون** (پیش‌تر ۳۸۶) — ۷۸ آزمون تازه:

| فایل | تعداد |
|---|---|
| `tests/test_themes.py` | ۱۹ |
| `tests/test_auth.py` | ۲۹ |
| `tests/test_trades_and_wallet.py` | ۱۵ |
| `tests/test_new_pages.py` | ۱۹ |

سخت‌گیرانه‌ترین آزمون‌ها:
- `test_api_credentials_are_never_stored_in_plaintext` — بایت‌های خام
  فایل پایگاه داده را می‌گردد.
- `test_stylesheet_builds_without_leftover_placeholders` — جاماندگی
  `{token}` در QSS را می‌گیرد (اشکالی که چشم نمی‌بیند).
- `test_all_pages_survive_every_theme` — قول «پوسته قابلیت را محدود
  نمی‌کند» را قفل می‌کند.

---

# نسخهٔ ۱٫۵٫۱ — اعمال طرح مرجع روی پنج صفحهٔ قدیمی

نسخهٔ ۱٫۵٫۰ ویجت‌های تازه را ساخت ولی فقط دو صفحهٔ نو (کیف پول و
معاملات) از آن‌ها استفاده می‌کردند. بازبینی نشان داد پنج صفحهٔ قدیمی
**هیچ‌کدام** از اجزای طرح مرجع را به کار نمی‌برند. این نسخه آن شکاف را
می‌بندد.

## ۱. داشبورد

نوار قیمت زنده (کلیک‌پذیر)، چهار کارت آمار، نوار تایم‌فریم و ستون
اسپارک‌لاین در جدول بازار. کارت «ارزهای صعودی» عمداً همین نام را دارد،
چون آنچه واقعاً محاسبه می‌شود سهم بازارهای صعودی است نه ارزش کل بازار؛
کلید داخلی `market_cap` برای سازگاری دست‌نخورده ماند.

## ۲. بازارها

- **ستون ستاره**: افزودن/حذف واچ‌لیست با یک کلیک؛ کلیک روی همان ستون
  دیگر مدال جزئیات را باز نمی‌کند.
- **کلید جفت‌ارز** (`USDT` / `BTC` / `ETH`) و **تراشه‌های چندانتخابی**
  (صعودی، نزولی، پرحجم) که با هم ترکیب می‌شوند.
- **گزینهٔ «فقط واچ‌لیست»** و **ستون روند** با اسپارک‌لاین.
- ستون ستاره و روند از مرتب‌سازی سرستون مستثنا شدند (مقدار قابل مقایسه
  ندارند).
- مسیر به‌روزرسانی درجا و نمایهٔ `_row_index` دست‌نخورده ماند.

## ۳. تحلیل

پنل کناری اندیکاتورها با کلید کشویی برای **هر ۲۴ اندیکاتور رجیستری** —
فهرست هاردکد نیست، پس اندیکاتور تازه خودبه‌خود ظاهر می‌شود. انتخاب کاربر
در کلید تنظیمات `analysis.enabled_indicators` ذخیره و پس از راه‌اندازی
دوباره بازیابی می‌شود، و تحلیل واقعاً با همان فهرست اجرا می‌گردد. نوار
تایم‌فریم بالای نمودار با فهرست کشویی دوطرفه همگام است.

## ۴. سیگنال‌ها

`SignalCard` حالا **حلقهٔ اطمینان** دارد (مطابق تصویر مرجع)، و متد تازهٔ
`set_labels()` باعث می‌شود تغییر زبان برچسب ردیف‌ها و جدول سابقه را هم
بازسازی کند — پیش‌تر سابقه به زبان قبلی می‌ماند.

## ۵. گزارش‌ها

چهار کارت آمار، **نمودار حلقه‌ای سهم جهت‌ها** و **نمودار سری زمانی
اطمینان**. برای تغذیهٔ نمودار، کلید `confidence_series` به خروجی
`reports/builder.py` افزوده شد (ترتیب زمانی، از قدیم به جدید). جهت‌ها و
درصدها دیگر خام انگلیسی نیستند و با زبان جاری ترجمه و رقم‌گذاری می‌شوند.

## ۶. اصلاح راست‌به‌چپ

`+۲.۳۴%` در چیدمان فارسی به شکل `۲.۳۴%+` دیده می‌شد. نشانگر چپ‌به‌راست
(`\u200e`) به همهٔ متن‌های درصد تغییر افزوده شد: جدول بازارها، داشبورد،
نوار قیمت و کارت آمار.

## ۷. آزمون‌ها

**۴۸۰ آزمون** (پیش‌تر ۴۶۴) — ۱۶ آزمون تازه در
`tests/test_pages_design_v151.py`: ستون ستاره، فیلترهای ترکیبی، حفظ
علامت درصد در RTL، پنل اندیکاتور، همگامی نوار تایم‌فریم، حلقهٔ اطمینان،
حفظ سابقه پس از تغییر زبان، نمودارهای گزارش و حالت بدون داده، و اعمال
هر چهار پوسته روی هر چهار صفحه.

آزمون‌های قدیمی که شمارهٔ ستون بازارها را هاردکد کرده بودند، حالا
ثابت‌های `COL_*` را از خود ماژول وارد می‌کنند.

---

# نسخهٔ ۱٫۵٫۲ — رفع شش ایراد گزارش‌شدهٔ کاربر

کاربر نسخهٔ ۱٫۵٫۱ را رد کرد و شش ایراد مشخص گرفت. هر شش مورد پیش از
اصلاح، به‌طور مستقل بازتولید و ریشه‌یابی شد.

## ۱. تعویض‌نشدن پوسته

`change_theme()` درست کار می‌کرد ولی تنها فراخوانندهٔ آن `save_settings()`
بود (`main_controller.py:1807`). یعنی تا کاربر «ذخیره» نمی‌زد، هیچ اتفاقی
نمی‌افتاد و طبیعی بود که فکر کند خراب است.

اکنون `theme_preview_requested` و `theme_combo.currentIndexChanged` هر دو
مستقیم به `change_theme` وصل‌اند و پوسته همان لحظه عوض و ذخیره می‌شود.

## ۲. نبودن بخش انتخاب پوسته

انتخابگر وجود داشت اما ته زبانهٔ «عمومی» دفن شده بود. زبانهٔ اختصاصی
**«ظاهر برنامه»** ساخته شد با چهار کارت پوسته (`ui/widgets/theme_card.py`:
`ThemeSwatch` + `ThemeCard`). اندازهٔ قلم و حالت فشرده هم به همین زبانه
منتقل شدند تا همهٔ تنظیمات ظاهری یک‌جا باشد؛ هر دو بی‌درنگ اعمال و ذخیره
می‌شوند.

## ۳. نبودن پیشنهاد در جست‌وجو

دو مشکل: `SearchBox` اصلاً `QCompleter` نداشت، و مهم‌تر اینکه سیگنال
`search_requested` در کل پروژه **هیچ مصرف‌کننده‌ای نداشت** — جست‌وجوی
سراسری واقعاً هیچ کاری نمی‌کرد.

حالا کادر جست‌وجو تکمیل‌کنندهٔ «شامل‌بودن» دارد، فهرست پیشنهاد از نام
صفحه‌ها و نمادهای بازار پر می‌شود، و انتخاب هر گزینه از راه
`suggestion_activated(kind, value)` به صفحهٔ درست می‌رود.

## ۴. وصل‌نشدن هوش مصنوعی

ریشه دو چیز بود:

- در `app/application.py` فقط **سرویس فعال** ثبت می‌شد. پس
  `ai.fallback_enabled=True` معنایی نداشت؛ چیزی برای جایگزینی وجود
  نداشت. متد `_register_fallback_providers()` اضافه شد و هر سرویسی که
  کلید ذخیره‌شده دارد یا کلید نمی‌خواهد، وارد زنجیره می‌شود.
- `ChatAgent` پارامتر `preferred_provider` نمی‌گرفت، پس هر پیام از ابتدای
  زنجیره (سرویس‌های محلیِ خاموش) شروع می‌شد و کاربر منتظر شکست آن‌ها
  می‌ماند. حالا سرویس انتخابی کاربر همیشه اول است.

پیام خطا هم بازنویسی شد: به‌جای فهرست نام کلاس استثناء، خلاصهٔ خوانا
می‌دهد؛ مثلاً «ollama: Could not reach the local Ollama service».

زنجیره با یک سرویس محلی سازگار با OpenAI آزموده شد: با خاموش‌بودن
Ollama، درخواست خودکار به سرویس بعدی رفت و پاسخ فارسی برگشت.

## ۵. خرابی آیکن‌ها

با `fontTools` اثبات شد که **هر ۱۳ گلیف** به‌کاررفته (`◧◫◪◈✦⇄◍▤⚙☎◆★☆`)
در فونت وزیرمتن **وجود ندارد**؛ ترسیمشان به فونت جایگزین سیستم واگذار
می‌شد و روی ویندوز مربع خالی می‌داد.

زیرسیستم `ui/icons/` ساخته شد: ۳۵ آیکن برداری ۲۴×۲۴ با قلم‌خور یکنواخت،
نگاشت نام‌های مستعار، حافظهٔ `lru_cache`، سازگار با صفحه‌های پرچگالی، و
هم‌رنگ‌شونده با پوستهٔ جاری. نکتهٔ فنی: `QSvgRenderer` مقدار
`currentColor` را نمی‌فهمد، پس رنگ پیش از ترسیم جای‌گذاری می‌شود.

## ۶. طراحی

آیکن‌های واقعی در منوی کناری (با تیره‌کردن گزینهٔ فعال)، نوار بالا، نوار
قابلیت‌ها و منوی کاربر؛ همگی با `apply_theme` هم‌رنگ پوسته می‌شوند.
حالت جمع‌شدهٔ منو فقط آیکن نشان می‌دهد و متن را به راهنمای ابزار می‌سپارد.

## آزمون‌ها

**۴۹۴ آزمون** (پیش‌تر ۴۸۰). فایل تازهٔ `tests/test_icons_search_theme.py`
با ۱۲ آزمون: تهی‌نبودن هر ۳۵ آیکن، بازگشت امن نام ناشناخته، نماندن
`currentColor` در خروجی، وجود آیکن برای همهٔ ورودی‌های منو، پیشنهادهای
جست‌وجو و تطبیق میانی، انتشار نوع و مقدار هنگام انتخاب، ثبت واقعی
سرویس‌های جایگزین، ثبت‌نشدن سرویس بدون کلید، احترام به خاموش‌بودن
جایگزینی، و اولویت سرویس انتخابی کاربر. دو آزمون هم در
`tests/test_settings_expanded.py` برای زبانهٔ ظاهر و اعمال بی‌درنگ پوسته.

پویش کامل ۴ پوسته × ۲ زبان × ۱۰ صفحه × ۸ زبانهٔ تنظیمات: بدون خطا.

---

# نسخهٔ ۱٫۵٫۳ — کندی، هنگ، روند بازار، اندیکاتور، چت

## ۱. کندی و هنگ‌کردن (مهم‌ترین ایراد)

کاربر گفت: «چند کار را پشت سر هم که انجام می‌دهیم کرش می‌کنه و هنگ
می‌شه». ریشه پیدا و اثبات شد: `AsyncRunner.submit()` برای هر فراخوانی یک
کار تازه می‌ساخت و **هیچ کار قبلی لغو نمی‌شد**. آزمون نشان داد ۱۵ کلیک
پیاپی روی «تازه‌سازی» ⇒ **۱۵ درخواست شبکهٔ هم‌زمان**.

راه‌حل: هم‌ادغامی بر پایهٔ کلید. هر کار یک کلید دارد (`markets`،
`signal`، …) و کار تازه با همان کلید، نمونهٔ قبلی را لغو می‌کند. پس از
اصلاح: بیشینهٔ هم‌زمانی **۱**. کارهای با کلید متفاوت هنوز موازی‌اند و
`coalesce=False` برای مواردی مثل ارسال پیام چت وجود دارد که هرگز نباید
لغو شوند.

دومین منبع کندی: مهلت اتصال ۱۵ ثانیه‌ای برای سرویس هوش مصنوعی. سرویس
محلی (`127.0.0.1`) یا هست یا نیست؛ حالا مهلتش ۲ ثانیه است و
`PROBE_TIMEOUT` اولاما از ۶ به ۲ ثانیه رسید. سرویس ابری دست‌نخورده ماند.

## ۲. ستون «روند» در بازارها

پیش‌تر این ستون فقط یک نمودار کوچک بود و چون کنترلر هرگز کلید `history`
را پر نمی‌کرد، همیشه **خالی** می‌ماند. ویجت تازهٔ `ui/widgets/trend_cell.py`
سه چیز نشان می‌دهد: نمودار خطی، برچسب فارسی «صعودی/نزولی/خنثی» با پیکان،
و رنگ سبز/قرمز از پوستهٔ جاری. نبودن تاریخچه فقط نمودار را پنهان می‌کند؛
برچسب و رنگ همیشه هستند.

تاریخچهٔ قیمت هم از خود تازه‌سازی‌های دوره‌ای ساخته می‌شود
(`_remember_prices`, سقف ۲۴ نقطه) — گرفتن کندل برای ۳۰۰ نماد کند بود.

نکتهٔ فنی: `configure_table` همهٔ ستون‌ها را `ResizeToContents` می‌کرد و
چون این سلول ویجت است نه متن، پهنایش صفر حساب می‌شد و محتوا روی هم
می‌افتاد. ستون روند حالا پهنای ثابت دارد.

## ۳. «قیمت با تتر مشخص بشه»

سرستون «قیمت (دلار)» بود در حالی که داده از جفت‌ارز USDT می‌آید. حالا
«قیمت (تتر)» است.

## ۴. انتخاب اندیکاتورها

کلیدها وجود داشتند و کار هم می‌کردند، ولی پنل باریک بود و ستون کلیدها
**بیرون از کادر** می‌افتاد؛ کاربر حق داشت فکر کند قابل انتخاب نیستند.
کمینه‌پهنای پنل و اندازهٔ اسپلیتر اصلاح شد و دو دکمهٔ «انتخاب همه» و
«پاک‌کردن انتخاب» اضافه شد تا هر دو حالتِ خواسته‌شده یک‌کلیکی باشد.

## ۵. چت

- **پیام سیستمی بازنویسی شد.** پیش‌تر مدل مجبور بود *فقط* JSON بدهد؛
  مدل‌های کوچک این را رعایت نمی‌کنند و نتیجه «جواب نمی‌دهد» بود. حالا
  متن ساده حالت پیش‌فرض است و JSON فقط برای فراخوانی ابزار و پیشنهاد
  اقدام. با سه قالب پاسخ (متن ساده، JSON داخل حصار کد، JSON خالص) آزموده
  شد؛ هر سه درست کار می‌کنند.
- **دامنهٔ گفتگو باز شد.** دستیار صریحاً یک دستیار عمومی است و باید به
  هر موضوعی پاسخ دهد؛ ابزار بازار فقط وقتی لازم است صدا زده می‌شود.
- **پاسخ بدون کلید `answer`** دیگر خطا نمی‌دهد؛ اگر متن خامی باشد همان
  نمایش داده می‌شود.

## ۶. ظاهر چت مثل ChatGPT

هر پیام حالا یک **ردیف تمام‌عرض** است با نشان گوینده و نام او؛ پیام
دستیار پس‌زمینهٔ ملایم دارد و پیام کاربر بدون پس‌زمینه است. ایراد
«بریده‌شدن متن وسط جمله» هم برطرف شد: علتش این بود که `QLabel` با
`setWordWrap` ارتفاع واقعی‌اش را به چیدمان اعلام نمی‌کرد
(`MinimumExpanding` حل کرد). آواتار از آیکن برداری استفاده می‌کند نه
ایموجی — ایموجی هم مثل گلیف‌های نسخهٔ قبل در وزیرمتن نیست و دایرهٔ خالی
می‌شد.

## ۷. مارجین و پدینگ

مقادیر خارج از مقیاس ۴تایی به نزدیک‌ترین گام استاندارد رفتند (۱۱→۱۲،
۱۴→۱۶). فاصله‌های ریز داخل ویجت‌های فشرده (۲ و ۶) عمداً نگه داشته شدند
تا سلسله‌مراتب بصری حفظ شود.

## آزمون‌ها

**۵۰۹ آزمون** (پیش‌تر ۴۹۴). فایل تازهٔ `tests/test_v153_fixes.py` با ۱۵
آزمون: انباشته‌نشدن کارهای هم‌کلید، موازی‌ماندن کارهای متفاوت، امکان
انصراف از هم‌ادغامی، برچسب و رنگ روند، پرنماندن ستون روند بدون تاریخچه،
سرستون تتر، انتخاب همه/هیچ اندیکاتور، عمومی‌بودن دامنهٔ چت، ترجیح متن
ساده، سبک ChatGPT، نبریده‌شدن متن بلند، و کوتاه‌بودن مهلت سرویس محلی.

## نسخهٔ ۱٫۵٫۵ — کرش مدال، نشانی سرویس هوش مصنوعی، و ظاهر

**کرش مدال بازار:** مدال جزئیات ارز کار پس‌زمینه راه می‌انداخت؛ بستن
پنجره پیش از رسیدن پاسخ، برنامه را با `Internal C++ object already
deleted` می‌بست. بازتولید شد، نگهبان زنده‌بودن پنجره و لغو کار
پس‌زمینه اضافه شد. دکمه‌های اقدام هم حالا اول مدال را می‌بندند.

**هوش مصنوعی:** ریشهٔ اصلیِ «کار نمی‌کند» پیدا شد — پیش‌فرض
`ai.base_url` نشانی اولامای محلی بود و روی هر سرویس ابری هم سوار
می‌شد، پس درخواست OpenRouter به `127.0.0.1:11434` می‌رفت. با کلید
واقعی کاربر آزموده شد: چت ۱٫۹ ثانیه، سیگنال هیبرید ۲۰ ثانیه با تحلیل
فارسی. اثبات با برگرداندن کد: خطای دقیقاً گزارش‌شدهٔ کاربر برگشت.

**ظاهر:** پوستهٔ تازهٔ «شفق نیمه‌شب»، لبهٔ نرم‌تر دکمه‌ها، حباب‌های گفتگو
به‌شکل کارت گرد، و نشان کاربر با آواتار ترسیمی و حلقهٔ وضعیت ورود.

- ۵۶۶ آزمون سبز | `--check` سالم | پویش ۵ پوسته × ۲ زبان: ۰ خطا.


## نسخهٔ ۱٫۵٫۴ — رفع «سیگنال با هوش مصنوعی تولید نمی‌شود»

گزارش کاربر: «قسمت سیگنال هم خیلی کنده... با هوش مصنوعی هم اصلا نمیده».
دو نقص واقعی و مستقل پیدا شد که هر دو **بی‌صدا** شکست می‌خوردند:

1. **نگاشت خروجی مدل.** پرامپت کلید `reason` می‌خواست، مدل‌های واقعی
   `reasoning` می‌نویسند و اعتبارسنج کل تصمیم معتبر را رد می‌کرد. کاربر
   «انتظار» با اطمینان صفر می‌دید. حالا نام‌های رایج پذیرفته می‌شوند.
2. **تحلیل نوشتاری فارسی.** دیکشنری خام به‌جای `AIMessage` فرستاده
   می‌شد؛ `AttributeError` بالادست بلعیده می‌شد و متن **همیشه** به قالب
   ماشینی برمی‌گشت، بی‌آنکه خطایی دیده شود.

همچنین سرویس انتخابی کاربر حالا اول زنجیره است (پیش‌تر هر بار اول
سراغ سرویس‌های محلیِ خاموش می‌رفت)، انتخاب تایم‌فریم در صفحهٔ سیگنال‌ها
دیگر دور ریخته نمی‌شود، و صفحهٔ چت انتخاب حالت گفت‌وگو دارد.

**ترجمه:** چهار کلید صدازده‌شده در کد هیچ ترجمه‌ای نداشتند؛ آشکارترینش
`reports.exported` بود که پس از خروجی گرفتن، کلید خام را به کاربر نشان
می‌داد. افزوده شدند و آزمونی گذاشته شد که کل کلیدهای کد را با هر دو زبان
می‌سنجد.

**امنیت و مسیر پول** رفتاری آزموده شدند: راز خام در هیچ فایلی روی دیسک
نیست، خطاهای صرافی پیش از نمایش ماسک می‌شوند، اندازهٔ موقعیت دقیقاً برابر
ریسک مجاز است و معاملهٔ واقعی پیش‌فرض خاموش است.

**بسته‌بندی ویندوزی** با ساخت واقعی آزموده شد: قلم فارسی در بسته نبود
(خروجی PDF نصبی خراب می‌شد) و حجم از ۶۱۰ به ۳۲۸ مگابایت رسید.
راهنما: `docs/BUILD_WINDOWS_FA.md`.

- ۵۵۱ آزمون سبز | `--check` سالم از کد و از فایل اجرایی | پویش ۰ خطا.


## نسخهٔ ۱.۵.۶ — رفع نقص‌های گزارش‌شدهٔ کاربر (۲۰۲۶-۰۹-۱۲)

### ۱. کیف پول LBank خالی بود
دو نقص مستقل روی هم افتاده بودند:
- `LBankProvider.get_account_balance()` فرض می‌کرد پاسخ
  `/v2/supplement/user_info.do` دیکشنری است، ولی صرافی **فهرست**
  برمی‌گرداند؛ نتیجه همیشه `{}` بود. چون `test_credentials()` موفق
  گزارش می‌داد، صفحهٔ تنظیمات سالم به نظر می‌رسید.
- هیچ جای رابط کاربری `sync_balances` را صدا نمی‌زد؛ دکمهٔ «همگام‌سازی»
  فقط `refresh_wallet` را اجرا می‌کرد و همان دادهٔ ذخیره‌شده را دوباره
  می‌کشید.
منطق تجزیه به `LBankProvider._parse_balances()` منتقل شد تا بدون شبکه
آزمون‌پذیر باشد. متد `MainController.sync_wallet()` اضافه شد.

### ۲. ورود ناموفق
`UserRepository.authenticate()` فقط `username` را می‌سنجید. اکنون ایمیل
(در صورت وجود «@») و نام نمایشی (فقط وقتی دقیقاً یک تطبیق دارد) هم
پذیرفته می‌شود. برچسب فرم در حالت ورود به «نام کاربری یا ایمیل» تغییر
می‌کند.

### ۳. هوش مصنوعی در مسیر سیگنال کار نمی‌کرد (ولی در چت درست بود)
مدل‌های استدلالی تمام بودجهٔ توکن را صرف زنجیرهٔ فکری می‌کردند و پاسخ
وسط جمله بریده می‌شد؛ چت چون متن آزاد می‌پذیرد سالم بود ولی مسیر سیگنال
که JSON می‌خواهد شکست می‌خورد. سه لایه اضافه شد:
`_repair_json()` برای بستن ساختار ناقص، `_force_final()` برای یک درخواست
جمع‌بندی بدون استدلال، و دورریختن مقادیر نیمه‌بریده.
**نکتهٔ ایمنی:** عدد بریده هرگز نباید ترمیم شود — «۶۰» بریده به «۶»
تبدیل می‌شد و مستقیم روی حجم معامله اثر می‌گذاشت؛ چنین مقادیری حذف
می‌شوند.

### ۴. ترتیب فهرست مدل‌ها
ماژول تازهٔ `ai/providers/ranking.py`: امتیازدهی الگومحور، پایین‌بردن
مدل‌های نامرتبط (تعبیه‌سازی/صوتی/تصویری) و نسخه‌های جانبی
(`:batch`، `codex`، `mini`)، و چیدمان لایه‌ای برای تنوع سازنده تا صدر
فهرست در انحصار یک شرکت نباشد. روی ۴۴۳ مدل واقعی OpenRouter آزموده شد.

### وضعیت
۵۸۹ آزمون موفق · `--check` سالم · جاروب رابط کاربری بدون خطا.

### پیگیری‌های پس از ۱.۵.۶ (همان نسخه، اصلاحات تکمیلی)

**قیمت‌گذاری غیرمستقیم دارایی‌ها.** پیش‌تر فقط جفت «X/USDT» بررسی می‌شد،
پس دارایی‌هایی که تنها با بیت‌کوین یا اتریوم جفت دارند بی‌ارزش نمایش
داده می‌شدند. متد `_usdt_price()` حالا مسیر واسط (BTC → ETH → USDC) را
هم امتحان می‌کند. نمونهٔ واقعی LBank: `BTCV` جفت تتری ندارد ولی از راه
`BTCV/BTC × BTC/USDT` برابر ۰.۴۱ دلار ارزش‌گذاری می‌شود.
`CONWAY` در LBank **هیچ** جفتی ندارد (بررسی شد: صفر مورد از ۱۳۹۱ نماد)،
پس «—» برایش درست و صادقانه است.

**پیام سقف روزانه.** پاسخ OpenRouter برای `free-models-per-day` واژهٔ
«credits» دارد و در دستهٔ «اعتبار تمام شد» می‌افتاد؛ کاربر گمان می‌کرد
باید پول بدهد، در حالی‌که این سهمیه خودبه‌خود بازمی‌گردد. حالا دستهٔ
جدا `daily_quota` با پیام درست دارد.

**جلوگیری از فراخوانی تکراری ابزار.** عامل خودمختار گاهی یک ابزار را با
همان پارامترها چند بار صدا می‌زد؛ هر بار یک گام و یک درخواست شبکه هدر
می‌رفت و سهمیهٔ روزانه زود تمام می‌شد. تکرارها حالا شناسایی و با تذکر به
مدل برگردانده می‌شوند (آزمون: ۸ فراخوانی تکراری ← ۱ اجرای واقعی).

**تأیید حالت ترکیبی.** با تمام‌شدن سهمیهٔ هوش مصنوعی، `generate_signal()`
همچنان سیگنال موتور ریاضی را با روایت فارسی قالبی برمی‌گرداند و
`analysis_source` را صادقانه `template` علامت می‌زند.

آزمون‌ها: ۵۹۶ مورد، همگی موفق.

## نسخهٔ ۱.۵.۷ — سه گزارش تازه (۲۰۲۶-۰۹-۱۲)

### ۱. سوییچ بین صرافی‌ها برای کیف پول
زیرساخت `set_default` در مخزن و سرویس وجود داشت ولی **هیچ دکمه‌ای در
رابط کاربری به آن وصل نبود**؛ کاربر با دو حساب متصل نمی‌توانست تعیین کند
کیف پول کدام را نشان دهد. دکمهٔ «انتخاب برای کیف پول» و ستون «فعال» (●)
اضافه شد. پس از سوییچ، حافظهٔ قیمت پاک و موجودی حساب تازه فوراً گرفته
می‌شود.

### ۲. موجودی فیوچرز
API قراردادهای LBank روی **دامنهٔ جداگانه** (`lbkperp.lbank.com`) با
پیشوند `/cfd` و قرارداد متفاوت است: بدنه باید JSON باشد و سه پارامتر
امضا باید در هدر هم تکرار شوند. متد `post_contract_signed()` و
`get_futures_balance()` اضافه شد و موجودی دو کیف پول در `sync_balances`
با هم جمع می‌شود؛ تفکیکش در `extra_config.balances_by_wallet` ذخیره و در
ستون تازهٔ «کیف پول» نمایش داده می‌شود (اسپات / فیوچرز / هر دو).

**محدودیت آزمون:** IP این محیط توسعه از سوی CDN صرافی برای نقاط پایانی
خصوصیِ قراردادها مسدود است (۴۰۳ با بدنهٔ HTML، در حالی که نقاط عمومی
همان دامنه JSON سالم می‌دهند). پس تجزیه‌گر و مسیر ادغام با دادهٔ ساختگی
آزموده شد، نه با حساب واقعی. اگر روی دستگاه شما هم فیوچرز خالی ماند،
همین مسدودی یا نبودِ مجوز «قرارداد» روی کلید علتش است — و در آن حالت
موجودی اسپات دست‌نخورده می‌ماند.

### ۳. هوش مصنوعی در مسیر سیگنال (از روی تصویر کاربر)
تصویر سه خطا را کنار هم نشان می‌داد: `Network error` برای OpenRouter،
`HTTP 500` از Ollama و `HTTP 503` از LM Studio. اندازه‌گیری نشان داد
پرامپت عامل خودمختار در گام آخر به ~۱۹۷۷ توکن می‌رسد و با
`max_tokens=1600` مجموع از ۳۵۰۰ توکن می‌گذرد، در حالی‌که **پنجرهٔ پیش‌فرض
Ollama فقط ۲۰۴۸ توکن** است ⇒ خطای مبهم ۵۰۰. چت چون پرامپت کوتاه‌تری
(~۸۴۵ توکن) دارد سالم می‌ماند — دقیقاً همان تناقضی که کاربر گزارش کرد.
حالا `num_ctx` بر پایهٔ `max_tokens` صریح ارسال می‌شود.

باگ دوم در همان مسیر: تصمیم WAIT بدون فیلد `reason` توسط اعتبارسنج رد
می‌شد و کاربر «اطمینان ۰٪» بدون توضیح می‌دید. حالا اندیشهٔ خود مدل
به‌عنوان دلیل به کار می‌رود و پرامپت جمع‌بندی هم صریحاً دلیل می‌خواهد.

آزمون‌ها: ۶۰۵ مورد، همگی موفق.

## نسخهٔ ۱.۵.۸ — تکمیل کارهای نیمه‌تمام (۲۰۲۶-۰۹-۱۲)

### تفکیک موجودی در خلاصه
کلیدهای `spot_total`/`futures_total` در نسخهٔ قبل تعریف شده بودند ولی
هیچ‌جا استفاده نمی‌شدند. حالا زیر کارت «موجودی در دسترس» خطی مثل
«اسپات: ۶.۰۲ $ • فیوچرز: ۲۵۰.۰۰ $» می‌آید. اگر کاربر اصلاً موجودی
فیوچرز نداشته باشد این خط نمایش داده نمی‌شود تا کارت شلوغ نشود.

### شفاف‌سازی دسترسی قرارداد
`test_connection` فقط اسپات را می‌سنجید؛ کلیدی که مجوز قرارداد ندارد
«اتصال برقرار» می‌گرفت و بعد کیف پول فیوچرز بی‌دلیل خالی می‌ماند —
همان تلهٔ `test_credentials` که یک بار کیف پول را شکسته بود. حالا پیام
`(futures access unavailable)` اضافه می‌شود.

**نکتهٔ طراحی:** دو متد جدا لازم شد. `get_futures_balance()` خطا را مهار
می‌کند (برای همگام‌سازی، تا نبود مجوز کل کیف پول را از کار نیندازد) و
`fetch_futures_balance()` خطا را بالا می‌دهد (برای آزمایش اتصال، تا علت
دیده شود). یک آزمون با بازرسی کد تضمین می‌کند مسیر همگام‌سازی به نسخهٔ
مهارشده وصل بماند.

### آزمون شکننده
`test_repeated_submits_do_not_pile_up` مهلت ثابت ۳ ثانیه داشت و زیر بار
اجرای کل مجموعه گاهی پیش از پایان کارها داوری می‌کرد. بررسی شد که
رگرسیون نیست (نسخهٔ ۱.۵.۷ هم همان رفتار را داشت و اجرای دوم سبز شد)، ولی
به‌جای نادیده‌گرفتن، آزمون حالا منتظر پایان واقعی کارها می‌ماند و
اطمینان می‌دهد دست‌کم یک کار اجرا شده باشد.

آزمون‌ها: ۶۰۸ مورد، همگی موفق.

## نسخهٔ ۱.۵.۹ — زبانهٔ امنیت و باگ انتخاب سطر در فارسی (۲۰۲۶-۰۹-۱۲)

### زبانهٔ «امنیت»
`AuthService` از همان ابتدا `change_password`، `update_profile`،
`sessions`، `revoke_session` و `revoke_other_sessions` را داشت، ولی هیچ
رابط کاربری‌ای به آن‌ها وصل نبود؛ قابلیتی که کاربر راهی برای رسیدن به آن
ندارد، عملاً وجود ندارد. زبانهٔ تازه سه بخش دارد:

- **پروفایل**: نام نمایشی و ایمیل.
- **تغییر رمز عبور**: رمز فعلی، جدید و تکرار. تطابق رمز جدید با تکرارش در
  لایهٔ رابط بررسی می‌شود تا کاربر پیش از رفت‌وبرگشت با پایگاه داده
  بازخورد بگیرد. پس از موفقیت، فیلدها فوراً پاک می‌شوند.
- **نشست‌های فعال**: دستگاه، زمان ایجاد، آخرین فعالیت و وضعیت
  (فعال / باطل‌شده / منقضی) با دکمه‌های پایان نشست و خروج از سایر دستگاه‌ها.

در حالت مهمان همهٔ کنترل‌ها خاموش و متن راهنما عوض می‌شود.

### باگ جدی: انتخاب سطر در چیدمان راست‌به‌چپ
هنگام نوشتن آزمون زبانهٔ امنیت، آزمونی که تنها اجرا می‌شد قبول می‌شد ولی
در اجرای کامل مجموعه رد می‌شد. ریشه‌یابی نشان داد مقصر ترتیب آزمون‌ها
نیست: هر آزمونی که `MainWindow` می‌سازد، جهت کل `QApplication` را
راست‌به‌چپ می‌کند و **در آن حالت `QTableWidget.selectRow()` سطر جاری را
تنظیم نمی‌کند** — `currentRow()` منفی می‌ماند.

این فقط مسئلهٔ آزمون نبود. جدول حساب‌های صرافی با `selectRow` روی حساب
پیش‌فرض می‌ایستاد و `selected_account_id()` به `currentRow()` تکیه
می‌کرد. نتیجه: **در فارسی — یعنی حالت پیش‌فرض برنامه — دکمه‌های «آزمایش
اتصال»، «فعال‌سازی» و «حذف» بدون کلیک دستی روی سطر هیچ کاری نمی‌کردند و
هیچ پیام خطایی هم نمی‌دادند.** با یک هارنس روی پنجرهٔ واقعاً نمایش‌داده‌شده
تأیید شد که کاربر هم متأثر می‌شد، نه فقط آزمون بی‌نمایش.

اصلاح در سه لایه:
- `set_accounts` از `setCurrentCell` استفاده می‌کند که در RTL درست کار می‌کند.
- `selected_account_id`، `selected_symbol` و `_selected_session_row` علاوه
  بر `currentRow()` مدلِ انتخاب را هم می‌خوانند.
- دکمهٔ پایان نشست وقتی سطری انتخاب نشده، سیگنال `session_revoke_blocked`
  می‌دهد و کاربر هشدار می‌بیند — سکوت بی‌دلیل همان چیزی بود که این باگ را
  این‌قدر دیریاب کرده بود.

### لغزش برچسب زبانه‌ها
فهرست بازترجمهٔ زبانه‌های تنظیمات هفت کلید داشت در حالی که هشت زبانه ساخته
می‌شد؛ با تغییر زبان، نام‌ها یک واحد می‌لغزیدند. حالا `TAB_KEYS` تنها منبع
حقیقت است: هم زبانه‌ها از رویش ساخته می‌شوند، هم بازترجمه از رویش می‌خواند،
و یک `assert` هنگام ساخت، ناهماهنگی را همان لحظه لو می‌دهد.

آزمون‌ها: ۶۶۳ مورد، همگی موفق.

## نسخهٔ ۱٫۶٫۰ — افزودن صرافی‌های بیت‌پین و توبیت (۱۴۰۴/۰۶/۲۱)

- **Toobit** (`market/providers/toobit/`) — پیاده‌سازی کامل با امضای
  HMAC-SHA256 و هدر `X-BB-APIKEY`. آزمایش زنده: ۵۱۸ نماد، تیکر، کندل،
  دفتر سفارش و تجمیع تایم‌فریم همگی درست کار کردند.
- **Bitpin / بیت‌پین** (`market/providers/bitpin/`) — پیاده‌سازی کامل با
  ورود توکنی (`/api/v1/usr/authenticate/`) و تازه‌سازی خودکار توکن.
  آزمایش زنده: ۴۲۷ بازار (۲۱۹ تومانی)، نرخ زندهٔ تتر/تومان.
- کشف مهم: دامنهٔ `api.bitpin.org` از بیرون ایران مسدود است ولی
  `api.bitpin.market` در دسترس است؛ و مسیر ورود مستندشده ۴۰۴ می‌دهد.
- بیت‌پین endpoint کندل ندارد؛ کندل‌ها از معاملات اخیر ساخته می‌شوند و
  تایم‌فریم‌های بلند با خطای روشن رد می‌شوند.
- رفع باگ: منبع نرخ تومان بیت‌پین در `market/fiat_rates.py` به نشانی مرده
  اشاره می‌کرد و هرگز کار نمی‌کرد؛ اصلاح و آزمایش شد (۲۳۷٬۷۷۸ تومان).
- هر دو در `registry.py` ثبت و در `exchange_catalog.py` با
  `implemented=True` معرفی شدند؛ در تنظیمات ← صرافی قابل انتخاب‌اند.
- `tests/test_toobit_bitpin_providers.py` با ۲۰ آزمون افزوده شد؛ کل مجموعه
  ۷۰۱ آزمون سبز.
- مستندات: `docs/EXCHANGES_BITPIN_TOOBIT_FA.md`.

### افزودهٔ تکمیلی ۱٫۶٫۰ — ورود واقعی به حساب صرافی

- **رفع باگ اصلی:** انتخاب صرافی فقط ذخیره می‌شد و موتور بازار عوض نمی‌شد.
  `Application.switch_exchange()` افزوده شد؛ موتور بازار/سیگنال بازساخته و
  صرافی قبلی بسته می‌شود. آزمایش زنده: توبیت ۲۹۷ بازار، بیت‌پین ۱۴۹ بازار.
- دکمه‌های «آزمایش/فعال‌سازی/حذف» حساب دیگر بی‌صدا نیستند.
- پیام‌های خطای صرافی به کلید ترجمه تبدیل شدند (`exchange.error.*`) و
  فضای‌نام تازهٔ `localization/{fa,en}/exchange.json` ساخته شد.
- راهنمای هر صرافی هنگام انتخاب نمایش داده می‌شود (الزام IP بیت‌پین).
- موجودی تومانی (IRT/IRR) اکنون از نرخ تتر ارزش‌گذاری می‌شود.
- مجموع آزمون‌ها: ۷۴۹ سبز.

### زیباسازی ۱٫۶٫۰ — پوستهٔ تازه، گردی، نوار بالا و چت

- **پوستهٔ تازه «ابریشم سلطنتی» (`royal_silk`)** افزوده شد: زغالی-آبی عمیق
  با لهجهٔ طلایی و رنگ اصلی فیروزه‌ای. طلا عمداً لهجه است نه رنگ دکمه
  (کنتراست متن)، و سبز/قرمز بازار از خانوادهٔ طلایی دور نگه داشته شد.
  مجموع پوسته‌ها: ۶.
- **گردی همهٔ گوشه‌ها:** تنها عنصر باقی‌مانده با لبهٔ تیز، زبانه‌ها بودند؛
  اکنون `border-top-*-radius` می‌گیرند. آزمون خودکار برای هر ۶ پوسته.
- **نوار بالا:** حالت «واردشده» و «مهمان» حالا بصری جدا هستند (قاب فیروزه‌ای
  توپر در برابر قاب خط‌چین خنثی).
- **رفع باگ واقعی:** شیوه‌نامهٔ درون‌خطیِ پدینگ در `UserMenuButton` بدون
  انتخابگر نوشته شده بود و در Qt کل قاعدهٔ رنگ/قاب پوسته را خنثی می‌کرد؛
  محدود به همان حالت شد.
- **رفع نشت متن داخلی در چت:** بلوک‌های `[App context: …]` و
  `[Mode guidance: …]` که عمداً به مدل داده می‌شوند، توسط مدل‌های کوچک
  تکرار و به کاربر نمایش داده می‌شدند؛ اکنون از پاسخ پاک می‌شوند.
- `tests/test_v160_ui_polish.py` با ۴۵ آزمون. مجموع: ۸۰۵ آزمون سبز.

## نسخهٔ ۱٫۶٫۱ — دروازهٔ OmniRoute، قلم «ب کودک»، پوستهٔ «نیلی ارکیده»

### ۱) دروازهٔ هوش مصنوعی OmniRoute

- `ai/providers/omniroute_provider.py` — کلاس `OmniRouteProvider` که از
  `OpenAICompatibleProvider` ارث می‌برد. چهار تفاوت رفتاری بازنویسی شد:
  نشانی پیش‌فرض `http://localhost:20128/v1`، سرآیندهای شناسایی برنامه
  (`X-Title`، `X-OmniRoute-Session-Id`)، پیام معنادار برای ۴۰۱ و برای
  «دروازه اجرا نشده»، و اولویت همیشگی مدل `auto`.
- ثبت در کاتالوگ (`ai/providers/catalog.py`) و در کارخانهٔ
  `PROVIDER_TYPES`. دروازه هم به‌عنوان سرویس عادی و هم در زنجیرهٔ
  جایگزینی کار می‌کند، پس همهٔ کارکردهای هوش مصنوعی (گفتگو، تحلیل، روایت
  سیگنال) از آن پشتیبانی می‌شوند.
- **فیلد تازهٔ `accepts_optional_key`** در `ProviderPreset`. پیش از این
  «کلید لازم ندارد» و «کلید نمی‌پذیرد» یکی گرفته شده بودند و کادر کلید
  برای دروازه غیرفعال می‌شد؛ کاربری که روی دروازه احراز هویت را روشن کرده
  بود راهی برای وارد کردن کلیدش نداشت. حالا این دو جدا هستند و
  `key_field_enabled` تصمیم می‌گیرد. `lmstudio` و `custom` هم از آن
  بهره‌مند شدند.
- `Application` دیگر کلید ذخیره‌شده را وقتی سرویس آن را «لازم» ندارد دور
  نمی‌اندازد — همان باگ بالا در مسیر ساخت ارائه‌دهنده هم بود.
- **دو ابزار تازهٔ عامل** در `ai/tools/omniroute_tools.py`:
  `omniroute_status` و `omniroute_models`. حالا می‌توان در خود گفتگو
  پرسید «به چه چیزی وصلم و چه مدل‌هایی دارم؟».
- **`ai/tools/composite.py`** — `CompositeToolset` که ابزار بازار و
  دروازه را کنار هم می‌گذارد. بدون آن، هر خانوادهٔ ابزار تازه یعنی تغییر
  هر سه عامل (چت، تحلیلگر، خودگردان).
- **باگ واقعی که آزمون گرفت:** کلاس پایه خطای شبکهٔ فهرست مدل را می‌بلعد و
  فهرست خالی برمی‌گرداند. چون `auto` را بی‌قیدوشرط جلو می‌چسباندیم،
  دروازهٔ خاموش هم «یک مدل دارد» به‌نظر می‌رسید. با پرچم
  `last_listing_ok` تفکیک شد.
- آزمون‌ها با شبیه‌ساز کامل قرارداد دروازه (`/home/user/mock_omniroute.py`)
  به‌صورت زنده وارسی شدند: حالت باز، حالت `REQUIRE_API_KEY`، دروازهٔ
  خاموش، و یک گفتگوی واقعی سرتاسری از پنجرهٔ برنامه.

### ۲) قلم فارسی «ب کودک»

- **نقص ریشه‌ای که پیدا شد:** برنامه هیچ‌جا `QFontDatabase.addApplicationFont`
  را صدا نمی‌زد. حتی وزیرمتنِ موجود فقط برای PDF به کار می‌رفت و رابط
  کاربری به قلم سیستم (تاهوما) می‌افتاد.
- `ui/themes/fonts.py` — سامانهٔ قلم: ثبت فایل‌ها، چهار گزینهٔ قلم،
  زنجیره‌های آمادهٔ QSS، و `apply_default_font` برای اجزای نقاشی‌شده.
- قلم‌های `Koodak.ttf` و `BKoodakBd.ttf` به `assets/fonts/` افزوده شدند
  (هر دو TrueType واقعی، تأییدشده با fontTools).
- **قاعدهٔ نمادهای بازار:** قلم کودک حرف لاتین ندارد، پس هر زنجیره به
  وزیرمتن ختم می‌شود و Qt خودش `BTC/USDT` را با قلم لاتین می‌کشد. آزمون
  این قاعده را قفل کرده است.
- انتخابگر قلم در «تنظیمات ← ظاهر برنامه»، با پیش‌نمایش هر گزینه با قلم
  خودش. بی‌درنگ اعمال می‌شود و **مستقل از پوسته** است: تعویض پوسته قلم را
  برنمی‌گرداند.
- `ui.font_family` به ترجیحات کاربر (`PREFERENCE_KEYS`) و پیش‌فرض‌ها
  اضافه شد، پس با راه‌اندازی بعدی حفظ می‌شود.

### ۳) پوستهٔ «نیلی ارکیده» (`orchid_indigo`)

- هفتمین پوسته: زمینهٔ نیلی عمیق، رنگ اصلی ارغوانی، لهجهٔ مرجانی.
- عمداً از خانوادهٔ فیروزه‌ایِ هر سه پوستهٔ تیرهٔ قبلی فاصله گرفت تا واقعاً
  متفاوت دیده شود.
- سبز سود و قرمز زیان از رنگ لهجه دور نگه داشته شدند؛ آزمون این را
  تضمین می‌کند.
- در `Theme` enum ثبت شد (بدون آن ذخیرهٔ انتخاب کاربر می‌شکند) و نام و
  توضیحش در هر دو زبان اضافه شد.

### مستندات و آزمون

- `docs/AI_OMNIROUTE_FA.md` و `docs/FONTS_AND_THEMES_FA.md` نوشته شدند.
- `tests/test_v161_omniroute_font_theme.py` با ۷۲ آزمون، شامل آزمون‌های
  یکپارچهٔ رابط کاربری (انتخاب قلم/پوسته/سرویس از صفحهٔ تنظیمات).

### پایان نسخهٔ ۱٫۶٫۱ — وارسی نهایی

- **کل مجموعهٔ آزمون: ۸۴۹ آزمون، همگی موفق، بدون هیچ شکست.**
- **دو نشتی منابع پیدا و رفع شد.** فیکسچرهای
  `test_exchange_login_flow.py` و `test_exchange_switching.py` موتور بازار
  را بالا می‌آوردند ولی پیش از بستن حلقهٔ رویداد آن را نمی‌بستند؛ تسک
  «lbank-ws» معلق می‌ماند و هشدار «Task was destroyed but it is pending!»
  چاپ می‌شد. کد محصول سالم بود — `MainController.shutdown()` موتور را در
  همان حلقه‌ای که باز شده می‌بندد. فیکسچر `TestTomanValuation` هم بی‌جهت
  موتور را روشن می‌کرد در حالی که فقط محاسبهٔ محض را می‌سنجد؛ سبک شد و
  زمانش از ۱۲ ثانیه به زیر یک ثانیه رسید.
- **چرا خلاصهٔ آزمون‌ها دیده نمی‌شد:** `pyproject.toml` از پیش
  `addopts = "-q"` دارد؛ دادن `-q` دوباره در خط فرمان یعنی `-qq` که سطر
  «N passed» را حذف می‌کند. در `docs/TROUBLESHOOTING_FA.md` ثبت شد.
- **قفل‌شدن اجرا روی ماشین کم‌حافظه:** اجرای هر ۸۴۹ آزمون در یک فرایند،
  ویجت‌های Qt را انباشته می‌کند تا فرایند در حالت `D` گیر کند. راه‌حل:
  اجرای دسته‌ای (`run_suite_batched.sh`) که همان پوشش را با حافظهٔ ثابت
  می‌دهد. شش دسته، همگی `rc=0`.
- `python3 main.py --check` سالم؛ خاموشی تمیز و داده‌های زنده.

## نسخهٔ ۱٫۶٫۲ — داده زندهٔ توبیت و پاسخ جریانی چت

### ۱) وب‌سوکت توبیت و آزادسازی موتور از وابستگی به LBank

- **اشکال واقعی که پیدا شد:** `MarketDataEngine.start()` به‌صورت ثابت
  `LBankWebSocketClient` می‌ساخت. توبیت `supports_websocket=True` اعلام
  می‌کرد ولی هیچ کلاینتی نداشت؛ یعنی کاربری که توبیت را انتخاب می‌کرد
  **داده زندهٔ LBank** می‌گرفت.
- واسط مشترک `MarketWebSocketClient` (یک `Protocol`) به
  `market/providers/base.py` اضافه شد و `ExchangeProvider` متد
  `create_websocket_client()` گرفت. موتور دیگر نام هیچ صرافی‌ای را
  نمی‌داند.
- `market/providers/toobit/websocket_client.py` نوشته شد؛ پروتکل با
  اتصال واقعی به `wss://stream.toobit.com/quote/ws/v1` راستی‌آزمایی شد.
- سه تلهٔ پروتکل که با آزمون زنده کشف شد:
  ۱. پاسخ کندل `topic` را `kline` می‌دهد نه `kline_1m`، و نوع بازه گاهی
     **فقط** داخل `params` است.
  ۲. درصد تغییر (`m`) کسری است و باید در ۱۰۰ ضرب شود.
  ۳. زمان میلی‌ثانیه است و مدل داخلی ثانیه می‌خواهد.
- سرور توبیت **ping نمی‌فرستد** (آزمون ۷۵ ثانیه‌ای)، پس کلاینت خودش
  اتصال را زنده نگه می‌دارد.
- صرافی‌ای که وب‌سوکت ادعا کند ولی کلاینت ندهد، حالا هشدار می‌گیرد و به
  REST برمی‌گردد.

### ۲) پاسخ جریانی چت

- SSE در `OpenAICompatibleProvider.stream()`؛ `AIProviderManager.stream()`
  همان زنجیرهٔ جایگزینی را دارد.
- `ai/agent/stream_extractor.py`: مقدار `answer` را از JSON **ناقص**
  بیرون می‌کشد تا کاربر `{"answer": "سلا` نبیند. گریز یونیکدِ نصف‌شده
  بین دو تکه هم درست بازسازی می‌شود.
- انتقال امن بین نخ‌ها با سیگنال `MainController.chat_stream_delta`.
- بازگشت خودکار در هر چهار حالت شکست (سرویس بدون جریان، خطای شبکه،
  جریان خالی، مرگ وسط راه).
- کلید `ai.chat_streaming` در «تنظیمات ← کارایی»، پیش‌فرض روشن.

### درسی که فقط آزمون زندهٔ رابط کاربری داد

نسخهٔ اول جریان را در دور اول خاموش می‌کرد تا درخواست ابزار پخش نشود.
**همهٔ آزمون‌های واحد سبز بودند** ولی در برنامهٔ واقعی ابزارها همیشه
ثبت‌اند و پرسش ساده در همان دور اول پاسخ می‌گیرد — پس عملاً هیچ پاسخی
جریانی نمی‌شد. اجرای واقعی پنجره با شمارش «اسنپ‌شات‌های میانی» این را
لو داد (۰ به‌جای ۱۲). حالا همهٔ دورها جریانی‌اند و پنهان‌ماندن درخواست
ابزار را استخراج‌گر تضمین می‌کند؛ آزمون
`test_agent_streams_even_when_tools_are_registered` همین را می‌پاید.

### آزمون و مستندات

- `tests/test_v162_toobit_websocket.py` — ۳۰ آزمون.
- `tests/test_v162_chat_streaming.py` — ۳۵ آزمون.
- `docs/LIVE_DATA_AND_STREAMING_FA.md` نوشته شد.

### بسته‌شدن مطمئن مدال‌ها (تکمیل کار باقی‌مانده)

- سناریوی کرش گزارش‌شدهٔ کاربر بازتولید شد: با `shiboken6.delete` شیء
  ++C نابود شد و تماس بعدی دقیقاً پیام
  «Internal C++ object already deleted» را داد.
- **یک شکاف واقعی پیدا شد:** `_export_analysis_pdf` فقط
  `if dialog is not None` را می‌سنجید. این کافی نیست — ارجاع پایتونی
  می‌ماند ولی شیء ++C نابود شده است. چون ساخت PDF کند است، بستن پنجره
  در همان فاصله برنامه را می‌بست. حالا از `_dialog_alive()` استفاده
  می‌کند.
- همهٔ مسیرهای دیگری که از نخ پس‌زمینه به مدال دست می‌زنند بازبینی
  شدند؛ بقیه از قبل محافظ داشتند.
- آزمون end-to-end با کنترلر واقعی: کار پس‌زمینه ثبت شد، پنجره وسط کار
  نابود شد، ۲۰۰ چرخهٔ رویداد اجرا شد — صفر خطا.
- `tests/test_v162_modal_close.py` با ۱۳ آزمون افزوده شد، شامل آزمونی
  که **اثبات می‌کند بدون محافظ واقعاً `RuntimeError` رخ می‌دهد** (اگر
  روزی شکست بخورد یعنی رفتار Qt عوض شده و محافظ‌ها باید بازبینی شوند).
- راهنمای عیب‌یابی در `docs/TROUBLESHOOTING_FA.md` نوشته شد.

## نسخهٔ ۱٫۷٫۲ — رفع هوش مصنوعیِ سیگنال و دکمه‌های پویش (۲۰۲۶/۰۹/۱۴)

سه ایراد گزارش‌شدهٔ کاربر:

- **مهلت تحلیل سیگنال** از ۴۵ به ۱۲۰ ثانیه رسید تا با مهلت چت یکسان
  باشد. مدل محلی در نخستین درخواست وزنه‌ها را بارگذاری می‌کند و از ۴۵
  ثانیه بیشتر طول می‌کشد؛ `NarrativeWriter` هم در شکست بی‌صدا به متن
  قالبی برمی‌گشت. مهاجرت `SettingsService._repair_stale_defaults`
  نصب‌های موجود را هم بالا می‌کشد، ولی فقط وقتی مقدار دقیقاً ۴۵ باشد —
  انتخاب کاربر هرگز بازنویسی نمی‌شود.
- **صداقت دربارهٔ منبع تحلیل:** `analysis_source` بررسی می‌شود؛ کلیدهای
  تازهٔ `scan_ai_fallback` و `ai_disabled_hint`. نتیجه در
  `AnalysisDialog` باز می‌شود.
- **`configure_button_column()`** در `ui/widgets/common.py`: عرض ثابت
  ۱۵۰ + ارتفاع ردیف ۴۶. پدینگِ خانه دکمه را به ۱۲ پیکسل له می‌کرد.
  روی جدول پویش (ستون ۷) و سابقه (ستون ۶) اعمال شد.
- **`scan_detail_requested`**: کلیک/دوکلیک روی ردیف پویش پنجرهٔ جزئیات
  را باز می‌کند (ستون دکمه استثنا). مقصد
  `MainController.show_scanned_signal_detail`.
- `tests/test_v172_signal_ai_and_scan_ui.py` با ۲۳ آزمون. کل: **۱۰۶۱ سبز**.
- مستند: `docs/SIGNAL_AI_TROUBLESHOOTING_FA.md`.

## نسخهٔ ۱٫۷٫۱ — نمایش گام‌به‌گام ابزارها در گفتگو (۲۰۲۶/۰۹/۱۴)

- ویجت تازهٔ `ui/widgets/tool_trail.py` (`ToolTrail` + `ToolStep`): هر
  ابزاری که دستیار صدا می‌زند به‌صورت یک ردیف با نشانهٔ وضعیت
  (در حال اجرا / موفق / ناموفق)، نام فارسی و ورودی‌ها زیر پاسخ می‌نشیند؛
  با کلیک، نتیجهٔ خام ابزار باز می‌شود.
- رفع دو ایراد ریشه‌ای: `ChatAgent._emit` حالا **پیش از** اجرای ابزار هم
  اعلام می‌کند (`running=True`)، و `MainController` به‌جای جمع‌کردن
  خلاصه‌ها در فهرست مردهٔ `used`، دو سیگنال `chat_tool_started` و
  `chat_tool_finished` می‌فرستد.
- ۱۴ نام فارسی/انگلیسی ابزار به `localization/{fa,en}/chat.json` اضافه شد.
- سبک `toolStep` در هر ۸ پوسته به `ui/themes/stylesheet.py` افزوده شد.
- `tests/test_v171_chat_tool_steps.py` با ۲۷ آزمون. کل مجموعه: **۱۰۳۸ سبز**.
- مستند: `docs/CHAT_TOOL_STEPS_FA.md`.

## نسخهٔ ۱٫۷٫۰ — پویش بازار، قلم‌ها، پوستهٔ تازه

### پویش همهٔ نمادها (`signals/scanner.py`)

- `MarketScanner` با هر دو موتور تزریقی، پس بدون شبکه هم آزمودنی است.
- **هرگز هوش مصنوعی صدا نمی‌زند** — خواستهٔ صریح کاربر برای نسوزاندن
  توکن. آزمون `test_scan_never_calls_ai` با بازرسی کد منبع تضمین
  می‌کند این وابستگی تصادفاً برنگردد.
- نمادها بر پایهٔ **گردش مالی** انتخاب می‌شوند نه حجم؛ حجم بر حسب واحد
  پایه است و بین ارزان و گران قابل مقایسه نیست.
- هم‌زمانی محدود از `performance.parallel_requests`: کاملاً ترتیبی یعنی
  دقیقه‌ها انتظار، کاملاً موازی یعنی خطای ۴۲۹.
- `Application.scan_market` نتیجه را ذخیره می‌کند (به‌جز «انتظار»، وگرنه
  سابقه با ردیف بی‌اثر پر می‌شد) و `analyze_scanned_signal` تحلیل
  هوشمند تک‌نماد را جدا انجام می‌دهد.
- پیشرفت از نخ شبکه با سیگنال `scan_progress_changed` به رابط کاربری
  می‌رسد — همان درس پاسخ جریانی چت.

### درجه‌بندی رنگی (`ui/signal_grading.py`)

- تنها منبع حقیقت رنگ‌ها؛ آستانه‌ها یک بار تعریف شده‌اند تا جدول پویش و
  سابقه و کارت سیگنال یک زبان داشته باشند.
- ۶۰+ سبز پررنگ ✓✓ / ۵۰–۵۹ سبز ✓ / ۳۵–۴۹ کهربایی / زیر ۳۵ خاکستری.
- سطح ریسک از **بدترین** عامل گرفته می‌شود نه میانگین.
- دو دام رفع شد: «انتظار» دیگر کم‌ریسک برچسب نمی‌خورد، و سیگنال بدون
  داده «نامشخص» است نه کم‌ریسک.
- ماژول نام توکن برمی‌گرداند نه رنگ، پس درجه‌بندی در هر هشت پوسته کار
  می‌کند. جدول‌ها هنگام تعویض پوسته بازسازی می‌شوند چون این رنگ‌ها با
  کد در خود آیتم می‌نشینند نه از QSS.

### قلم‌ها

- پیش‌فرض به **وزیرمتن** تغییر کرد (خواستهٔ کاربر). «ب کودک» حذف نشد.
- «ساحل» (OFL) همراه برنامه توزیع می‌شود.
- **ایران‌سنس** قلم تجاری fontiran.com است و حق توزیع مجددش را نداریم،
  پس `bundled=False` گرفت: از قلم‌های نصب‌شدهٔ سیستم یا پوشهٔ
  `assets/fonts/user/` خودکار پیدا می‌شود و تا آن زمان با برچسب «نصب
  نیست» نشان داده می‌شود — نه گزینه‌ای که بی‌صدا کار نکند.

### پوسته و چیدمان

- پوستهٔ هشتم: **نئون کربنی** (زغالی خنثی، لهجهٔ نعنایی، نرم‌ترین گوشه‌ها).
- `ResponsiveGrid`: شمار ستون‌ها با عرض عوض می‌شود (۴ بزرگ / ۲ کوچک).
  پیش‌تر `index % 2` بود یعنی همیشه دو ستون.
- کارت‌های پوسته با `heightForWidth` نسبت ابعاد ثابت گرفتند.

### ورودی‌های عددی

- هیچ قاعدهٔ QSS برای دکمه‌های اسپین‌باکس نبود، پس Qt فلش سیستمی
  می‌کشید. حالا فلش برداری data-URI هم‌رنگ پوسته است.
- `center_numeric_inputs` در `BasePage` اجرا می‌شود، پس هر صفحهٔ تازه‌ای
  هم خودکار پوشش می‌گیرد.
- اندازهٔ متن اسلایدر گرفت (هشت کلیک برای ۱۰۰→۱۴۰ قابل قبول نبود).

### آزمون‌ها

`tests/test_v170_scanner_fonts_theme.py` (۴۵) و
`tests/test_v170_signals_ui.py` (۲۷). دو آزمون قدیمی با واقعیت تازه
هماهنگ شدند: فهرست پوسته‌ها و پیش‌فرض قلم.

---

## نسخهٔ ۱٫۸٫۰ — پوستهٔ طلایی، ویرایشگر ظاهر، چیدمان دلخواه، آموزش

### پوستهٔ «طلایی تاریک» (`gold_dark`)

پوستهٔ نهم. مشکی/ذغالی با طلا و برنز، `is_dark=True`.
**تنها پوسته‌ای که همهٔ شعاع‌هایش صفر است** — «خطوط مربعی» که کاربر
خواست، هویت همین پوسته است و بقیه همچنان گوشهٔ گرد دارند.

افزودن یک پوسته **چهار** جا را لمس می‌کند و هیچ‌کدام اختیاری نیست:
`ui/themes/catalog.py`، `Theme` در `app/core/constants.py`،
`localization/{fa,en}/settings.json` (کلیدهای `themes` و
`theme_descriptions` — فایل `themes.json` **وجود ندارد**)، و مجموعهٔ
انتظار در `tests/test_themes.py`.

آزمون قدیمی «هیچ دکمه‌ای گوشهٔ تیز نداشته باشد» با این پوسته تناقض
داشت. به‌جای ضعیف‌کردن آزمون، قاعده دقیق‌تر نوشته شد: مجموعهٔ
`SQUARE_THEMES` صریح است و برای پوستهٔ مربعی شعاع **باید** صفر باشد و
برای بقیه دست‌کم ۶. آزمون تازه‌ای هم اضافه شد که مربعی‌بودن را در همهٔ
عناصر بررسی می‌کند، نه فقط دکمه‌ها.

### ویرایشگر زندهٔ ظاهر و پوستهٔ سفارشی

`ui/widgets/theme_editor.py` توکن‌ها را به کنترل تبدیل می‌کند: ۲۲ رنگ
(انتخابگر رنگ سیستم)، ۱۲ سنجه (اسلایدر با بازهٔ محدود) و ۲ جلوه. هر
تغییر بی‌درنگ روی کل برنامه می‌نشیند.

`ui/themes/custom.py` پوستهٔ سفارشی را **پایه + تفاوت** ذخیره می‌کند،
نه کپی کامل. دلیلش ارتقاپذیری است: توکنی که در نسخه‌های بعدی اضافه
شود، خودبه‌خود از پوستهٔ پایه می‌آید و پوستهٔ کاربر نمی‌شکند. فایل‌ها در
`data_dir/themes/*.json` می‌نشینند — کنار دادهٔ کاربر، نه داخل پوشهٔ
برنامه.

سه نکته که در عمل لازم شدند:

* **رنگ نامعتبر کل برگهٔ سبک را از کار می‌اندازد** و رابط سفید می‌شود.
  `sanitise_overrides` هر مقدار را پیش از رسیدن به QSS اعتبارسنجی
  می‌کند، سنجه‌ها را در بازه می‌بُرد و کلید ناشناخته را دور می‌ریزد؛ پس
  فایل دستکاری‌شده هم نمی‌تواند چیزی تزریق کند.
* **گرادیان پوستهٔ پایه رنگ انتخابی کاربر را می‌پوشاند.** کاربر رنگ
  اصلی را عوض می‌کرد و هیچ تغییری نمی‌دید. حالا با تغییر `primary`،
  گرادیان کهنه پاک می‌شود مگر اینکه خود کاربر گرادیان داده باشد.
* **بازنویسیِ بی‌اثر ذخیره نمی‌شود.** اگر مقدار به همان چیزی برگردد که
  پوستهٔ پایه دارد، کلید حذف می‌گردد.

پیش‌نمایش با شناسهٔ ثابت `custom__preview` اعمال می‌شود و **ترجیح کاربر
را نمی‌نویسد**؛ تا وقتی «ذخیره» نزده، انتخاب قبلی‌اش دست‌نخورده است.

### چیدمان قابل جابه‌جایی

`ui/widgets/arrangeable.py`. کنترل‌های جابه‌جایی فقط در «حالت چیدمان»
دیده می‌شوند، پس صفحهٔ عادی شلوغ‌تر نشد.

دکمه به‌جای کشیدن و رها کردن، انتخابی آگاهانه است: کارت‌ها خودشان جدول
و نمودار تعاملی دارند و drag روی آن‌ها با انتخاب ردیف تداخل می‌کند.

هنگام جابه‌جایی، ویجت‌ها از چیدمان برداشته و دوباره درج می‌شوند —
**خودِ شیء دست‌نخورده می‌ماند**، پس ردیف‌های جدول و موقعیت پیمایش از
بین نمی‌رود.

ترتیب و نمایانی در **یک رشته** ذخیره می‌شوند (`-` یعنی پنهان)، در
ترجیح `ui.dashboard_layout`. بخشی که در رشته نباشد به انتها می‌رود، پس
افزودن کارت تازه در نسخه‌های بعدی چیدمان ذخیره‌شدهٔ کاربر را نمی‌شکند.

### تمام‌صفحهٔ جدول‌ها

`ui/widgets/table_toolbar.py`. جدول از چیدمانش جدا **نمی‌شود**؛ به یک
`QDialog` منتقل و در بازگشت به همان نمایهٔ قبلی برگردانده می‌شود. هر سه
راه بستن (دکمه، Escape، نوار عنوان) به `closeEvent` می‌رسند، پس
بازگرداندن یک‌جا انجام می‌شود و `_restored` از درج دوباره جلوگیری
می‌کند.

پیوست‌کردن در `BasePage` انجام می‌شود — همان الگوی
`center_numeric_inputs` — پس هر **۱۲** جدول در هر **۸** صفحه خودکار
پوشش گرفت و صفحهٔ تازه‌ای هم که بعداً اضافه شود این کنترل را دارد.

عنوان پنجره از نزدیک‌ترین کارت دربرگیرنده و در نبود آن از عنوان صفحه
خوانده می‌شود، پس با تغییر زبان خودبه‌خود درست می‌ماند.

### کارت وضعیت هوش مصنوعی

`ui/widgets/ai_status_card.py` در نوار کناری، با تایمر سه‌دقیقه‌ای در
کنترلر (نه در ویجت). بررسی **واقعی** است: `autonomous_agent().health_check()`
روی نخ پس‌زمینه، نه خواندن تنظیمات. اگر هوش مصنوعی خاموش باشد اصلاً
درخواستی نمی‌رود.

حالت `checking` رنگ خودش را دارد؛ چراغ سبزِ کهنه در حین بررسی به کاربر
دروغ می‌گوید که سرویس همین الان وصل است.

### آموزش معامله‌گری

پانزده فصل در سه سطح، در `localization/{fa,en}/tutorial.json` — نه در
کد. `Translator.raw()` اضافه شد چون `tr()` ساختار را با `str()` به متن
بدریخت تبدیل می‌کند.

نمای دوستونی با جست‌وجو (در عنوان **و** متن)، پیمایش درس‌به‌درس و حفظ
فصل فعلی هنگام تغییر زبان.

### آزمون‌ها

`test_v180_table_fullscreen.py` (۱۷)، `test_v180_ai_status_card.py` (۱۴)،
`test_v180_tutorial.py` (۲۳)، `test_v180_custom_themes.py` (۳۱)،
`test_v180_arrangeable_layout.py` (۲۶).

## نسخهٔ ۱.۹.۰ — حساب کاربری حرفه‌ای، بازیابی رمز، سیگنال خودکار

**ماشین‌حساب حجم پوزیشن (مورد ۲.۱ نقشهٔ راه).**
`signals/position_sizing.py` ماژول محاسبهٔ خالص و بدون Qt است: از روی
حد ضرر، حجم و ارزش پوزیشن و وجه تضمین و قیمت تقریبی لیکوییدیشن و نسبت
ریسک‌به‌سود و کارمزد رفت‌وبرگشت و کمینهٔ نرخ برد لازم را حساب می‌کند.
جهت معامله از محل حد ضرر استنتاج می‌شود و از کاربر پرسیده نمی‌شود.
ورودی نامعتبر استثنا پرتاب نمی‌کند — ماشین‌حسابی که وسط تایپ بیفتد
بی‌فایده است — و به‌جایش `valid=False` با کلید دلیل برمی‌گرداند.
ویجت `ui/widgets/position_calculator.py` داخل پنجرهٔ جزئیات سیگنال
می‌نشیند، از پیش با اعداد همان سیگنال و سرمایهٔ واقعی کاربر پر می‌شود و
زنده محاسبه می‌کند. هفت هشدار خطر به‌ترتیب شدت، با مهم‌ترینشان «حد ضرر
آن‌سوی لیکوییدیشن».

سه ایراد حین آزمون پیدا و رفع شد: کارمزد یک‌طرفه حساب می‌شد درحالی‌که
برچسبش «رفت‌وبرگشت» بود؛ هدف سود در سمت اشتباه قیمت با `abs` به‌غلط سود
شمرده می‌شد؛ و پنجرهٔ جزئیات هیچ آزمونی نداشت، به همین دلیل دو ایراد
قدیمی در آن پنهان مانده بود (سطر «ورود» برای هر سیگنال واقعی خط تیره
نشان می‌داد چون `entry_price` خوانده نمی‌شد، و صفرهای اضافهٔ قیمت پاک
نمی‌شد چون `rstrip("0")` روی رقم فارسی «۰» بی‌اثر است).

**حساب کاربری حرفه‌ای و بازیابی رمز عبور.**
ایمیل هنگام ثبت‌نام اجباری و یکتا شد — بدون آن، کاربری که رمزش را
فراموش کند برای همیشه از حسابش بیرون می‌ماند. مخزن کاربران
`find_by_email()` و `reset_password()` گرفت (دومی رمز قبلی نمی‌خواهد،
پس هرگز نباید مستقیم به رابط کاربری وصل شود) و یکتایی ایمیل در ساخت و
ویرایش پروفایل بررسی می‌شود.

`app/core/password_reset.py` جریان سه‌گامی را مدیریت می‌کند. تصمیم‌های
امنیتی: کد **چکیده‌شده** ذخیره می‌شود نه خام؛ پس از ۱۵ دقیقه یا ۵ تلاش
ناموفق می‌سوزد؛ یک‌بارمصرف است؛ و برای ایمیل ناموجود هم پاسخ موفق
برمی‌گردد تا این صفحه به ابزار فهرست‌برداری کاربران تبدیل نشود.
پنجرهٔ `ui/dialogs/password_reset_dialog.py` سه گام را در یک پنجره نشان
می‌دهد تا کاربر بتواند وسط کار ایمیلش را اصلاح کند.

**بخش ایمیل در تنظیمات.**
برنامه دسکتاپ است و سرور ندارد، پس به‌جای اجارهٔ سرویس ابری (وابستگی و
هزینهٔ دائمی و فرستادن ایمیل کاربران به ثالث) از SMTP خود کاربر استفاده
می‌کند. شش سرویس آماده با پرکردن خودکار سرور و پورت، به‌علاوهٔ حالت
دستی و دکمهٔ آزمایش اتصال (خارج از نخ رابط کاربری، چون SMTP تا ۲۰ ثانیه
طول می‌کشد). رمز در انبار رمزنگاری‌شده می‌نشیند، نه در جدول تنظیمات، و
هرگز بازخوانده یا نمایش داده نمی‌شود. اگر تنظیم نشده باشد، کد بازیابی
درون برنامه نشان داده می‌شود.

**سیگنال‌گیری خودکار دوره‌ای.**
`signals/auto_scanner.py` دو چرخهٔ هم‌زمان دارد: «تمرکز» روی نمادهای
پراطمینانِ پویش‌های پیشین با فاصلهٔ کوتاه، و «چرخش کامل» روی همهٔ
نمادها با فاصلهٔ بلندتر که فهرست تمرکز را تغذیه می‌کند. زمان‌بند هیچ
وابستگی به Qt ندارد و بدون شبکه آزمودنی است. چرخش کامل اولویت دارد
چون فهرست تمرکز از آن ساخته می‌شود. اگر چرخهٔ قبلی تمام نشده باشد نوبت
تازه **رد می‌شود** نه اینکه صف شود؛ صف‌شدن پویش‌ها برنامه را خفه
می‌کند. فهرست تمرکز پس از هر چرخه مرتب می‌شود ولی یک‌باره خالی
نمی‌شود، چون نوسان لحظه‌ای نباید آن را بی‌ثبات کند. اطلاع‌رسانی سیگنال
تکراری را نادیده می‌گیرد، وگرنه کاربر اعلان‌ها را می‌بندد.

**رفع خطای «HTTP 500» اولاما.**
علت واقعی: برنامه پنجرهٔ متن ثابت ۸۱۹۲ می‌خواست (فرمول
`max_tokens*2 + 2048` که به طول پیام کاری نداشت) درحالی‌که
`ollama run` در ترمینال پیش‌فرض ۴۰۹۶ را می‌گیرد. روی کارت گرافیک با
VRAM محدود، همان مدلی که دستی کار می‌کند از راه برنامه شکست می‌خورد —
حتی با ۱۶ گیگ رم سیستم. سه اصلاح: پنجره از روی طول واقعی پیام حساب
می‌شود؛ در صورت خطای سرور یک بار با پنجرهٔ محافظه‌کارانه و یک بار بدون
`format=json` تلاش دوباره می‌شود (مدل‌های استدلالی با JSON اجباری
می‌شکنند)؛ و متن خطای واقعی اولاما به‌جای «HTTP 500» خالی نشان داده
می‌شود.

**کادر خالی نوار کناری.**
آن مستطیل، کارت وضعیت اتصال صرافی بود که `set_connection_card` هرگز
برایش صدا زده نمی‌شد (فقط در یک اسکریپت پیش‌نمایش). حالا از
`_refresh_connection_indicator` تغذیه می‌شود. شمارندهٔ بازار نوار پایین
هم که همین وضع را داشت، وصل شد.

**آزمون‌ها:** `tests/test_v190_position_sizing.py` (۳۵) و
`tests/test_v190_account_and_auto_scan.py` (۴۱).


---

## نسخهٔ ۱.۹.۱ — پیگیری نتیجهٔ واقعی سیگنال‌ها (مورد ۱.۵ نقشهٔ راه)

کاربر پیش‌تر گفته بود اگر مورد ۱.۵ انتخاب شود باید **حالت کامل و
خودکار** پیاده شود: پایش پس‌زمینه تا رسیدن به هدف یا حد ضرر، به‌علاوهٔ
صفحهٔ آمار عملکرد به تفکیک نماد، تایم‌فریم و سطح اطمینان. همین انجام شد.

### چرا این مهم‌ترین موردِ باقی‌مانده بود

تا این نسخه برنامه سیگنال می‌ساخت و بعد فراموشش می‌کرد. «ضریب اطمینان
۷۲٪» یک **ادعا** بود که هیچ‌کس نمی‌توانست ردش کند یا تأییدش کند. حالا
هر سیگنال جهت‌دار دنبال می‌شود و واقعیت ثبت می‌شود. اگر معلوم شود
سیگنال‌های با اطمینان بالا بهتر از سیگنال‌های با اطمینان پایین نیستند،
یعنی موتور باید بازنگری شود — و این را فقط با داده می‌توان فهمید.

### ساختار

* `app/database/models.py` → `SignalOutcomeRecord`، جدول
  `signal_outcomes`، کلید خارجی یکتا به `signals.id` با
  `ondelete=CASCADE`. مهاجرت `a4f1c9d2b730` (سرشاخهٔ تازه؛ رفت و برگشت
  هر دو آزموده شد). پایگاه داده حالا ۲۰ جدول دارد.
* `signals/outcome_tracker.py` — **منطق خالص**، بدون Qt و بدون شبکه،
  دقیقاً مثل `auto_scanner.py`. توابع `update_outcome`، `summarize`،
  `group_by`، `confidence_buckets`، `expiry_for`.
* `app/database/repositories/outcome_repository.py` — ثبت، خواندن،
  به‌روزرسانی و گزارش. هیچ تصمیمی اینجا گرفته نمی‌شود.
* `Application.refresh_outcomes()` — یک دور بررسی قیمت روی سیگنال‌های
  باز. قیمت‌ها **یک‌جا** گرفته می‌شوند (`get_all_tickers`) نه نماد به
  نماد؛ وگرنه سی سیگنال باز یعنی سی درخواست و برخورد با محدودیت نرخ.
* `MainController.start_outcome_tracker()` — تایمر ۳۰ ثانیه‌ای که موعد
  را می‌سنجد؛ فاصلهٔ واقعی (`signals.track_interval`، پیش‌فرض ۱۸۰
  ثانیه) را کاربر تعیین می‌کند. محافظ همپوشانی با کلید `outcome-track`.
* `ui/widgets/performance_view.py` + زبانهٔ دوم صفحهٔ گزارش‌ها.

### سه تصمیمی که آمار را صادق نگه می‌دارد

۱. **حد ضرر بر هدف مقدم است.** اگر در یک بازه هم هدف و هم حد ضرر لمس
   شده باشد، باخت ثبت می‌شود. با یک قیمت در هر بازه نمی‌توان فهمید کدام
   زودتر بوده؛ فرض خوش‌بینانه آمار را نظام‌مند بهتر از واقعیت نشان
   می‌دهد و این بدترین کاری است که یک ابزار سنجش می‌تواند بکند.
۲. **اهرم وارد درصد نمی‌شود.** اهرم انتخاب لحظه‌ای کاربر است نه ویژگی
   سیگنال؛ واردکردنش مقایسهٔ دو سیگنال را بی‌معنا می‌کرد.
۳. **نرخ برد فقط روی سیگنال‌های بسته‌شده.** شمردن سیگنال‌های باز نرخ را
   مصنوعی پایین می‌آورد. سیگنال منقضی هم جداگانه شمرده می‌شود چون نه برد
   بود نه باخت.

به‌علاوه: هدف‌ها پله‌ای شمرده می‌شوند (هدف اول سیگنال را نمی‌بندد)،
مهلت پیگیری به تایم‌فریم بستگی دارد (۱۵ دقیقه‌ای ۲۴ ساعت، روزانه ۵۰۴
ساعت)، و سیگنال بسته‌شده دیگر با قیمت‌های بعدی بازنویسی نمی‌شود.

### نقص‌هایی که پیش از تحویل پیدا و رفع شد

* **`TypeError` در مقایسهٔ تاریخ.** پایگاه داده تاریخ بی‌منطقهٔ زمانی
  می‌دهد ولی منطق با زمان آگاه کار می‌کند. تابع `as_utc()` اضافه شد.
* **دو نقص راست‌به‌چپ، در بازبینی چشمی رندر پیدا شد.** ستون «برد /
  باخت» برای ۲ برد و ۴ باخت عبارت «۴ / ۲» نشان می‌داد — یعنی کاربر
  باخت را برد می‌خواند. علامت منفی هم به انتهای عدد می‌پرید («۰.۳۱R-»).
  با نشانگر `\u200e` در `PerformanceView._ltr()` حل شد.

### جای‌گذاری در رابط کاربری

زبانهٔ دوم صفحهٔ گزارش‌ها، نه صفحهٔ یازدهم نوار کناری: ده جای نوار پر
است و میان‌برها `Ctrl+1` تا `Ctrl+0` هستند. از این گذشته «چه سیگنالی
دادیم» و «چه شد» یک پرسش‌اند از دو زاویه. هر سه جدول صفحه خودبه‌خود
کنترل تمام‌صفحه گرفتند (قاعدهٔ `BasePage`).

### آزمون

`tests/test_v191_outcome_tracking.py` — ۶۹ آزمون: منطق درصد و R، تقدم
حد ضرر، هدف‌های پله‌ای، انقضا، قفل‌شدن سیگنال بسته، آمار و تفکیک، مخزن
(یکتایی، WAIT، بدون حد ضرر، `backfill`)، نمای عملکرد (از جمله هر دو
نقص راست‌به‌چپ)، بومی‌سازی هر دو زبان، و کلیدهای تنظیمات.

---

## نسخهٔ ۱.۹.۲ — فهرست‌های دیده‌بانی چندگانه (مورد ۵.۳ نقشهٔ راه)

### چه چیزی واقعاً نیمه‌کاره بود

بررسی پیش از کار نشان داد جدول `watchlists` از همان ابتدا ستون‌های
`list_name`، `position` و `note` را داشت، با قید یکتایی روی
(`list_name`, `symbol_id`). یعنی مدل داده از روز اول چند فهرست را
پشتیبانی می‌کرد، ولی رابط کاربری هرگز از نام `"default"` بیرون نرفته بود
و هیچ راهی برای مرتب‌سازی وجود نداشت. پس این مورد **کوچ داده لازم
نداشت**؛ کار، کامل‌کردن چیزی بود که نیمه‌ساخته رها شده بود.

### آنچه افزوده شد

* **لایهٔ مخزن** — یازده متد تازه در `symbol_repository.py` کنار سه متد
  قدیمی که دست‌نخورده ماندند: `watchlist_names`، `watchlist_counts`،
  `create_watchlist`، `rename_watchlist`، `delete_watchlist`،
  `watchlist_details`، `reorder_watchlist`، `move_in_watchlist`،
  `set_watchlist_note`، `find_symbol_lists`.
* **`ui/widgets/watchlist_panel.py`** — انتخاب فهرست با شمارش اعضا،
  ساخت/تغییر نام/حذف فهرست، جابه‌جایی بالا و پایین، یادداشت، و حذف نماد.
* **جای‌گذاری:** صفحهٔ بازارها دو زبانه شد («همهٔ بازارها» و
  «فهرست‌های دیده‌بانی») — نه صفحهٔ یازدهم نوار کناری.
* **فصل ۱۷ آموزش** در هر دو زبان: چرا چند فهرست، چرا ترتیب دستی به‌جای
  مرتب‌سازی خودکار، و چرا یادداشت مهم است.

### تصمیم‌هایی که از دست‌رفتن داده جلوگیری می‌کنند

۱. **فهرست «پیش‌فرض» نه حذف می‌شود نه تغییر نام.** فقط خالی می‌شود.
   ستارهٔ جدول بازارها به این نام گره خورده است؛ اگر نامش عوض شود،
   ستاره‌ها به فهرستی اشاره می‌کنند که دیگر وجود ندارد.
۲. **تغییر نام روی نام موجود رد می‌شود.** ادغام خاموش دو فهرست، از نگاه
   کاربر یعنی گم‌شدن داده.
۳. **`reorder_watchlist` اعضای نیامده در ورودی را حذف نمی‌کند**، آن‌ها
   را با حفظ ترتیب نسبی به انتها می‌برد. اگر رابط کاربری روزی فقط
   ردیف‌های دیده‌شده را بفرستد، بقیه قربانی نمی‌شوند.

### دو نقص واقعی که در همین دور پیدا و رفع شد

* **`QTableWidget.selectRow()` در چیدمان راست‌به‌چپ بی‌صدا کار نمی‌کند.**
  ردیف جاری منفی‌یک می‌ماند و انتخاب انجام نمی‌شود. در حالت انگلیسی
  درست کار می‌کرد و فقط در فارسی می‌شکست — یعنی پس از هر جابه‌جایی،
  انتخاب کاربر می‌پرید و باید دوباره روی نماد کلیک می‌کرد.
  `setCurrentCell()` جایگزین شد و آزمونی گذاشته شد که جهت برنامه را
  راست‌به‌چپ می‌کند تا کسی دوباره به `selectRow` برنگردد.
* **دکمهٔ نقش «خطر» در حالت غیرفعال همچنان قرمزِ پررنگ بود.** برگهٔ سبک
  برای `role="primary"` قاعدهٔ `:disabled` داشت ولی برای `danger` نداشت،
  پس «حذف فهرست» غیرفعال، دقیقاً مثل فعال دیده می‌شد. قاعدهٔ `:disabled`
  برای `danger` اضافه شد و معلوم شد `role="success"` اصلاً قاعدهٔ پایه
  نداشته (دکمهٔ کنش در صفحهٔ چت خاکستری معمولی درمی‌آمد) — آن هم افزوده
  شد. هر دو در تمام نُه پوسته بررسی شدند.

### آزمون

`tests/test_v192_watchlist.py` — ۷۸ آزمون: نام‌ها و شمارش، ساخت و تغییر
نام و حذف (با محافظت از فهرست پیش‌فرض)، ترتیب دستی و مرزهایش، یادداشت،
سازگاری کامل رابط قدیمی، ویجت (از جمله دام راست‌به‌چپ) و بومی‌سازی هر دو
زبان. انتظار شمار جدول‌های صفحهٔ بازارها در
`tests/test_v180_table_fullscreen.py` از ۱ به ۲ رسید، چون جدول تازه
خودبه‌خود کنترل تمام‌صفحه گرفت.

---

## نسخهٔ ۱.۹.۳ — پیمایش صفحه و اصلاح اندازه‌ها

### گزارش کاربر

«قسمت سیگنال را اسکرول بده، همه چی در هم برهم شده، اندازه‌ها را درست
کن، صفحه اسکرول بخوره.»

### ریشهٔ واقعی مشکل

صفحهٔ سیگنال‌ها هیچ `QScrollArea` ای نداشت و همهٔ بخش‌هایش مستقیم در
`layout_root()` روی هم انباشته شده بودند. اندازه‌گیری نشان داد صفحه
**حداقل ۱۱۸۱ پیکسل** ارتفاع طلب می‌کند؛ وقتی پنجره ۸۰۰ پیکسل بود، Qt
به‌جای پیمایش، همه‌چیز را تا حد حداقل‌ها فشرده می‌کرد. این دقیقاً همان
«در هم برهم» شدنی است که کاربر دید — نه یک ایراد ظاهری، بلکه نتیجهٔ
نبود پیمایش.

سه عامل جداگانه به آن ۱۱۸۱ پیکسل دامن می‌زد و هر سه رفع شد:

۱. **نبود پیمایش** — عامل اصلی.
۲. **ویجت‌های مخفی که فضا می‌گرفتند.** `progress` و `agent_trail` با
   وجود `hide()` هرکدام ۴۸۰ پیکسل ارتفاع گرفته بودند.
۳. **برچسب چندخطی فشرده‌شده.** `wait_note` با `setWordWrap(True)`
   ارتفاع لازمش (۶۳ پیکسل) را به چیدمان اعلام نمی‌کرد و ۲۱ پیکسل
   می‌گرفت، پس متنش نصفه دیده می‌شد.

### راه‌حل: پیمایش به‌صورت قابلیت `BasePage`

به‌جای وصله‌کردن یک صفحه، پرچم `scrollable` به `BasePage` اضافه شد.
صفحه‌ای که آن را `True` کند، محتوایش داخل `QScrollArea` می‌رود ولی
**سرآیند بیرون می‌ماند** تا عنوان صفحه همیشه دیده شود. چون
`layout_root()` همچنان چیدمان محتوا را برمی‌گرداند، هیچ زیرکلاسی و هیچ
کد بیرونی تغییری نمی‌بیند — ده صفحهٔ موجود دست‌نخورده کار می‌کنند.

### جدول پویش: رشد تا سقف معقول

ارتفاع ثابت ۱۹۰ پیکسلی جدول نتیجهٔ پویش یعنی از ردیف پنجم به بعد زیر
متن راهنما پنهان می‌شد. حالا `_fit_scan_table_height()` ارتفاع را با
تعداد نتیجه‌ها تنظیم می‌کند، تا سقف `SCAN_VISIBLE_ROWS = 12`. سقف عمدی
است: بدون آن، شصت نتیجهٔ پویش صفحه‌ای چند هزار پیکسلی می‌ساخت. از سقف
به بعد خود جدول اسکرول می‌گیرد — اسکرول تو در تو فقط وقتی پذیرفتنی است
که گریزی نباشد.

### نقصی که این بررسی لو داد

آزمونِ «آیا صفحه در نمایشگر لپ‌تاپ جا می‌شود» روی هر ده صفحه اجرا شد و
**صفحهٔ گزارش‌ها** را هم گیر انداخت: حداقل ۹۷۵ پیکسل. علتش این بود که
زبانهٔ «عملکرد» با `_wrap_scroll` پیچیده شده بود ولی زبانهٔ «گزارش» نه،
و همان یکی ارتفاعش را به کل صفحه تحمیل می‌کرد. حالا هر دو زبانه پیمایش
دارند و صفحه به ۲۲۲ پیکسل رسید.

### نتیجهٔ عددی

| صفحه | پیش از این | اکنون |
|---|---|---|
| سیگنال‌ها | ۱۱۸۱ | ۱۷۷ |
| گزارش‌ها | ۹۷۵ | ۲۲۲ |

هر ده صفحه اکنون در پنجرهٔ ۸۰۰ پیکسلی جا می‌شوند.

### آزمون

`tests/test_v193_page_scrolling.py` — ۲۵ آزمون. مهم‌ترینشان
`test_page_fits_a_laptop_screen` است که **روی هر ده صفحه** اجرا می‌شود:
از این پس هر صفحه‌ای که بیش از حد بلند شود، پیش از رسیدن به دست کاربر
گیر می‌افتد. بقیه: واقعی‌بودن پیمایش (نه صرفاً وجود `QScrollArea`)،
بیرون‌ماندن سرآیند، خاموش‌بودن پیمایش افقی، رشد و سقف جدول پویش،
فضا‌نگرفتن ویجت مخفی، و دست‌نخوردگی صفحه‌های غیراسکرولی.

---

## نسخهٔ ۱.۹.۴ — بهینه‌سازی سرعت، خروجی CSV، و پاک‌سازی آرشیوها

### پاک‌سازی حافظه

بیست آرشیو نسخه‌های ۱.۳.۱ تا ۱.۸.۰ (مجموعاً ۱۸ مگابایت) به درخواست
کاربر حذف شدند. فضای کاری از ۴۶ به ۲۹ مگابایت رسید. نسخه‌های ۱.۹.x
نگه داشته شدند.

### بررسی کارهای ناتمام

پیش از افزودن هر چیز تازه، پروژه اسکن شد: صفر مورد
`TODO/FIXME/HACK/NotImplementedError` و صفر تابع بدنه‌خالی
غیرانتزاعی. چیزی از دورهای پیشین نیمه‌کاره نمانده بود.

### بهینه‌سازی: اندازه‌گیری پیش از تغییر

هیچ تغییری بدون پروفایل انجام نشد. `python -X importtime` زنجیرهٔ
واقعی را نشان داد و دو گلوگاه ساختاری لو رفت.

**گلوگاه یکم — خواندن تنظیمات، pandas را بار می‌کرد.**

زنجیره این بود:

```
app.config.settings_service
  → app.database.repositories            (وارد‌کردن همهٔ مخازن)
    → outcome_repository
      → signals.outcome_tracker
        → signals/__init__.py            ← مقصر
          → SignalEngine → indicators → pandas
```

‏`signals/outcome_tracker.py` خودش جز `dataclass` و `datetime` هیچ
وابستگی‌ای ندارد، ولی چون داخل بستهٔ `signals` است، وارد کردنش
`__init__.py` را اجرا می‌کرد و آن هم کل موتور را می‌کشید. با
`__getattr__` سطح ماژول (PEP 562) نام‌های سنگین تنبل شدند:

| سنجه | پیش از این | اکنون |
|---|---|---|
| `import signals.outcome_tracker` | ۵۲۹ ms | **۴۷ ms** |
| `import app.application` | ۱۸۵۰ ms | **۶۳۱ ms** |
| کل راه‌اندازی | ۲۶۴۷ ms | **۱۲۰۶ ms** |

‏`from signals import SignalEngine` دقیقاً مثل گذشته کار می‌کند.

**گلوگاه دوم — ویجت‌ها پایگاه داده را می‌کشیدند.**

‏`ui/widgets/watchlist_panel.py` برای یک ثابت رشته‌ای،
`app.database.repositories.symbol_repository` را وارد می‌کرد و با آن
کل SQLAlchemy را. ثابت به `app/core/constants.py` منتقل شد
(`DEFAULT_WATCHLIST`). هم معماری درست‌تر شد و هم لایهٔ رابط کاربری سبک.

**گلوگاه سوم — کش اندیکاتور که کار را دوباره انجام می‌داد.**

در `IndicatorEngine.calculate()`، فراخوانی `candles_to_dataframe()`
**پیش از** بررسی کش بود. یعنی در تحلیلی که صددرصد کش داشت، برای هر ۲۴
اندیکاتور یک DataFrame از نو ساخته می‌شد. حالا `_data_fingerprint()`
کلید کش را از خود لیست کندل‌ها می‌سازد و تبدیل تا پس از بررسی کش عقب
افتاده است. `calculate_many()` هم تبدیل تنبل گرفت.

| ۲۴ اندیکاتور | پیش از این | اکنون |
|---|---|---|
| بدون کش | ۴۸٫۴ ms | ۴۲٫۸ ms |
| با اصابت کامل کش | ۱۳٫۴ ms | **۰٫۱ ms** |

در پویش ۱۴۰۰ نمادی این چند ثانیه صرفه‌جویی است.

### ابزار تازه: خروجی CSV از همهٔ جدول‌ها

از سیزده پیشنهاد سند تازه، مورد «الف‑۴» همین دور پیاده شد چون
بیشترین بازده را به ازای کمترین ریسک داشت. دکمه به `TableToolbar`
اضافه شد و چون آن نوار **خودکار به هر جدول برنامه** وصل می‌شود، این
قابلیت یکجا همه‌جا آمد.

چهار تصمیم ریز که کیفیت خروجی را می‌سازند:

۱. **`utf-8-sig` نه `utf-8`.** اکسل ویندوز بدون BOM فایل را با
   کدگذاری محلی می‌خواند و متن فارسی به هم می‌ریزد — همان دردی که یک
   بار در خروجی PDF داشتیم.
۲. **نشانگرهای `\u200e` و `\u200f` پاک می‌شوند.** روی صفحه لازم‌اند تا
   `BTC/USDT` وارونه نشود، در اکسل فقط کاراکتر ناخوانا می‌سازند.
۳. **ستون و ردیف پنهان در خروجی نمی‌آیند** — کاربر همان چیزی را
   می‌گیرد که می‌بیند.
۴. **سلول ویجت‌دار خالی می‌ماند** — متن دکمهٔ «تحلیل هوشمند» داده نیست.

### سند پیشنهاد

`docs/NEW_TOOLS_PROPOSAL_FA.md` — سیزده پیشنهاد در چهار دسته
(کم‌کردن کار تکراری، فهم تصمیم برنامه، جلوگیری از اشتباه، سرعت و
آسودگی)، هیچ‌کدام هم‌پوشان با ۲۲ مورد باقی‌ماندهٔ نقشهٔ راه. هر مورد
با اندازه (⚡/◐/⬛) و دلیل انتخاب. یک مورد هم عمداً رد شد: اعلان
بیرونی برای هر سیگنال، چون کاربر را به واکنش فوری به هر تکان بازار
عادت می‌دهد.

### آزمون

`tests/test_v194_performance.py` — ۲۴ آزمون. نکتهٔ روش‌شناختی: این‌ها
زمان اجرا را نمی‌سنجند (که روی ماشین‌های مختلف فرق می‌کند و آزمون
شکننده می‌سازد)، بلکه **ساختارهایی** را می‌سنجند که سرعت از آن‌ها
می‌آید — مثلاً «آیا `import signals.outcome_tracker` پای pandas را وسط
می‌کشد؟» در فرایند مستقل بررسی می‌شود. مهم‌ترین آزمون این دسته
`test_calculate_many_matches_individual_results` است: بهینه‌سازی‌ای که
خروجی را عوض کند، بهینه‌سازی نیست.

## نسخهٔ ۱.۹.۱۴ — ۲۰۲۶/۰۹/۲۰

**پویشگر اسکلپ** (`trading/scalp_scanner.py`): فیلتر سخت نقدینگی (دادهٔ زنده
نشان داد بیشترین نوسان‌گرها حجم ۴ تا ۶۷ دلار دارند)، اسپرد از **دفتر سفارش
واقعی** (تخمین ۲۴ساعته تا ۱۱ برابر خطا داشت)، سود خالص پس از کارمزد و اسپرد،
و `feasibility_note` که غیرواقعی بودن هدف را پیش از معامله اعلام می‌کند.

**موتور معاملهٔ خودکار** (`trading/auto_trader.py`): پیش‌فرض کاغذی؛ معاملهٔ
واقعی نیازمند حالت `live` **و** عبارت تأیید دقیق؛ سقف سخت اهرم ۲۵ و همزمانی
۱۰؛ سقف زیان روزانه با توقف خودکار؛ اولویت حد ضرر بر حد سود؛ `LiveOrderGateway`
عمداً پیاده‌نشده و صریح شکست می‌خورد.

**سرویس اسکلپ** (`trading/scalp_service.py`): ریاضی فهرست را کوتاه می‌کند،
هوش مصنوعی فقط ۵ نامزد برتر را بازبینی می‌کند و هرگز مانع کار نمی‌شود.

**تنظیم‌ها:** ۱۶ کلید تازه (۱۳۲ در مجموع). **آزمون‌ها:** ‎+۵۲.

## نسخهٔ ۱.۹.۱۳ — ۲۰۲۶/۰۹/۲۰

**ارائه‌دهندهٔ آشنا (AshnaAI).** پیش‌تنظیم تازه در `ai/providers/catalog.py`؛
پایه `https://api.ashna.ai/v1/api` (مسیر `/v1/api` نه `/v1`)، سازگار با
OpenAI پس بدون کلاس تازه. ۸ مدل پیشنهادی از کاتالوگ رسمی؛ شناسهٔ ایجنت
سفارشی هم به‌جای نام مدل پذیرفته می‌شود. با سرور ساختگی سرتاسری آزموده شد:
`/models`، `/chat/completions`، حالت JSON و خطای کلید نادرست.

**عیب‌یاب سازنده‌ها.** `tools/build_doctor.py` + `scripts/doctor.bat`؛ ده
بررسی با راه‌حل مشخص برای هرکدام، خروجی در `doctor_report.txt`.

**آزمون‌ها:** ‎+۳۱.

## نسخهٔ ۱.۹.۱۲ — ۲۰۲۶/۰۹/۱۹

**رفع ربات ساخت نصبی.** سه ایراد ویندوزی: نبودِ `pause` (پنجره بی‌صدا بسته
می‌شد)، تشخیص ناقص `.venv` (پوشه بود ولی `activate.bat` نبود)، و میان‌بر
فروشگاه مایکروسافت به‌جای پایتون. به‌علاوه: خواندن مسیر ISCC از رجیستری و
تشخیص «ISCC موفق ولی بدون خروجی».

**نسخهٔ موبایل.** `mobile/` — Kivy + موتور خالص‌پایتون بدون pandas/numpy.
هم‌ارزی عددی با دسکتاپ آزمون دارد (۰ اختلاف در ۸ اندیکاتور، تلورانس 1e-9).
بدون کلید API روی گوشی.

**ربات APK.** `tools/build_apk.py` + `scripts/build_apk.bat`؛ تشخیص WSL،
ترجمهٔ مسیر ویندوز به `/mnt/…`، آزمون هم‌ارزی پیش از ساخت.

**آزمون‌ها:** ‎+۴۱.

**مستندات:** `docs/MOBILE_FA.md`.

## نسخهٔ ۱.۹.۱۱ — ۲۰۲۶/۰۹/۱۹

**رفع اشکال «همیشه منتظر» (بحرانی).** `_apply_ai_decision` تصمیم را از
`decision.direction` می‌خواند؛ چنین صفتی روی `AnalysisResult` وجود ندارد و
`getattr(..., None)` خطا را می‌بلعید، پس کل پاسخ مدل دور ریخته می‌شد. حالا
از `decision.structured` با کلیدهای صریح خوانده می‌شود و مقدار جهت با
`SignalDirection` اعتبارسنجی می‌گردد. ضریب اطمینان صفر دیگر حذف نمی‌شود.

**سرعت.** در حالت `ai_only` درخواست روایت زائد حذف شد: ۲ فراخوانی به‌جای ۴.

**ربات ساخت فایل نصبی.** `tools/build_installer.py` + `installer/CryptoAITrader.iss`
+ `scripts/build_installer.bat`.

**ربات به‌روزرسانی.** `app/core/updater.py` + بخش تازه در تنظیمات ← پشتیبان‌گیری.
مقایسهٔ نسخه عددی، بررسی SHA-256، پیش‌فرض خاموش، دادهٔ کاربر محفوظ.

**آزمون‌ها:** ‎+۴۷ (۱۷ سیگنال، ۳۰ ساخت/به‌روزرسانی).

**مستندات:** `docs/BUILD_INSTALLER_FA.md`.

