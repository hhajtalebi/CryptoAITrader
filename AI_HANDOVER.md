# تحویل پروژه به هوش مصنوعی بعدی

این فایل منبع ادامهٔ کار است. با هر نسخه باید همین فایل به‌روز شود.
تاریخ این نگارش: ۲۰۲۶-۰۹-۲۲، نسخهٔ برنامه `1.9.23`.

## قانون کار با کاربر

- پاسخ به کاربر فارسی است.
- تا وقتی کاربر نسخه را تست نکرده و صریح نگفته «مورد تأیید است»،
  کامیت و پوش به گیت‌هاب ممنوع است.
- بعد از هر تغییر قابل تست، زیپ تازه ساخته شود و روی همان صفحهٔ دانلود
  گذاشته شود. نشان‌دادن زیپ در پنل Diff دانلود محسوب نمی‌شود.
- تنظیم ذخیره‌شدهٔ کاربر بازنویسی نمی‌شود. کلید تازه می‌تواند پیش‌فرض
  داشته باشد؛ کلید موجود در پایگاه داده را مهاجرت نکن.
- سفارش واقعی تا تأیید صریح کاربر کاغذی می‌ماند. payload سفارش LBank
  یا Toobit را حدس نزن. اگر اجراکنندهٔ تأییدشده نباشد، درگاه باید
  استثنا بدهد، نه اینکه وانمود کند سفارش رفته است.
- صفحهٔ معاملات نباید اسکرول کل صفحه بگیرد. ارتفاع حداقل باید از
  ۶۶۰ پیکسل لپ‌تاپ کمتر بماند. کنترل تازه را در ردیف افقی موجود یا داخل
  جعبهٔ جمع‌شونده بگذار. زبانهٔ تنظیمات کارایی و زبانه‌های گزارش باید
  `QScrollArea` باشند.
- آزمون تازه اگر به رابط وابسته نیست، PySide6 را وارد نکند. در این
  محیط `python3` سیستمی PySide6 ندارد.

## برنامه چیست

برنامهٔ دسکتاپ تحلیل رمزارز و سیگنال فیوچرز است. مالک مخزن:
`hhajtalebi/CryptoAITrader`. رابط PySide6، داده از REST و WebSocket،
پایگاه SQLite، هوش مصنوعی اختیاری (Ollama یا سرویس سازگار با OpenAI،
از جمله آشنا).

پیش‌فرض معامله کاغذی است. قیمت زنده است. سفارش واقعی فقط وقتی هر سه
شرط برقرار باشد می‌رود، و حتی آن‌وقت هم تا وصل شدن اجراکنندهٔ تأییدشده
بلند شکست می‌خورد:

1. معاملهٔ خودکار روشن باشد.
2. `scalp.mode` برابر `live` باشد.
3. عبارت تأیید دقیقاً `معامله واقعی را می‌پذیرم` باشد.

## اجرا

در ویندوز: `RUN_WINDOWS.bat`. پایتون ۳٫۱۱ یا بالاتر. وابستگی‌ها در
`pyproject.toml`. نسخه در `app/core/constants.py` با نام `APP_VERSION`.

## نقشهٔ کوتاه

| مسیر | نقش |
|---|---|
| `app/application.py` | ساخت سرویس‌ها، عامل هوش مصنوعی، مهلت سیگنال |
| `app/config/defaults.py` | کلیدها و پیش‌فرض‌ها. نبود کلید در پایگاه خطا نیست |
| `market/engine.py` | REST، سوکت، کش، `peek_candles`، `keepalive` |
| `market/live_feed.py` | قیمت زنده. `is_fresh` اگر تیک تازه باشد حتی با شکست REST |
| `ui/controllers/main_controller.py` | تنها جایی که صفحه را به موتور وصل می‌کند |
| `ui/pages/trades_page.py` | پنل خودکار و دستی. جعبهٔ دستی پیش‌فرض جمع است |
| `ui/pages/analysis_page.py` | `set_live_price` بدون redraw کامل؛ `set_chart_data` کل کندل را می‌کشد |
| `trading/auto_trader.py` | باز، پایش، بستن. سقف اهرم `HARD_MAX_LEVERAGE = 200` |
| `trading/micro_plan.py` | سود خالص بعد از کارمزد |
| `trading/execution.py` | `build_gateway`؛ اجراکنندهٔ پیش‌فرض `None` |
| `trading/scalp_service.py` | ساخت `AutoTradeConfig` از تنظیم‌ها |
| `ai/speed_profile.py` | سریع / متعادل / عمیق |
| `app/database/repositories/trade_repository.py` | `open_trade`/`close_trade` هر دو `fee` می‌گیرند |

## نسخهٔ ۱.۹.۲۳ — چه عوض شد

هیچ قابلیت تازه‌ای ندارد. هفت آزمون مردود به صفر رسید.

- **پایان خط `.bat` (علت واقعی، پنج آزمون).** پنج فایل `.bat` در مخزن
  LF داشتند و `.gitattributes` وجود نداشت، پس هر کلون روی هر
  سیستم‌عاملی LF می‌گرفت. حالا `.gitattributes` قاعدهٔ
  `*.bat text eol=crlf` دارد و `git ls-files --eol '*.bat'` مقدار
  `i/lf w/crlf` می‌دهد. بلاب‌های گیت دست‌نخورده ماندند. **احتمالاً
  همین علت شکست سازنده روی ویندوز کاربر است** — عیب‌یاب خودِ برنامه
  اسکریپت‌ها را «ناموفق» می‌زد.
- **دو آزمون کهنه.** یکی روی منبع `_auto_trader` دنبال رشتهٔ
  `build_trader_config` می‌گشت که یک لایه واسطهٔ `_auto_trade_config()`
  فاصله افتاده بود؛ دیگری فیکِ تنظیماتی داشت که فقط `get_int` داشت در
  حالی که پروفایل سرعت از ۱.۹.۲۲ با `get` می‌خواند. در هر دو مورد
  **کد محصول درست بود**. هر دو آزمون رفتاری شدند و روی کدِ عمدی
  خراب‌شده هم آزموده شدند تا ثابت شود رگرسیون را می‌گیرند.
- **پاک‌سازی قطعی پنجره.** `tests/conftest.py::destroy_window` اضافه
  شد. `deleteLater()` + `processEvents()` درخت ویجت را آزاد نمی‌کند؛
  باید `sendPostedEvents(None, QEvent.DeferredDelete)` صدا زده شود.
  `test_new_pages.py` از رد شدن بر سقف ۶۰۰ ثانیه به ۴۶ ثانیه رسید و
  `test_exchange_login_flow.py` از ۲۱۵ ثانیه به ۵۸ ثانیه. زمان کل
  مجموعه از ۱۰۰۴ ثانیه به ۲۸۳ ثانیه رسید و شمار آزمون‌های قبول از
  ۲۰۱۹ به **۲۰۵۰** — چون ۲۴ آزمون `test_new_pages.py` پیش‌تر به‌خاطر
  رد شدن از سقف زمانی هرگز شمرده نمی‌شدند. **این اشکال
  کاربر را نمی‌زند** — `main.py` فقط یک بار پنجره می‌سازد.
- **مستندات.** نسخه در **شش** جا ذکر می‌شود نه چهار جا: دو README هم
  هست. `README_EN.md` هنوز «Version 1.0.0» و «Python 3.12+» می‌گفت.
  شمار کلیدهای تنظیمات ۱۴۲ است نه ۱۳۶. `PROJECT_PROGRESS.md` مدخل
  ۱.۹.۲۲ نداشت که نوشته شد.

## نسخهٔ ۱.۹.۲۲ — چه عوض شد

- اتصال: اگر پینگ اول شکست بخورد سوکت باز هم تلاش می‌کند
  (`ensure_streaming`). تیک سوکت `_mark_rest_alive(True)` می‌زند.
  نگهبان هر ۱۵ ثانیه `keepalive` را صدا می‌زند. سقف نماد سوکت ۸۰ است.
  کف نظرسنجی خوراک ۰٫۵ ثانیه و پیش‌فرض ۲ ثانیه است.
- قیمت صفحه: تیک قیمت ۱ ثانیه، پایش معامله ۲ ثانیه، کندل ۸ ثانیه،
  تازه‌سازی جدول ۰٫۵ ثانیه. قیمت از کش می‌آید و `set_live_price` صدا
  زده می‌شود. `set_chart_data` هر ثانیه صدا زده نمی‌شود.
- معاملهٔ سریع: دکمهٔ «تنظیم خودکار سریع» مارجین ۱۰، اهرم ۲۰۰، هدف و
  حد ضرر ۳ یا ۴ یا ۵، همزمانی ۱، پایش ۰٫۴، نگهداری ۹۰ و کارمزد ۰٫۰۰۰۶
  را ذخیره می‌کند. حالت و عبارت تأیید را پاک نمی‌کند. ذخیرهٔ تنظیم،
  موتور در حال اجرا را با `apply_config` به‌روز می‌کند.
- کارمزد: ورود روی رکورد ذخیره می‌شود. خروج فقط به `close_trade` داده
  می‌شود. سود خالص = ناخالص − هر دو طرف. برای ۱۰×۲۰۰، کارمزد
  رفت‌وبرگشت ۲٫۴۰ دلار است؛ هدف خالص ۳ دلار یعنی ناخالص ۵٫۴۰ دلار.
- کاربر موتور خودکار `self.app.auth.user_id` است، نه `_current_user`.
- درگاه خالی به موتور وصل است تا حالت زنده داخل `open_position` شکست
  بخورد، نه اینکه بی‌صدا کاغذی بماند در حالی که کاربر فکر می‌کند واقعی است.
- پروفایل سرعت در زبانهٔ کارایی تنظیمات است. متعادل عددهای ذخیره‌شده را
  نگه می‌دارد. سریع یک گام و یک ابزار چت است.
- دفترچهٔ نتیجه تیک ساعتی دارد. پیش‌فرض خاموش است
  (`signals.scorecard_auto`).

## خلاصهٔ نسخه‌های اخیر، از روی آزمون‌ها

این شرح جایگزین شرح تفصیلی `BUILD_INFO.txt` برای ۱.۹.۱۷ تا ۱.۹.۲۱ نیست
و آن بخش‌ها عمداً اختراع نشده‌اند. فقط همان چیزی است که docstring
آزمون‌ها می‌گویند:

- ۱.۹.۱۴: پویشگر اسکالپ، فیلتر نقدینگی سخت، موتور خودکار کاغذی با سقف اهرم.
- ۱.۹.۱۷: پیش‌بینی بازه‌ای افق ۱۵ دقیقه / ۱ ساعت / ۴ ساعت، نه عدد قطعی.
  بازهٔ دورتر باید پهن‌تر باشد.
- ۱.۹.۱۸: هشدار قیمت و سیگنال؛ نمودار زنده؛ پنل معاملهٔ خودکار.
- ۱.۹.۱۹: دفترچهٔ نتیجهٔ پیش‌بینی دیگر کد مرده نیست.
- ۱.۹.۲۰: سرویس «آشنا» در تنظیمات دیده می‌شود و مسیر کلید تا انبار رمز
  وصل است.
- ۱.۹.۲۱: آفلاین فقط بعد از چند شکست پیاپی REST؛ تنظیم دستی معاملهٔ
  خودکار؛ ورود بر پایهٔ اطمینان.

## عددهایی که نباید دوباره خراب شوند

- `validated()` حداقل پایش را ۰٫۲۵ ثانیه می‌گذارد، نه ۱٫۰. اگر ورودی
  پایش به رابط اضافه شود باید داخل جعبهٔ جمع‌شونده باشد و حداقلش ۰٫۲۵.
- آزمون چرخهٔ معامله قیمت ۱۰۲٫۵ و ۹۶ را روی هدف ۲ و حد ۳ با اهرم ۱۰
  هنوز می‌بندد. کارمزد پیش‌فرض این فاصله را آن‌قدر جابه‌جا نمی‌کند که
  آن قیمت‌ها از هدف بگذرند.
- آزمون اهرم ۱۲۵ دیگر سقف نیست. ورودی آزمون ۵۰۰ است تا سقف ۲۰۰ هنوز
  اهرم را ببرد.
- `trading/__init__.py` نباید `HARD_MAX_LEVERAGE` را وارد کند، مگر
  اینکه همهٔ واردکننده‌های بسته بررسی شوند.
- `.gitattributes` نباید حذف شود و قاعدهٔ `*.bat text eol=crlf` باید
  بماند. بررسی: `git ls-files --eol '*.bat'` باید `w/crlf` بدهد. بدون
  آن، پنج آزمون می‌شکنند و سازندهٔ ویندوز بی‌صدا می‌افتد.
- هر فیکسچری که `MainWindow` می‌سازد باید در پایان
  `destroy_window(window, app)` را صدا بزند، نه `deleteLater()` تنها.
- فیکِ تنظیمات در آزمون‌ها باید `get` داشته باشد، نه فقط `get_int`.
- نسخه در شش جا نوشته می‌شود: `app/core/constants.py:13`،
  `pyproject.toml:7`، `BUILD_INFO.txt` خط ۴ و ۶، `README.md` خط ۷
  (با جداکنندهٔ فارسی `٫`) و `README_EN.md` خط ۵.

## کار باز که بدون کاربر تمام نمی‌شود

1. `doctor_report.txt` نرسیده است. **از ۱.۹.۲۳ یک علت اثبات‌شدنی در
   دست است:** فایل‌های `.bat` پایان خط LF داشتند و عیب‌یاب خودِ برنامه
   اسکریپت‌ها را «ناموفق» می‌زد. این درست شده است. اگر سازنده هنوز
   شکست می‌خورد، آن‌وقت گزارش تازه لازم است — و حدس زدن دربارهٔ
   سازنده بدون آن گزارش همچنان ممنوع است (سه بار حدس زده شد و هر سه
   بار غلط بود).
2. اسکرین‌شات بخش تولید سیگنال نرسیده است.
3. اجراکنندهٔ سفارش LBank و Toobit هنوز سند تأییدشده ندارد. تا رسیدن
   سند، payload نساز. اگر روزی ساخته شد و اندازهٔ قرارداد نبود، حجم را
   حدس نزن و سفارش را رد کن.

## پیشنهادهای بعدی

فهرست جداست: `ADVANCED_FEATURES_FA.md`.
