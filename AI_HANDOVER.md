# تحویل پروژه به هوش مصنوعی بعدی

این فایل منبع ادامهٔ کار است. با هر نسخه باید همین فایل به‌روز شود.
تاریخ این نگارش: ۲۰۲۶-۰۹-۲۵، نسخهٔ برنامه `2.5.4`.

## وضعیت جاری ۲.۵.۴ — بسته‌شدن خودکار EXE، معاملهٔ خودکاری که باز نمی‌شد، اسکالپ فوق‌سریع

گزارش کاربر: (۱) EXE پس از چند دقیقه/ساعت خودش بسته می‌شود؛ (۲) معاملهٔ خودکار هیچ معامله‌ای باز نمی‌کند؛
(۳) «اسکالپ فوق‌سریع» با اهرم بالا، بستن در سود خالص کاربر (مثلاً ۲–۳ دلار پس از کارمزد) و ~۱۰۰ معاملهٔ هم‌زمان
با ۱۰۰۰ دلار. پاسخ‌های کاربر: فعلاً کاغذی با قیمت زنده؛ سقف هم‌زمانی تا ۲۰۰ قابل تنظیم.
- بسته‌شدن: هر `TaskHandle` فرزند Qt ‏`AsyncRunner` بود و هرگز پاک نمی‌شد (چند کار در ثانیه ← ده‌ها هزار QObject).
  حالا `_on_handle_finished` ← صف `_graveyard` ← `purge_finished` (نابودی هم‌گام با `shiboken6.delete` پس از
  `FINISHED_HANDLE_GRACE_SECONDS=2`؛ `stop()` همه را پس از join نابود می‌کند). `deleteLater` مستقیم آزمون‌ها را
  با segfault می‌کشت (GC چرخه‌ای runner/نابودی هم‌زمان با emit) — تکرار نشود. کار زمان‌بندی‌نشده (runner متوقف)
  دستهٔ بی‌والد می‌گیرد. `app/diagnostics.py`: `data/logs/app.log` (چرخشی ۵MB)، `crash.log` (faulthandler +
  excepthook نخ‌ها)، `session.json` (`SessionMonitor`: ضربان ۶۰ث، `clean`؛ نشست ناتمام قبلی ← اعلان
  `common.unclean_exit`)، پیام‌های Qt در لاگ، `MainController.health_counters/log_health`. نوشتن DB پایش معامله
  محدود شد (`PERSIST_INTERVAL_SECONDS=2`، تغییر حد ضرر فوری). رویدادهای موتور: اعلان‌ها محدود (۲٫۵ث) و تازه‌سازی
  جدول‌ها یکجا (۴۰۰ms: `_schedule_auto_refresh`/`_flush_auto_refresh`).
- باز نشدن: حد ضرر دورِ سیگنال در اهرم بالا همیشه `risk_exceeds_loss_budget` می‌داد. حالا `_open_trade_locked` هدف
  و حد ضرر را به بودجهٔ دلاری کاربر محدود می‌کند (`stop_source`/`target_source` در extra رکورد). `scan_stats()`
  (نامزد/بازشده/دلیل‌های رد) در پنل با `trades.auto.scan_summary` و `trades.auto.reject.*` هر ۳ ثانیه دیده می‌شود.
- فوق‌سریع: `trading/ultra_scalp.py` (`momentum`، `UltraScalpSource` از کش تیک کل بازار + فیلتر نقدینگی)، حالت موتور
  `ultra` (`ULTRA_MIN_SCAN_INTERVAL=1`، `ULTRA_MIN_HOLD_SECONDS=10`، بدون ابطال سیگنال)، `HARD_MAX_CONCURRENT=200`،
  دکمهٔ «⚡ اسکالپ فوق‌سریع» (`TradesPage.ULTRA_PRESET`: ۱۰$×۵۰، هدف ۲$، ضرر ۲$، ۱۰۰ هم‌زمان، ۱۸۰ث، پویش ۱ث)، فیلد
  «بیشترین زمان باز ماندن»، تنظیم‌های `scalp.ultra_*`. کش روزانهٔ سود/زیان ۱ث و یک عکس پرتفوی برای هر ورود.
  حالت سفارش (کاغذی/واقعی) با پیش‌تنظیم عوض نمی‌شود؛ درگاه واقعی همچنان کاغذی است. سوکت حداکثر ۸۰ نماد
  (`MAX_STREAMED_SYMBOLS`)، بقیه با REST هر ۳ ثانیه.
آزمون: `tests/test_v254_ultra_and_stability.py`؛ کل مجموعه 2564 پاس + 2 skip. شبیه‌سازی ۳۰۰ نماد: ۱۰۰ معامله در
کمتر از ۸ ثانیه، بستن در سود خالص ≈ ۲ دلار (دادهٔ مصنوعی؛ نشانهٔ سودآوری نیست). گزارش: `docs/RELEASE_2.5.4_FA.md`.
**commit نشده** (آخرین commit: `511d2b5` = 2.5.1+2.5.2؛ 2.5.3 هم commit نشده است).

## وضعیت قبلی ۲.۵.۳ — ربات‌های ساخت فایل نصبی ویندوز و APK

گزارش کاربر: ساخت ویندوز «پنجره‌های زیادی از بخش‌های مختلف نرم‌افزار باز می‌کند و فایل نصبی نمی‌سازد»؛ APK با
«APK ساخته نشد» (RESULT=1) تمام می‌شود. علت ویندوز: `tools/build_installer.py` کل آزمون‌ها را روی دسکتاپ واقعی
(بدون offscreen) اجرا می‌کرد و هر شکست PyInstaller را متوقف می‌کرد؛ BOM در ابتدای bat جلوی `@echo off` را می‌گرفت؛
`.iss` بدون BOM بود. رفع: `tools/build_common.py` (`run_streaming` خروجی زنده + `build_logs/*.log`، `headless_env`)،
گام «بررسی سلامت کد» (compileall + import بی‌پنجرهٔ `SMOKE_CODE`)، آزمون کامل فقط با `--with-tests`/`RUN_TESTS=1`
(بی‌پنجره)، `upx=False`، `tests/conftest.py` پیش‌فرض offscreen، `scripts/build_windows.bat` ← `build_installer.bat`.
علت APK: `mobile/assets/` (آیکون/پیش‌نمایش/قلم) نبود، پذیرش مجوز SDK، `python-bidi` بدون پین (Rust)، آزمون ریاضی
با پایتون بدون numpy/pandas، ساخت روی `/mnt/c`، `pip --user` مسدود در اوبونتو ۲۴. رفع: `mobile/assets/*`،
`android.accept_sdk_license = True`، `python-bidi==0.4.2`، `warn_on_root = 0`، `check_toolchain`/`probe_script`
(buildozer در `~/.cai-buildozer`، فرمان apt دقیق)، `build_script` (کپی به `~/cryptoaitrader-apk`، ساخت روی
فایل‌سیستم لینوکس، برگرداندن APK به `mobile/bin`)، ردکردن آزمون ریاضی در نبود بسته‌ها. موبایل 1.9.13.
آزمون: `tests/test_v253_build_tools.py`. ساخت واقعی ویندوز/APK در sandbox ممکن نیست. گزارش: `docs/RELEASE_2.5.3_FA.md`.
**commit نشده** (آخرین commit: `511d2b5` = 2.5.1+2.5.2).

## وضعیت قبلی ۲.۵.۲ — فیوچرز LBank: HTTP 403 از Cloudflare

گزارش کاربر پس از 2.5.1: اسپات ✓ (۳ دارایی)، فیوچرز ✗ `NetworkError: HTTP 403 … /cfd/openApi/v1/prv/account`.
`lbkperp.lbank.com` پشت Cloudflare است (`api.lbkex.com` نه). رفع: `BROWSER_HEADERS` در `_contract_client`،
`LBankRestClient._classify_forbidden` + `_cloudflare_code`/`CLOUDFLARE_REASONS` ← `AccessBlockedError`
(`app/exceptions/errors.py`, زیرکلاس `NetworkError`) در `no_retry_on`، شکستن حلقهٔ دارایی‌های فیوچرز پس از
مسدودی، `wallet.hint.blocked/blocked_region` در `_wallet_error_hint`. کد Cloudflare واقعی کاربر هنوز نامعلوم؛
گزارش بعدی کیف پول آن را نشان می‌دهد. گزارش: `docs/RELEASE_2.5.2_FA.md`. commit شد در `511d2b5`.

## وضعیت قبلی ۲.۵.۱ — نمایش دارایی کیف پول، واچ‌لیست، ترتیب و قیمت بازارها

خواسته‌های کاربر: کیف پول هیچ دارایی (کل/اسپات/فیوچرز) نشان نمی‌داد؛ ساخت واچ‌لیست با کلیک راست روی ردیف/نماد
بازار؛ «افزودن به واچ‌لیست» مودال کار نمی‌کرد؛ ترتیب نمادها بر پایهٔ ارزش و حجم (BTC، ETH بالا)؛ قیمت دقیق تتری.
پیاده‌سازی: `LBankRestClient.signed_headers()` (امضا در سرآیند + بدنه)، `LBankEndpoints.USER_INFO_ACCOUNT/
USER_INFO_LEGACY`، `LBankProvider._spot_rows`/`last_sync_report`/زنجیرهٔ مسیرهای اسپات، `ExchangeAccountService`
(شکست اسپات با فیوچرز سالم غیرکشنده، `details["report"]`)، `WalletPage` (`status_panel`/`set_sync_report`،
`auto_sync_checkbox`، `spot_search`/`hide_small_checkbox`)، `MainController.start_wallet_auto_sync`/`sync_wallet(silent=)`/
`_fill_wallet_report`/`_wallet_error_hint`؛ `SymbolRepository.add_to_watchlist(symbol, exchange=None)` رکورد را می‌سازد،
`is_in_watchlist`، `MainController.set_watchlist_membership`/`_sync_symbol_table`؛ `market/market_rank.py`
(`sort_by_market_value`, `price_decimals`, `quote_usdt_prices`)، `MarketsPage.build_context_menu` و حالت
`market_cap`. آزمون: `tests/test_v251_wallet_watchlist_markets.py`. گزارش: `docs/RELEASE_2.5.1_FA.md`.
**این تغییرها commit نشده‌اند** (دستور صریح کاربر)؛ آخرین commit: `982cce2` (2.5.0).

## وضعیت قبلی ۲.۵.۰ — معامله از سیگنال، کیف پول سه‌زبانه، موجودی کاغذی

خواسته‌های کاربر: اشتراک سیگنال در شبکه‌های اجتماعی؛ جست‌وجوی بالا کشیده و گرد؛ «اقدام به معامله» با
ورود/SL/TP1–3 و بستن با اهداف (پاسخ کاربر: پلکانی ⅓/⅓/باقی، SL به ورود پس از TP1)، بستن مودال و رفتن به جدول
باز؛ بازسازی جدول تاریخچه و عنوان‌ها؛ ماشین‌حساب پرشده از سیگنال؛ بی‌درنگ؛ کیف پول سه‌زبانه (نمای کلی/اسپات/
فیوچرز) با اعداد دقیق فیوچرز؛ موجودی کاغذی جعلی برابر کیف پول واقعی. پیاده‌سازی: `trading/staged_targets.py`
(`next_step`)، `trading/trade_monitor.evaluate_staged`، `PaperTradeRepository.partial_close/realized_pnl_since`،
`trading/paper_account.py`، `MainController._on_trade_requested(signal, dialog=)`/`_apply_trade_step`/
`_paper_account`/`sync_paper_balance_with_wallet`/`_fill_wallet_tabs`، `WalletPage` (سه زبانه، `MetricStrip`،
`paper_sync_requested`)، `TradesPage.HISTORY_COLUMNS` (۱۶ ستون، عنوان هنگام ساخت، به‌روزرسانی درجا)،
`PositionCalculator` (TP2/TP3، `signal_entry`, `trade_values`)، `SignalDetailDialog` (`share_to`, `trade_payload`,
`trade_opened`)، `LBankRestClient._handle_response(contract=True)` (پاسخ `success:true`/`result:""` قبلاً «LBank
error 0» می‌شد — علت نمایش‌ندادن فیوچرز)، `LBankProvider._parse_futures_details`، `wallet_details` در
`extra_config`. گزارش: `docs/RELEASE_2.5.0_FA.md`. commit/push فقط با دستور صریح.

## وضعیت قبلی ۲.۴.۲ — سبکی رابط و کپی سیگنال

گزارش کاربر پس از 2.4.1: «هنوز خیلی سنگین است و هنگ می‌کند» + «امکان کپی نام و اطلاعات
سیگنال در جدول یا مودال». علت‌های یافته: (۱) کاروان GIL — محاسبهٔ pandas پویش روی نخ
پس‌زمینه نخ رابط را پس از هر فراخوانی Qt معطل می‌کرد (پروفایل: ~۹۵٪ زمان هر نماد در
`compute_timeframe_analysis`)؛ (۲) تایمر ۳۰ ثانیه‌ای پیگیری نتیجه روی نخ رابط
`outcome_repository.performance()` (تا ۵۰۰۰ ردیف) را اجرا می‌کرد، حتی وقتی صفحه پنهان بود؛
(۳) پویش‌های پیاپی همان سیگنال را بی‌وقفه ذخیره و پیگیری می‌کردند (رشد پایگاه داده)؛
(۴) ترمینال معاملات هر ثانیه حتی پنهان رسم می‌شد. اصلاح: `signals/compute_pool.py`
(`ComputePool` با spawn، ≤۲ کارگر، اولویت پایین، fallback محلی، بستن در بیکاری) فقط در
`is_bulk_fetch()`؛ `SignalEngine.set_compute_pool`؛ `Application.compute_pool()`
(`performance.process_pool`، متغیر `CRYPTOAI_NO_PROCESS_POOL` که conftest روشن می‌کند)؛
`Application._should_store_scanned` (۳۰ دقیقه/۵ واحد)؛ `_refresh_outcome_view` تنبل با
`run_blocking` و `ShowWatcher`؛ `_terminal_timer_tick`؛ `UiStallWatchdog`؛
`multiprocessing.freeze_support()` در main.py. کپی: `ui/signal_share.py`، دکمه‌های
`SignalDetailDialog.copy_symbol/copy_info`، منوی راست‌کلیک و Ctrl+C در `SignalsPage`
(`copy_notice` → toast). آزمون: `tests/test_v242_lightweight_and_copy.py`. گزارش:
`docs/RELEASE_2.4.2_FA.md`. 2.4.0، 2.4.1 و 2.4.2 هنوز commit نشده‌اند.

## تحویل قبلی ۲.۴.۱ — سبک‌سازی پویش کل بازار

گزارش کاربر پس از 2.4.0: «بخش سیگنال خوب شد ولی سیستم خیلی سنگین است، هنگ می‌کند
و کل سیستم را درگیر می‌کند.» علت‌ها: (۱) هر کندلِ پویش (~۴۰۰۰ درخواست برای کل صرافی)
با ORM در SQLite نوشته می‌شد (دیسک و CPU)؛ (۲) کندل‌های پویش حافظهٔ نهان ۵۰۰تایی را پر
و تیکر/قیمت داغ را بیرون می‌انداختند؛ (۳) محاسبهٔ هم‌گام اندیکاتورها روی تنها حلقهٔ
asyncio تا ۴۰۰ms قفل می‌ساخت و با GIL رابط را کند می‌کرد؛ (۴) هر اندیکاتور DataFrame را
از نو می‌ساخت (~۳۵٪ CPU)؛ (۵) جدول پویش با صدها ردیف و ستون «به اندازهٔ محتوا» هر ۸۰۰ms
از نو رسم می‌شد. اصلاح: `market.engine.bulk_fetch()` (ContextVar)، `signals.scanner.CpuGovernor`
(دستی ۰٫۶، خودکار ۰٫۳۵)، انتظار در مکث نرخ، دروازهٔ محاسبه در `SignalEngine`،
`IndicatorEngine._calculate(frame_holder)`، numpy در `BaseIndicator.calculate`، سقف ۲۰۰ ردیف
و تازه‌سازی تطبیقی. آزمون: `tests/test_v241_performance.py`. گزارش: `docs/RELEASE_2.4.1_FA.md`.
2.4.0 و 2.4.1 هنوز commit نشده‌اند.

## تحویل قبلی ۲.۴.۰ — پویش کل بازار و منبع سیگنال‌گیری خودکار

درخواست کاربر: پویش سیگنال‌ها فقط روی تعداد محدودی نماد (پیش‌فرض ۶۰) اجرا می‌شد؛
تعداد دستی بماند ولی گزینه‌ای برای پویش همهٔ نمادهای صرافی باشد. سیگنال‌گیری
خودکار باید «چرخش کامل» را روی همهٔ نمادها انجام دهد و منبعش بین سیگنال‌های
پیداشده و کل بازار قابل‌انتخاب باشد. پیاده‌سازی: `signals/scan_universe.py` (جهان
پویش، پالایش هوشمند، اولویت نقدشوندگی/نوسان)، `MarketScanner.scan(universe, filters,
on_signal)`، `Application.scan_market(universe, min_turnover, smart_filter, on_signal)`،
`AutoScanConfig.source/universe/min_turnover/smart_filter` + `seed_focus`، کنترل‌های
تازه در `ui/pages/signals_page.py` و نتیجهٔ زنده/پیشرفت در `MainController`. کلیدهای
تازه: `signals.auto_scan_{source,universe,min_turnover,smart_filter}` و
`signals.scan_{universe,limit,min_turnover,smart_filter}`. آزمون:
`tests/test_v240_scan_universe.py`. گزارش: `docs/RELEASE_2.4.0_FA.md`. 2.3.2 با دستور
کاربر commit/push شد (`80ce0ab`)؛ 2.4.0 هنوز commit نشده است.

## تحویل تاریخی ۲.۳.۲ — اتصال LBank

گزارش کاربر: روی LBank حدود ۲۰ ثانیه آنلاین و بعد آفلاین؛ وب‌سوکت LBank هرگز وصل
نمی‌شد (صرافی‌های دیگر سالم). علت‌ها: جدول خطای LBank جابه‌جا بود و 10004
(«Request too frequent») خطای احراز هویت شمرده می‌شد → REST «مرده» و بدون مکث؛
provider.ping هم خطا را می‌بلعید؛ وب‌سوکت فقط `www.lbkex.net` (نشانی قدیمی) را
می‌شناخت. اصلاح: `market/providers/lbank/{constants,rest_client,provider,websocket_client}.py`،
`retry_async(no_retry_on=...)`، آمار عیب‌یابی در `RedundantStream.stats()`؛ تست
`tests/test_v232_lbank.py`. گزارش: `docs/RELEASE_2.3.2_FA.md`. stubهای Qt اکنون در
`.cache/qtstub` داخل مخزن (نادیده در snapshot) ساخته می‌شوند. با دستور کاربر commit/push شد (`80ce0ab`).

## تحویل تاریخی ۲.۳.۱ — اتصال پایدار و داشبورد

رگرسیون 2.3.0 (خواندن بدون کش همهٔ تیکرها هر ۲ ثانیه و کش مشترک ۰٫۱ ثانیه‌ای)
باعث محدودیت نرخ صرافی و «چند ثانیه آنلاین، بعد قطع» می‌شد. اصلاح: کش مشترک،
مکث سراسری 429/418، آنلاین از REST یا WS، snapshot هر ۳۰ ثانیه. داشبورد مرکز
فرمان گرفت (`ui/widgets/dashboard_widgets.py`، `summarise_market` در کنترلر).
گزارش: `docs/RELEASE_2.3.1_FA.md`. Qt در محیط لینوکس بدون libGL با stubهای
ساخته‌شده در `/home/user/qtstub` (خارج مخزن) اجرا شد. هیچ commit/push انجام نشده.

## تحویل تاریخی ۲.۳.۰ — معامله، اتصال افزونه و تاریخچه

به درخواست کاربر، ورود/خروج هم‌زمان و پایش اسکن از هم جدا شدند؛ محاسبات
خالص کارمزد و سر‌به‌سر، محافظ هزینه/ریسک و سقف زیان روزانهٔ پایدار اصلاح شد.
دو client مستقل از همان صرافی، watchdog، REST تازه و حفظ اشتراک نمودار اضافه شد.
ورود دستی از فرصت‌ها پس از موفقیت به تاریخچهٔ Open می‌رود؛ جدول کامل‌تر،
مودال جزئیات و خروج مشترک دارد. انتخاب قبل از تحلیل و چرخش بازار/علاقه‌مندی‌ها
اصلاح شد. هیچ سود، زیان صفر یا اتصال همیشگی تضمین نمی‌شود.

گزارش دقیق: `docs/RELEASE_2.3.0_FA.md`. آزمون انتخابی **۷۵۸ پاس + ۴ skip**؛
۵۰ رگرسیون تازه. ۳۳ آزمون قدیمی غیرنمایشی جداگانه نیز پاس شدند. full suite
هنوز ۳۰ خطای collection کتابخانهٔ Qt دارد؛ native GUI/ویندوز تأیید نشده است.
بسته: `CryptoAITrader-v2.3.0-2026-09-23.zip`؛ درگاه `tools/serve_downloads.py`.
هیچ commit/push/Release انجام نشده؛ پس از تست ZIP منتظر دستور صریح کاربر بمان.
R6 (feed/tick/trading) و R10 (دامنهٔ چرخش) فقط بخشی رفع شدند؛ agentها و
استقلال آستانهٔ watch هنوز بررسی می‌خواهند. R4/R5/R7/R8/R9 دست‌نخورده‌اند.
بخش‌های نسخه‌های قدیمی پایین، **تاریخی** هستند.

## تحویل تاریخی ۲.۲٫۲ — رفع سه ایراد و درگاه دانلود

به درخواست کاربر، R1 (کش پیش‌بینی)، R2 (انتقال کامل تنظیم‌های معامله و
سن مجاز دادهٔ کش متصل) و R3 (برچسب درست حساسیت backup بدون حذف ciphertext)
اصلاح شدند. جزئیات: `docs/RELEASE_2.2.2_FA.md`.
آزمون ترکیبی هسته/درگاه: ۷۰۸ پاس + ۳ skip؛ تست کامل Qt/ویندوز تأیید نشده است.
صفحهٔ دانلود: `tools/serve_downloads.py` با template همراه، مستقل از دسکتاپ.
ZIP جدید: `CryptoAITrader-v2.2.2-2026-09-23.zip`؛ هیچ commit/push انجام نشده.
R4 تا R10 بررسی اولیه هنوز باز هستند؛ آن‌ها را رفع‌شده معرفی نکن.

## تحویل ۲.۲٫۱ — بررسی اولیهٔ این جلسه (تاریخی)

ابتدا `AGENTS.md`، `docs/PROJECT_MEMORY_FA.md` و `docs/REVIEW_VALIDATION_FA.md`
خوانده شوند. نمایهٔ همهٔ فایل‌ها/نمادها در `docs/CODE_MAP.md` است.
این مرحله منطق محصول را تغییر نداده؛ تنها مستندات و شمارهٔ نسخه عوض شده‌اند.
نسخهٔ اصلی 2.2.0 از کامیت `a66e61a` جداگانه ZIP شد. شاخهٔ این جلسه
`arena/01a0cf53-cryptoaitrader` است؛ هیچ commit/push انجام نشده است.

آزمون این محیط: ۶۷۴ پاس + ۲ skip در ۳۳ فایل مستقل از UI؛ جمع‌آوری کامل
به علت نبود libGL/کتابخانه‌های Qt مسدود شد. شمار تست‌های قبلی در ادامه
تاریخی است. سه ایرادِ بازتولیدشده و هنوز اصلاح‌نشده: cache پیش‌بینی با
تایم‌فریم متفاوت، انتقال ناقص تنظیم‌های جدید معامله به config موتور،
و وجود ciphertext رازها در backup با manifest نادرست.

مستندات قدیمی دربارهٔ «AI فقط روایت»، «راز هرگز در SQLite» و مسیر داده
دقیق نیستند؛ برای توسعه به نقشهٔ جدید و خود کد رجوع شود. افزایش نسخه و
تحویل ZIP پایان هر مرحله الزامی است؛ کاربر پس از تست جداگانه اجازهٔ
commit/push می‌دهد.

## قانون کار با کاربر

- پاسخ به کاربر فارسی است.
- تا وقتی کاربر نسخه را تست نکرده و صریح نگفته «مورد تأیید است»،
  کامیت و پوش به گیت‌هاب ممنوع است.
- بعد از هر تغییر قابل تست، زیپ تازه ساخته شود و روی همان صفحهٔ دانلود
  گذاشته شود. نشان‌دادن زیپ در پنل Diff دانلود محسوب نمی‌شود.
- تنظیم ذخیره‌شدهٔ کاربر بازنویسی نمی‌شود. کلید تازه می‌تواند پیش‌فرض
  داشته باشد؛ کلید موجود در پایگاه داده را مهاجرت نکن.
- سفارش واقعی تا تأیید صریح کاربر کاغذی می‌ماند. payload سفارش LBank
  یا Toobit را حدس نزن. اگر اجراکنندهٔ تأییدشده نباشد، درگاه باید
  استثنا بدهد، نه اینکه وانمود کند سفارش رفته است.
- صفحهٔ معاملات نباید اسکرول کل صفحه بگیرد. ارتفاع حداقل باید از
  ۶۶۰ پیکسل لپ‌تاپ کمتر بماند. کنترل تازه را در ردیف افقی موجود یا داخل
  جعبهٔ جمع‌شونده بگذار. زبانهٔ تنظیمات کارایی و زبانه‌های گزارش باید
  `QScrollArea` باشند.
- آزمون تازه اگر به رابط وابسته نیست، PySide6 را وارد نکند. در این
  محیط `python3` سیستمی PySide6 ندارد.

## برنامه چیست

برنامهٔ دسکتاپ تحلیل رمزارز و سیگنال فیوچرز است. مالک مخزن:
`hhajtalebi/CryptoAITrader`. رابط PySide6، داده از REST و WebSocket،
پایگاه SQLite، هوش مصنوعی اختیاری (Ollama یا سرویس سازگار با OpenAI،
از جمله آشنا).

پیش‌فرض معامله کاغذی است. قیمت زنده است. سفارش واقعی فقط وقتی هر سه
شرط برقرار باشد می‌رود، و حتی آن‌وقت هم تا وصل شدن اجراکنندهٔ تأییدشده
بلند شکست می‌خورد:

1. معاملهٔ خودکار روشن باشد.
2. `scalp.mode` برابر `live` باشد.
3. عبارت تأیید دقیقاً `معامله واقعی را می‌پذیرم` باشد.

## اجرا

در ویندوز: `RUN_WINDOWS.bat`. پایتون ۳٫۱۱ یا بالاتر. وابستگی‌ها در
`pyproject.toml`. نسخه در `app/core/constants.py` با نام `APP_VERSION`.

## نقشهٔ کوتاه

| مسیر | نقش |
|---|---|
| `app/application.py` | ساخت سرویس‌ها، عامل هوش مصنوعی، مهلت سیگنال |
| `app/config/defaults.py` | کلیدها و پیش‌فرض‌ها. نبود کلید در پایگاه خطا نیست |
| `market/engine.py` | REST، سوکت، کش، `peek_candles`، `keepalive` |
| `market/live_feed.py` | قیمت زنده. `is_fresh` اگر تیک تازه باشد حتی با شکست REST |
| `ui/controllers/main_controller.py` | تنها جایی که صفحه را به موتور وصل می‌کند |
| `ui/pages/trades_page.py` | پنل خودکار و دستی. جعبهٔ دستی پیش‌فرض جمع است |
| `ui/pages/analysis_page.py` | `set_live_price` بدون redraw کامل؛ `set_chart_data` کل کندل را می‌کشد |
| `trading/auto_trader.py` | باز، پایش، بستن. سقف اهرم `HARD_MAX_LEVERAGE = 200` |
| `trading/micro_plan.py` | سود خالص بعد از کارمزد |
| `trading/execution.py` | `build_gateway`؛ اجراکنندهٔ پیش‌فرض `None` |
| `trading/scalp_service.py` | ساخت `AutoTradeConfig` از تنظیم‌ها |
| `ai/speed_profile.py` | سریع / متعادل / عمیق |
| `signals/prediction/engine.py` | موتور هوش پیش‌بینی: `assess()` همه‌چیز را می‌سازد و در DB ثبت می‌کند |
| `app/database/repositories/prediction_repository.py` | ثبت/حل/امتیاز پیش‌بینی‌ها؛ `due_for_resolution` مقایسه را با int انجام می‌دهد نه رشته |
| `app/database/repositories/trade_repository.py` | `open_trade`/`close_trade` هر دو `fee` می‌گیرند |

## نسخهٔ ۲.۲٫۰ — چه عوض شد

**فهرست مشکلات کاربر روی صفحهٔ معاملهٔ خودکار — همه رفع شد.**

1. کارت‌های سرمایه: آیکون + رنگ سود/زیان.
2. نمودار: اندیکاتور (EMA/SMA/BB از کتابخانهٔ واحد)، خط‌کشی
   (افقی/روند/پاک‌کردن)، منوی نماد آیکون‌دار (دیلیگیت)،
   عکس‌لحظه‌ای PNG، نوع نمودار، بازنشانی زوم، بازخوانی کندل ۵
   ثانیه.
3. پنل تصمیم: دکمهٔ تحلیل عامل هوش مصنوعی روی نماد/تایم‌فریمِ
   نمودار (`on_ai_opinion_requested` → `set_ai_opinion`).
4. جدول فرصت‌ها: پویش پس‌زمینهٔ همیشگی (`_run_watch_scan`، کلیدهای
   `scalp.watch_*`) + تنظیمات مودال (ScannerSettingsDialog) — حتی
   با موتور خاموش جدول زنده است؛ ورود همچنان دروازه‌های موتور.
5. موقعیت‌ها: ستون ۱۹ اکشن با دکمهٔ بستن (مرتب‌سازی این جدول
   خاموش — ویجت سلولی و مرتب‌سازی سازگار نیستند)؛ کلیک نماد →
   مودال PositionDetailDialog با دکمهٔ بستن.
6. نمادهای مورد علاقه: مودال تیک‌خور با جست‌وجو و آیکون؛ ذخیره در
   تنظیمات + همگام‌سازی با watchlist دیتابیس (`on_selected_symbols_saved`).

**نکته برای ادامه:** `_watch_rows_from_candidates` «قابل ورود» را
از آستانه‌های `scalp.watch_min_confidence`/`watch_max_spread`
می‌سازد — ورود واقعی جدا از آن، دروازه‌های کامل موتور را می‌گذرد؛
این دو عمداً جدایند.

## نسخهٔ ۲.۱٫۱ — چه عوض شد

**بازخورد کاربر: «شبیه تصویر نیست، نمودار کار نمی‌کند، نمادی
قابل انتخاب نیست».** سه تغییر:

1. **چیدمان ترمینالی**: حذف `addStretch` انتهایی (فضای مرده)؛
   نمودار غالب ۵:۲ با ستون راست (پیش‌بینی+ریسک)؛ زبانه‌های پایین
   «فرصت‌ها (n) | موقعیت‌ها (n)» به سبک صرافی‌ها با شمار زنده؛
   هدر یک‌ردیفی؛ تنظیمات پیش‌فرض جمع.
2. **کمبوی نماد خالی**: `set_auto_symbols` حالا در
   `_on_symbols_loaded` کنار تحلیل/سیگنال صدا زده می‌شود و نخستین
   نماد **با سیگنال** انتخاب می‌شود (تلهٔ Qt: `addItem` روی کمبوی
   خالی ایندکس را ۰ می‌کند و `setCurrentIndex(0)` بعدی سیگنال
   نمی‌دهد — اول ‎-1‎ در حالت بی‌صدا).
3. **بوت‌استرپ فقط تیکِ اول بود**: اگر بازار دیر لود می‌شد، نمودار
   برای همیشه خالی می‌ماند — حالا هر تیک تا نخستین موفقیت.

نکته برای ادامه: برچسب نماد نمودار در سربرگ پنل پیش‌بینی است تا
پیوند «نمودار ↔ پیش‌بینی» همیشه دیده شود؛ خواستهٔ کاربر این بود
که پنل پیش‌بینی از همین نمودار تغذیه شود (و می‌شود — همان نماد،
همان موتور واحد).

## نسخهٔ ۲.۱٫۰ — چه عوض شد

**بازطراحی صفحهٔ «معاملهٔ خودکار» به ترمینال حرفه‌ای.** مرجع طراحی،
تصویر کاربر بود (فقط چیدمان/تراکم/UX — هیچ عددی از تصویر وارد برنامه
نشد؛ همه از موتورهای واقعی). محدوده: فقط همین صفحه؛ بقیهٔ صفحات و
کنترلر فقط در حد سیم‌کشی دست خوردند.

- **هدر ترمینال**: حالت موتور، Paper/Live، وب‌سوکت، آخرین تیک با
  میلی‌ثانیه، تأخیر، شروع/توقف، دکمهٔ تنظیمات (پنل جمع‌شونده).
- **۹ کارت سرمایه** (موجودی/آزاد/مارجین/روزانه/باز/فرصت‌ها/نرخ برد/
  افت/ریسک) + **پنل ریسک ۱۰خانه** با حکم بدترین‌حالت؛ جلوی ورود
  خطرناک را همان موتور می‌گیرد (پنل فقط شفافیت می‌دهد).
- **نمودار کندل+حجم** با همان `PriceChart` صفحهٔ تحلیل — منبع واحد؛
  تایم‌فریم 1m/5m/15m/1h/4h و نماد از کارت؛ خط زنده + ورود/SL/TP
  موقعیت باز + P50 پیش‌بینی؛ `mark_signal` حالا تکرار-امن است.
- **پنل پیش‌بینی خلاصه** از همان موتور واحد صفحهٔ پیش‌بینی — هیچ موتور
  پیش‌بینی جدیدی ساخته نشد.
- **پویش فرصت ۱۷ستونه** + جست‌وجو/فیلتر + دوبار کلیک = درخواست ورود
  دستی (`opportunity_candidate` → همان `open_trade` با همهٔ دروازه‌ها).
- **موقعیت‌ها ۱۸ستونه** (ستون مقدار افزوده شد)؛ خروج همچنان
  تیک‌محور میلی‌ثانیه‌ای با پشتیبان poll؛ STALE همچنان ورود را می‌بندد.
- **نکتهٔ فنی مهم — نگاشت UserRole**: مرتب‌سازی Qt سطرها را جابه‌جا
  می‌کند؛ ایندکس واقعی هر ردیف در `Qt.ItemDataRole.UserRole` ستون
  نماد ذخیره و در دوبار کلیک/انتخاب خوانده می‌شود. اگر جدول تازه‌ای
  با کلیک سطری ساختید، همین الگو را نگه دارید.
- **AICandidate** چهار فیلد تازه گرفت (probability/expected_move/
  mtf/risk_reward) که پویش AI پر می‌کند و در ردیف فرصت‌ها می‌نشیند.
- بومی‌سازی ‎+۸۸ رشته (fa/en)؛ آزمون‌ها ۲۲۴۸ شد (۲۲۴۷ پاس + ۱ skip).

## نسخهٔ ۲.۰٫۰ — چه عوض شد

**چرا ۲٫۰ و نه ۱٫۱۲؟** تصمیم کاربر: بعد از ۱.۹.۲۳ این دور، بزرگ‌ترین
تغییر معماری از نسخهٔ ۱ به بعد است — برنامه یک موتور محاسباتی تازه
(پیش‌بینی احتمالاتی چندافقی)، جدول پایگاه داده، دو ابزار عامل و یک
صفحهٔ تازه گرفت. سطح نسخه باید این جهش را نشان دهد؛ همهٔ فایل‌ها و
بسته‌ها با ۲.۰٫۰ هم‌نسخه شدند. (در جریان کار با نام کاری ۱٫۱۱ شمرده
می‌شد؛ تحویل نهایی همان محتواست.)

فازهای ۲ تا ۱۵ِ سند حاکم `docs/PREDICTIVE_ENGINE_FA.md` کامل شد: موتور
هوش پیش‌بینی از این پس **کامل و متصل** است، نه فقط پایه.

۱. **نردبان ۱۳ افق** (۱ دقیقه تا ۷ روز): برنامه‌ریز `horizons.py` برای
   هر افق مناسب‌ترین تایم‌فریم پایه را برمی‌گزیند؛ افقِ بدونِ تاریخچهٔ
   کافی با دلیل «غیرفعال» گزارش می‌شود، هرگز جعل نمی‌شود.
۲. **رژیم و مرحلهٔ بازار** (`regime.py`): شش رژیم + مرحلهٔ چرخهٔ بازار
   (انباشت/رشد/توزیع/ریزش) + ماتریس احتمال گذار؛ هم‌راستایی
   چندتایم‌فریمی از موتور موجود `signals/multi_timeframe` می‌آید.
۳. **توزیع احتمال** (`distribution.py`): چندک‌های تجربی P10…P90 از
   بازده‌های پیش‌روی واقعی؛ fallback تحلیلی با مقیاس ATR وقتی نمونه
   کم است، و برچسب صادقانهٔ «quantiles from ATR scaling».
۴. **آنسامبل** (`models/`): آماری + GBM (LightGBM→XGBoost) + LSTM
   اختیاری با walk-forward؛ برچسب‌ها `log(ahead/base)`، مهر زمانی
   بسته‌شدن، بدون نشت. sklearn لازم نیست — API بومی.
۵. **موتور اصلی** (`engine.py`): `assess()` کل خط لوله را می‌رانَد:
   کیفیت داده → فیچر → رژیم → ۱۳ افق → ترکیب چهارجزئی → سناریو →
   هشدار → «چه عوض شد» → ثبت. کش ۶۰ ثانیه؛ ضدهرزنامهٔ ثبت.
۶. **قانون ۵ — دقت ثبت‌شده**: هر پیش‌بینی در جدول `predictions`
   ذخیره می‌شود؛ افق که سر آید با **قیمت واقعی** حل می‌شود و دقت،
   بریر و سلامت مدل فقط از حل‌شده‌ها می‌آید. حکم neutral با بازه
   (P25–P75) است نه جهت.
۷. **عامل AI**: دو ابزار تازه `get_prediction_report` و
   `get_prediction_accuracy` — LLM توضیح می‌دهد، عدد از موتور می‌آید.
۸. **صفحهٔ «پیش‌بینی»**: یازدهمین صفحه با نردبان افق‌ها، رژیم‌ها،
   سناریوها، هشدارها، دقت و تایم‌لاین؛ کلید ناوبری `nav.prediction`
   (آیکون chart، Ctrl+5). میان‌برهای عددی به ده صفحهٔ نخست سقف
   خوردند و صفحات بعد از دهم F1/F2… می‌گیرند تا Ctrl+1 دوبار بسته
   نشود.
۹. **رفع دو باگ واقعی رابط**: (الف) دکمهٔ «تحلیل» جدول پویش با قلم
   واقعی وزیرمتن ۵ پیکسل له می‌شد — `BUTTON_ROW_HEIGHT` به ۵۴ رفت.
   (ب) فونت سطح برنامه بین آزمون‌ها نشت می‌کرد و مجموعه را
   ترتیب-وابسته می‌کرد؛ فیکسچر `_isolate_qt_app_look` در conftest
   فونت و شیوه‌نامه را بعد از هر آزمون برمی‌گرداند — مجموعهٔ کامل
   (۲۱۷۸ پاس + ۱ skip که LSTM است و torch اختیاری می‌خواهد) حالا ۵
   دقیقه است، نه ۱۴.

## نسخهٔ ۱.۱۰٫۰ — چه عوض شد

این نسخه آغاز پروژهٔ بزرگ «موتور هوش پیش‌بینی» است. سند حاکم آن
`docs/PREDICTIVE_ENGINE_FA.md` است — قبل از هر کاری آن را بخوان.

۱. **فاز ۱ انجام شد**: موتور کیفیت داده (`market/quality.py`) و خزانهٔ
   فیچر (`signals/prediction/features.py`) با ۳۶ آزمون تازه. هیچ فیچری
   بدون مهر زمانی «بسته‌شدن کندل» ساخته نمی‌شود — قانون ضد look-ahead.
۲. **نگهبان اتصال** (`market/resilience.py`): اگر وظیفهٔ فید قیمت
   بمیرد، با عقب‌نشینی نمایی + jitter زنده می‌شود و وضعیت سه‌حالته
   (آنلاین/در حال اتصال مجدد/آفلاین) به نوار وضعیت می‌رود. این همان
   قابلیت «همیشه آنلاین» است که کاربر حیاتی می‌داند.
۳. **گروه وابستگی `ml`** در pyproject (torch/xgboost/lightgbm) برای
   فاز ۵؛ اختیاری — مدل آماری بدون آن‌ها هم کار می‌کند.
۴. تصمیم‌های کاربر: آنسامبل کامل با PyTorch؛ داده فقط کریپتو داخلی؛
   رویدادها (Fed/CPI/…) دستی وارد می‌شوند.

## نسخهٔ ۱.۹.۲۳ — چه عوض شد

هیچ قابلیت تازه‌ای ندارد. هفت آزمون مردود به صفر رسید.

- **پایان خط `.bat` (علت واقعی، پنج آزمون).** پنج فایل `.bat` در مخزن
  LF داشتند و `.gitattributes` وجود نداشت، پس هر کلون روی هر
  سیستم‌عاملی LF می‌گرفت. حالا `.gitattributes` قاعدهٔ
  `*.bat text eol=crlf` دارد و `git ls-files --eol '*.bat'` مقدار
  `i/lf w/crlf` می‌دهد. بلاب‌های گیت دست‌نخورده ماندند. **احتمالاً
  همین علت شکست سازنده روی ویندوز کاربر است** — عیب‌یاب خودِ برنامه
  اسکریپت‌ها را «ناموفق» می‌زد.
- **دو آزمون کهنه.** یکی روی منبع `_auto_trader` دنبال رشتهٔ
  `build_trader_config` می‌گشت که یک لایه واسطهٔ `_auto_trade_config()`
  فاصله افتاده بود؛ دیگری فیکِ تنظیماتی داشت که فقط `get_int` داشت در
  حالی که پروفایل سرعت از ۱.۹.۲۲ با `get` می‌خواند. در هر دو مورد
  **کد محصول درست بود**. هر دو آزمون رفتاری شدند و روی کدِ عمدی
  خراب‌شده هم آزموده شدند تا ثابت شود رگرسیون را می‌گیرند.
- **پاک‌سازی قطعی پنجره.** `tests/conftest.py::destroy_window` اضافه
  شد. `deleteLater()` + `processEvents()` درخت ویجت را آزاد نمی‌کند؛
  باید `sendPostedEvents(None, QEvent.DeferredDelete)` صدا زده شود.
  `test_new_pages.py` از رد شدن بر سقف ۶۰۰ ثانیه به ۴۶ ثانیه رسید و
  `test_exchange_login_flow.py` از ۲۱۵ ثانیه به ۵۸ ثانیه. زمان کل
  مجموعه از ۱۰۰۴ ثانیه به ۲۸۳ ثانیه رسید و شمار آزمون‌های قبول از
  ۲۰۱۹ به **۲۰۵۰** — چون ۲۴ آزمون `test_new_pages.py` پیش‌تر به‌خاطر
  رد شدن از سقف زمانی هرگز شمرده نمی‌شدند. **این اشکال
  کاربر را نمی‌زند** — `main.py` فقط یک بار پنجره می‌سازد.
- **مستندات.** نسخه در **شش** جا ذکر می‌شود نه چهار جا: دو README هم
  هست. `README_EN.md` هنوز «Version 1.0.0» و «Python 3.12+» می‌گفت.
  شمار کلیدهای تنظیمات ۱۴۲ است نه ۱۳۶. `PROJECT_PROGRESS.md` مدخل
  ۱.۹.۲۲ نداشت که نوشته شد.

## نسخهٔ ۱.۹.۲۲ — چه عوض شد

- اتصال: اگر پینگ اول شکست بخورد سوکت باز هم تلاش می‌کند
  (`ensure_streaming`). تیک سوکت `_mark_rest_alive(True)` می‌زند.
  نگهبان هر ۱۵ ثانیه `keepalive` را صدا می‌زند. سقف نماد سوکت ۸۰ است.
  کف نظرسنجی خوراک ۰٫۵ ثانیه و پیش‌فرض ۲ ثانیه است.
- قیمت صفحه: تیک قیمت ۱ ثانیه، پایش معامله ۲ ثانیه، کندل ۸ ثانیه،
  تازه‌سازی جدول ۰٫۵ ثانیه. قیمت از کش می‌آید و `set_live_price` صدا
  زده می‌شود. `set_chart_data` هر ثانیه صدا زده نمی‌شود.
- معاملهٔ سریع: دکمهٔ «تنظیم خودکار سریع» مارجین ۱۰، اهرم ۲۰۰، هدف و
  حد ضرر ۳ یا ۴ یا ۵، همزمانی ۱، پایش ۰٫۴، نگهداری ۹۰ و کارمزد ۰٫۰۰۰۶
  را ذخیره می‌کند. حالت و عبارت تأیید را پاک نمی‌کند. ذخیرهٔ تنظیم،
  موتور در حال اجرا را با `apply_config` به‌روز می‌کند.
- کارمزد: ورود روی رکورد ذخیره می‌شود. خروج فقط به `close_trade` داده
  می‌شود. سود خالص = ناخالص − هر دو طرف. برای ۱۰×۲۰۰، کارمزد
  رفت‌وبرگشت ۲٫۴۰ دلار است؛ هدف خالص ۳ دلار یعنی ناخالص ۵٫۴۰ دلار.
- کاربر موتور خودکار `self.app.auth.user_id` است، نه `_current_user`.
- درگاه خالی به موتور وصل است تا حالت زنده داخل `open_position` شکست
  بخورد، نه اینکه بی‌صدا کاغذی بماند در حالی که کاربر فکر می‌کند واقعی است.
- پروفایل سرعت در زبانهٔ کارایی تنظیمات است. متعادل عددهای ذخیره‌شده را
  نگه می‌دارد. سریع یک گام و یک ابزار چت است.
- دفترچهٔ نتیجه تیک ساعتی دارد. پیش‌فرض خاموش است
  (`signals.scorecard_auto`).

## خلاصهٔ نسخه‌های اخیر، از روی آزمون‌ها

این شرح جایگزین شرح تفصیلی `BUILD_INFO.txt` برای ۱.۹.۱۷ تا ۱.۹.۲۱ نیست
و آن بخش‌ها عمداً اختراع نشده‌اند. فقط همان چیزی است که docstring
آزمون‌ها می‌گویند:

- ۱.۹.۱۴: پویشگر اسکالپ، فیلتر نقدینگی سخت، موتور خودکار کاغذی با سقف اهرم.
- ۱.۹.۱۷: پیش‌بینی بازه‌ای افق ۱۵ دقیقه / ۱ ساعت / ۴ ساعت، نه عدد قطعی.
  بازهٔ دورتر باید پهن‌تر باشد.
- ۱.۹.۱۸: هشدار قیمت و سیگنال؛ نمودار زنده؛ پنل معاملهٔ خودکار.
- ۱.۹.۱۹: دفترچهٔ نتیجهٔ پیش‌بینی دیگر کد مرده نیست.
- ۱.۹.۲۰: سرویس «آشنا» در تنظیمات دیده می‌شود و مسیر کلید تا انبار رمز
  وصل است.
- ۱.۹.۲۱: آفلاین فقط بعد از چند شکست پیاپی REST؛ تنظیم دستی معاملهٔ
  خودکار؛ ورود بر پایهٔ اطمینان.

## عددهایی که نباید دوباره خراب شوند

- `validated()` حداقل پایش را ۰٫۲۵ ثانیه می‌گذارد، نه ۱٫۰. اگر ورودی
  پایش به رابط اضافه شود باید داخل جعبهٔ جمع‌شونده باشد و حداقلش ۰٫۲۵.
- آزمون چرخهٔ معامله قیمت ۱۰۲٫۵ و ۹۶ را روی هدف ۲ و حد ۳ با اهرم ۱۰
  هنوز می‌بندد. کارمزد پیش‌فرض این فاصله را آن‌قدر جابه‌جا نمی‌کند که
  آن قیمت‌ها از هدف بگذرند.
- آزمون اهرم ۱۲۵ دیگر سقف نیست. ورودی آزمون ۵۰۰ است تا سقف ۲۰۰ هنوز
  اهرم را ببرد.
- `trading/__init__.py` نباید `HARD_MAX_LEVERAGE` را وارد کند، مگر
  اینکه همهٔ واردکننده‌های بسته بررسی شوند.
- `.gitattributes` نباید حذف شود و قاعدهٔ `*.bat text eol=crlf` باید
  بماند. بررسی: `git ls-files --eol '*.bat'` باید `w/crlf` بدهد. بدون
  آن، پنج آزمون می‌شکنند و سازندهٔ ویندوز بی‌صدا می‌افتد.
- هر فیکسچری که `MainWindow` می‌سازد باید در پایان
  `destroy_window(window, app)` را صدا بزند، نه `deleteLater()` تنها.
- فیکِ تنظیمات در آزمون‌ها باید `get` داشته باشد، نه فقط `get_int`.
- `MarketToolset.tool_names` در کلاس ابزار بازار **property** است؛ در
  `CompositeToolset` متد. فراخوانی با پرانتز روی ابزار بازار خطا می‌دهد.
- قلم وزیرمتن که یک بار در QFontDatabase بار شود دیگر متریک «جانشین»
  نمی‌دهد؛ آزمونی که اندازهٔ ویجت می‌سنجد باید با همان قلم ساخته شود
  که برنامهٔ واقعی دارد (`ThemeManager.apply`)، وگرنه نتیجه به ترتیب
  اجرای آزمون‌ها وابسته می‌شود.
- نسخه در شش جا نوشته می‌شود: `app/core/constants.py:13`،
  `pyproject.toml:7`، `BUILD_INFO.txt` خط ۴ و ۶، `README.md` خط ۷
  (با جداکنندهٔ فارسی `٫`) و `README_EN.md` خط ۵.

## کار باز که بدون کاربر تمام نمی‌شود

1. `doctor_report.txt` نرسیده است. **از ۱.۹.۲۳ یک علت اثبات‌شدنی در
   دست است:** فایل‌های `.bat` پایان خط LF داشتند و عیب‌یاب خودِ برنامه
   اسکریپت‌ها را «ناموفق» می‌زد. این درست شده است. اگر سازنده هنوز
   شکست می‌خورد، آن‌وقت گزارش تازه لازم است — و حدس زدن دربارهٔ
   سازنده بدون آن گزارش همچنان ممنوع است (سه بار حدس زده شد و هر سه
   بار غلط بود).
2. اسکرین‌شات بخش تولید سیگنال نرسیده است.
3. اجراکنندهٔ سفارش LBank و Toobit هنوز سند تأییدشده ندارد. تا رسیدن
   سند، payload نساز. اگر روزی ساخته شد و اندازهٔ قرارداد نبود، حجم را
   حدس نزن و سفارش را رد کن.

## پیشنهادهای بعدی

فهرست جداست: `ADVANCED_FEATURES_FA.md`.
