# قواعد ادامهٔ توسعهٔ CryptoAITrader

## پیش از هر تغییر

1. ابتدا `docs/PROJECT_MEMORY_FA.md`، سپس `AI_HANDOVER.md` و `NEXT_TASK.md` را بخوان.
2. برای یافتن کلاس‌ها، توابع، وابستگی‌ها و اتصال‌های UI از `docs/CODE_MAP.md` استفاده کن؛ این فایل نمایهٔ ایستا از نسخهٔ مبناست، نه تضمین صحت یا جایگزین خواندن کد فعلی.
3. یافته‌ها و محدودیت‌های بررسی در `docs/REVIEW_VALIDATION_FA.md` ثبت شده‌اند. نتیجهٔ آزمون تاریخی را نتیجهٔ اجرای امروز معرفی نکن.
4. پیش از ویرایش، وضعیت گیت و مسیر واقعی فراخوانی را بررسی کن. فقط محدودهٔ توافق‌شده را تغییر بده؛ رفع سه ایراد R1/R2/R3 به درخواست کاربر در 2.2.2 انجام شد؛ در 2.3.0 مسیر auto/WS/history هم تغییر کرده؛ R6 و R10 جزئی‌اند و دیگر موارد طبق گزارش تحویل باز هستند.

## توافق با مالک پروژه

- پاسخ‌ها فارسی باشند.
- **هیچ commit، push، انتشار Release یا ساخت PR بدون درخواست صریح کاربر انجام نشود.** ابتدا ZIP برای دانلود تحویل بده؛ کاربر تست می‌کند و سپس دستور ثبت/ارسال می‌دهد. صرفِ تحویل ZIP یا گذشت زمان، تأیید نیست.
- کار این جلسه فقط روی شاخهٔ `arena/01a0cf53-cryptoaitrader` است. شاخه را عوض نکن.
- پایان هر مرحلهٔ توسعه: نسخه را افزایش بده، تاریخچه و حافظهٔ پروژه را به‌روز کن، آزمون‌ها و محدودیت‌ها را صادقانه گزارش کن و ZIP تازه را واقعاً برای دانلود ارائه کن؛ نمایش diff کافی نیست.
- نسخهٔ جاری: **2.4.2** (استخر فرایند محاسبهٔ پویش `signals/compute_pool.py`، صفحهٔ عملکرد تنبل، حذف ذخیرهٔ تکراری، نگهبان هنگ `ui/responsiveness.py`، کپی سیگنال `ui/signal_share.py`؛ `docs/RELEASE_2.4.2_FA.md`). پیش از آن 2.4.1 (سبک‌سازی پویش: bulk_fetch بدون نوشتن کندل، CpuGovernor، دروازهٔ محاسبه، سقف جدول؛ `docs/RELEASE_2.4.1_FA.md`). پیش از آن 2.4.0 (پویش کل بازار صرافی، پالایش هوشمند، منبع found/market/both برای سیگنال‌گیری خودکار؛ `docs/RELEASE_2.4.0_FA.md`). پیش از آن 2.3.2 (اتصال LBank: جدول خطا، مکث 10004، دامنه‌های جایگزین WS/REST؛ `docs/RELEASE_2.3.2_FA.md`). پیش از آن 2.3.1: رفع قطع اتصال و داشبورد (`docs/RELEASE_2.3.1_FA.md`). پیش از آن 2.3.0: مسیر ورود/خروج، کارمزد، دو اتصال همان provider، چرخش بازار و تاریخچه. وضعیت دقیق در `docs/RELEASE_2.3.0_FA.md`. نسخه‌های 2.2.0 اصلی و 2.2.1 مستندات جدا نگهداری شده‌اند. سایت دانلود فقط ZIP سورس و اطلاعات عمومی را ارائه می‌کند، نه backup شخصی.
- وصلهٔ کوچک/مستندسازی: افزایش patch؛ قابلیت سازگار: minor؛ تغییر ناسازگار: major با توافق کاربر. شمارهٔ انتشارهای قبلی را بازنویسی نکن.
- نسخهٔ دسکتاپ را در `app/core/constants.py`، `pyproject.toml`، سربرگ `BUILD_INFO.txt` و هر دو README همگام کن؛ `AI_HANDOVER.md`، `PROJECT_PROGRESS.md`، `NEXT_TASK.md` و گزارش تحویل جاری را هم به‌روز کن. نسخهٔ موبایل مستقل است؛ فعلاً `mobile/version.py` برابر `1.9.12` مانده است.

## حفاظت از محصول و داده

- پایگاه داده، تنظیمات ذخیره‌شده، کلیدها، حساب‌ها و سابقهٔ کاربر را بازنشانی نکن. تست فقط روی مسیر موقت با `CAT_DATA_DIR` انجام شود.
- ارسال سفارش واقعی، آزمون با پول واقعی یا استفاده از حساب/API خصوصی بدون اجازه ممنوع است. `LiveOrderGateway` فعلاً executor تأییدشده ندارد؛ این محافظ را دور نزن و payload صرافی را حدس نزن.
- عدد confidence، احتمال سود تضمین‌شده نیست. دادهٔ گم‌شده را جعل نکن؛ WAIT/INSUFFICIENT_DATA/STALE خروجی‌های معتبرند.
- UI از نخ پس‌زمینه تغییر نکند؛ نتیجه از پل Qt منتقل شود. `EventBus` شنونده را در نخ ناشر اجرا می‌کند.
- قراردادهای مشترک Domain Model، Registry، Repository و ترجمه‌های fa/en را حفظ کن. رفتار برنامه نباید وابسته به نام پوسته باشد.
- `.gitattributes` و CRLF فایل‌های `.bat` را حفظ کن. آزمون‌های پنجره از `destroy_window` برای پاک‌سازی استفاده کنند.
- ZIP از فایل‌های سورسِ آگاهانه انتخاب‌شده ساخته شود، شامل فایل‌های جدیدِ هنوز commit‌نشده؛ نه فقط `git archive HEAD`. `.git`، `.venv`، کش، لاگ، دادهٔ واقعی، `.env` و رازها وارد آن نشوند. `.env.example` جزو سورس است.
- ZIP و گزارش‌های حجیم تحویل را بیرون مخزن بساز؛ ZIP جدید را داخل خودش یا نسخه‌های بعدی تو در تو نکن. ZIP قدیمیِ از پیش trackشده در `release/` را بدون توافق حذف نکن.
- CRC، فهرست فایل‌ها، SHA-256 و نبود فایل حساس در بسته را بررسی کن. نتیجهٔ تست، محتوای تغییرات و نام بسته را همراه تحویل بنویس.
