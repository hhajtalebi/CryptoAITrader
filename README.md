<div dir="rtl">

# Crypto AI Trader

**برنامه دسکتاپ حرفه‌ای تحلیل بازار ارز دیجیتال و تولید سیگنال فیوچرز**

نسخه ۲٫۴٫۲ · ویندوز ۱۰/۱۱ · پایتون ۳٫۱۱+ (آزموده‌شده تا ۳٫۱۳) · فارسی و انگلیسی

[English README](README_EN.md)

> **نسخهٔ جاری ۲٫۴٫۲:** محاسبهٔ سنگین پویش در فرایند جدا با اولویت پایین (رابط دیگر
> هنگ نمی‌کند)، صفحهٔ عملکرد تنبل و خارج از نخ رابط، حذف ذخیرهٔ تکراری سیگنال‌ها، نگهبان
> هنگ در لاگ؛ و کپی نماد / اطلاعات کامل سیگنال از جدول و پنجرهٔ جزئیات.
> [گزارش 2.4.2](docs/RELEASE_2.4.2_FA.md).
>
> **تحویل قبلی ۲٫۴٫۱:** رفع سنگینی و هنگ هنگام پویش کل بازار: پویش دیگر روی دیسک
> نمی‌نویسد، سهم CPU محدود است، حلقهٔ شبکه قفل نمی‌شود، اندیکاتورها ~۴۵٪ سریع‌تر و
> جدول نتایج سبک شد. [گزارش 2.4.1](docs/RELEASE_2.4.1_FA.md).
>
> **تحویل قبلی ۲٫۴٫۰:** سیگنال‌ها → پویش: انتخاب دامنه بین «پرگردش‌ترین‌ها» با
> تعداد دستی و «کل صرافی» (همهٔ نمادها)، پالایش هوشمند و کمینهٔ گردش، نتیجهٔ زنده و
> برآورد زمان. سیگنال‌گیری خودکار با منبع قابل‌انتخاب: «سیگنال‌های پیداشده»، «کل
> بازار» یا «هر دو»؛ چرخش کامل روی همهٔ نمادهای صرافی. [گزارش 2.4.0](docs/RELEASE_2.4.0_FA.md).
>
> **تحویل قبلی ۲٫۳٫۲:** رفع اتصال LBank — وب‌سوکت با دامنهٔ رسمی
> `api.lbank.info` و دامنه‌های جایگزین، و رفع «۲۰ ثانیه آنلاین، بعد آفلاین» (کد
> 10004 محدودیت نرخ بود، نه خطای احراز هویت). [گزارش 2.3.2](docs/RELEASE_2.3.2_FA.md).
>
> **تحویل قبلی ۲٫۳٫۱:** رفع «چند ثانیه آنلاین و بعد قطع» (ترافیک اضافهٔ REST و
> محدودیت نرخ صرافی)، مکث خودکار هنگام 429/418، آنلاین‌بودن بر پایهٔ دادهٔ تازهٔ
> REST یا وب‌سوکت، و داشبورد حرفه‌ای با مرکز فرمان. [گزارش 2.3.1](docs/RELEASE_2.3.1_FA.md).
>
> **تحویل قبلی ۲٫۳٫۰:** پایش خروج مستقل از اسکن، جلوگیری از ورود/خروج
> تکراری، محاسبهٔ خالص کارمزد، دو اتصال همان صرافی و REST تازه، چرخش بازار
> و علاقه‌مندی‌ها، ورود از فرصت‌ها به تاریخچهٔ Open و خروج از ردیف/مودال.
> **۷۵۸ پاس + ۴ skip** در زیرمجموعهٔ انتخابی؛ GUI بومی تأیید نشده است.
> سود، زیان صفر و اتصال دائم تضمین نمی‌شوند. [گزارش 2.3.0](docs/RELEASE_2.3.0_FA.md).
> هیچ commit/push انجام نشده؛ این تحویل سورس است و پس از تست شما منتشر می‌شود.

> **تحویل قبلی ۲٫۲٫۲:** سه ایراد بررسی قبل اصلاح شد: کش پیش‌بینی، انتقال
> تنظیم‌های موتور معامله و برچسب حساسیت backup (بدون حذف کلید).
> درگاه دانلود محلی نیز اضافه شد. نتیجهٔ هسته: **۷۰۸ پاس + ۳ skip**؛
> تست کامل GUI/ویندوز تأیید نشده است. جزئیات و دستور راه‌اندازی در
> [گزارش نسخهٔ ۲٫۲٫۲](docs/RELEASE_2.2.2_FA.md).
> یادداشت ۲٫۲٫۱ در ادامه تاریخی است. هیچ commit/push انجام نشده است.

> **تحویل ۲٫۲٫۱ — شناخت و مستندسازی (۲۰۲۶-۰۹-۲۳):** منطق برنامه تغییر نکرده است.
> نقشهٔ واقعی اتصال اجزا: [حافظهٔ پروژه](docs/PROJECT_MEMORY_FA.md)؛
> فهرست تمام فایل‌ها/کلاس‌ها/توابع: [نمایهٔ کد](docs/CODE_MAP.md)؛
> آزمون‌ها، محدودیت‌ها و ایرادهای باز: [گزارش بررسی](docs/REVIEW_VALIDATION_FA.md).
> بعضی توضیحات تاریخی پایین با کد فعلی یکسان نیستند: موتور ۵ استراتژی و
> رجیستری ۱۴ تایم‌فریم دارد؛ AI در بعضی حالت‌ها تصمیم‌گیر است؛ داده به‌طور
> پیش‌فرض کنار برنامه است و رازهای رمزنگاری‌شده می‌توانند در DB و backup باشند.
> نتیجهٔ اجرای این مرحله ۶۷۴ پاس و ۲ skip در زیرمجموعهٔ بدون UI است؛ آزمون
> کامل به‌علت نبود کتابخانه‌های سیستمی Qt در محیط بررسی اجرا نشد.
> این بسته سورس است، نه EXE تازه. هیچ commit/push بدون اجازهٔ کاربر انجام نمی‌شود.

---

## ⚠ پیش از هر چیز

این برنامه **ابزار تحلیل** است، نه مشاور مالی و نه ربات معامله‌گر.

- پیش‌فرض **کاغذی** است: قیمت زنده است ولی سفارش واقعی نمی‌رود. زیرساخت سفارش واقعی هست، ولی تا وقتی اجراکنندهٔ تأییدشدهٔ صرافی وصل نشده و کاربر خودش حالت زنده را با عبارت تأیید روشن نکرده، هیچ سفارش واقعی ارسال نمی‌شود. جزئیات در [`AI_HANDOVER.md`](AI_HANDOVER.md).
- «درصد اطمینان» یک سیگنال، **احتمال سود نیست**. این عدد فقط می‌گوید چند درصد از عوامل تحلیلی با هم هم‌جهت شده‌اند. اطمینان ۸۰٪ یعنی اندیکاتورها با هم موافق‌اند، نه اینکه ۸۰٪ احتمال دارد معامله سودده شود.
- مسئولیت هر تصمیم معاملاتی کاملاً با خود شماست.

---

## این برنامه چه می‌کند؟

| قابلیت | توضیح |
|---|---|
| **داده زنده بازار** | اتصال REST و WebSocket به صرافی، با اتصال مجدد خودکار و حالت آفلاین |
| **۱۳ تایم‌فریم** | از ۱ دقیقه تا ۱ هفته؛ تایم‌فریم‌هایی که صرافی ندارد به‌صورت خودکار از تایم‌فریم پایین‌تر ساخته می‌شوند |
| **۲۴ اندیکاتور** | روند، مومنتوم، نوسان، حجم، و حمایت/مقاومت |
| **موتور سیگنال** | خروجی `LONG` / `SHORT` / `WAIT` با ورود، حد ضرر، سه هدف، نسبت ریسک به ریوارد و اهرم پیشنهادی |
| **موتور ریسک** | حد ضرر بر پایه ساختار بازار یا ATR، محدودسازی فاصله حد ضرر، رد کردن ستاپ‌های بی‌کیفیت |
| **تحلیل هوش مصنوعی (اختیاری)** | تفسیر متنی چند تایم‌فریمی با مدل محلی (Ollama) یا هر سرویس سازگار با OpenAI |
| **هوش پیش‌بینی (جدید در ۲٫۰)** | نردبان ۱۳ افق از ۱ دقیقه تا ۷ روز با توزیع احتمال (P10…P90)، رژیم بازار، سناریوها و دقتِ ثبت‌شده در برابر قیمت واقعی |
| **گزارش‌گیری** | خروجی CSV، Excel، JSON، PDF و HTML |
| **پشتیبان‌گیری** | نسخه پشتیبان دستی و خودکار پیش از هر مهاجرت، با امکان بازیابی |
| **دو زبانه** | فارسی راست‌به‌چپ و انگلیسی چپ‌به‌راست، با تعویض زبان بدون بستن برنامه |

---

## نصب سریع

### روش ۱ — فایل اجرایی (ساده‌ترین)

فایل `CryptoAITrader.exe` را از پوشه `dist` اجرا کنید. نیازی به نصب پایتون نیست.

### روش ۲ — اجرا از کد

</div>

```bash
git clone <repository-url>
cd CryptoAITrader

python -m venv .venv
.venv\Scripts\activate        # ویندوز
source .venv/bin/activate     # لینوکس و مک

python -m pip install --upgrade pip   # مهم: pip قدیمی باعث خطای نصب می‌شود
pip install -r requirements.txt
python main.py
```

<div dir="rtl">

### ساخت فایل اجرایی ویندوز

</div>

```bat
scripts\build_windows.bat
```

<div dir="rtl">

خروجی در `dist\CryptoAITrader\CryptoAITrader.exe` ساخته می‌شود.

---

## اولین اجرا

با اجرای نخست، یک **ویزارد خوش‌آمدگویی** باز می‌شود و این موارد را می‌پرسد:

۱. زبان و پوسته (تیره، روشن، یا هماهنگ با ویندوز)
۲. صرافی مورد استفاده — پیش‌فرض **LBank** که برای داده عمومی **به هیچ کلید API نیاز ندارد**
۳. تحلیل هوش مصنوعی — به‌صورت پیش‌فرض **خاموش** است؛ برنامه بدون هیچ سرویس پولی کار می‌کند
۴. پارامترهای ریسک — موجودی فرضی حساب، درصد ریسک هر معامله، و بیشینه اهرم

تنظیمات شما در پایگاه داده ذخیره می‌شود و **هرگز با مقادیر پیش‌فرض یا در به‌روزرسانی‌ها بازنویسی نمی‌شود**.

---

## حالت‌های اجرا

</div>

```bash
python main.py                      # رابط گرافیکی
python main.py --check              # بررسی سلامت اجزا بدون باز کردن پنجره
python main.py --signal BTC/USDT    # تولید یک سیگنال در خط فرمان
python main.py --log-level DEBUG    # اجرا با گزارش کامل
```

<div dir="rtl">

---

## راهنمای صفحات

| صفحه | کاربرد |
|---|---|
| **داشبورد** | وضعیت اتصال، خلاصه بازارهای زیر نظر، آخرین سیگنال‌ها |
| **بازارها** | فهرست کامل بازارهای صرافی با جست‌وجو و افزودن به فهرست پیگیری. دوبار کلیک روی هر ردیف، آن نماد را در صفحه تحلیل باز می‌کند |
| **تحلیل** | نمودار شمعی با حجم، میانگین‌های متحرک، سطوح کلیدی، جدول اندیکاتورها و متن تحلیل هوش مصنوعی |
| **سیگنال‌ها** | تولید سیگنال چند تایم‌فریمی و مشاهده سابقه کامل |
| **پیش‌بینی** | گزارش موتور هوش پیش‌بینی: افق‌ها، رژیم‌ها، سناریوها، هشدارها، «چه عوض شد» و دقت |
| **معامله** | ترمینال حرفه‌ای معاملهٔ خودکار: ۹ کارت سرمایه با آیکون، نمودار کندل زنده (۱m تا 4h) با اندیکاتور EMA/SMA/بولینگر، ابزار خط‌کشی، عکس‌لحظه‌ای و منوی نماد آیکون‌دار؛ پنل پیش‌بینی با تحلیل لحظه‌ای هوش مصنوعی؛ پویش فرصت ۱۷ستونه که همیشه (حتی بدون موتور روشن) زنده است + تنظیمات مودال؛ موقعیت‌های باز ۱۹ستونه با دکمهٔ بستن هر ردیف و مودال جزئیات؛ پنل ریسک ۱۰خانه؛ نمادهای مورد علاقه با انتخاب تیک‌خور و ذخیره در دیتابیس |
| **گزارش‌ها** | ساخت گزارش از سیگنال‌های یک بازه و خروجی گرفتن در پنج قالب |
| **تنظیمات** | زبان، پوسته، صرافی، هوش مصنوعی، پارامترهای ریسک، پشتیبان‌گیری و بازیابی |
| **راهنما** | توضیح مفاهیم و پاسخ پرسش‌های پرتکرار |

---

## سیگنال چگونه ساخته می‌شود؟

</div>

```
کندل چند تایم‌فریم  →  ۲۴ اندیکاتور  →  ۳ استراتژی رأی می‌دهند
                                            ↓
                          رأی‌ها با وزن تایم‌فریم جمع می‌شوند
                          (۱ دقیقه: ۰٫۳  …  ۱ هفته: ۲٫۰)
                                            ↓
                        |امتیاز| ≥ ۰٫۲۲  →  LONG یا SHORT
                        در غیر این صورت   →  WAIT
                                            ↓
                    موتور ریسک بررسی می‌کند: فاصله حد ضرر، R/R، اهرم
                    اگر ستاپ قبول نشود  →  WAIT با ذکر دلیل
                                            ↓
              (اختیاری) هوش مصنوعی فقط متن تحلیل را اضافه می‌کند
                     و هیچ‌گاه قیمت‌ها را تغییر نمی‌دهد
```

<div dir="rtl">

**`WAIT` یک خروجی معتبر و مفید است.** بیشتر اوقات بازار ستاپ روشنی ندارد و برنامه عمداً سیگنال نمی‌سازد. برنامه‌ای که همیشه سیگنال می‌دهد، در حال حدس زدن است.

---

## تحلیل هوش مصنوعی (اختیاری)

برنامه بدون هوش مصنوعی کاملاً کار می‌کند. برای فعال کردن آن سه گزینه دارید:

**۱. مدل محلی (رایگان، پیشنهادی)**

</div>

```bash
# نصب Ollama از ollama.com
ollama pull llama3.1
ollama serve
```

<div dir="rtl">

سپس در تنظیمات: ارائه‌دهنده = `local`، مدل = `llama3.1`

**۲. سرویس سازگار با OpenAI** — نشانی سرویس و کلید API را در تنظیمات وارد کنید.

**۳. ارائه‌دهنده سفارشی** — راهنمای [`docs/ADD_AI_PROVIDER_FA.md`](docs/ADD_AI_PROVIDER_FA.md).

هوش مصنوعی در این برنامه **داده نمی‌سازد**. تنها به داده واقعی بازار از راه ابزارهای تعریف‌شده دسترسی دارد و اگر داده کافی نباشد، موظف است `INSUFFICIENT_DATA` برگرداند. خروجی مدل پیش از نمایش اعتبارسنجی می‌شود.

---

## امنیت کلیدهای API

- انبار رازها از keyring، فایل رمزنگاری‌شده یا **DatabaseBackend رمزنگاری‌شده** پشتیبانی می‌کند؛ Application با تنظیم پیش‌فرض `security.store_secrets_in_db=True` از DB استفاده می‌کند.
- مقدار خام کلید نباید در سورس یا لاگ نوشته شود؛ فیلتر لاگ و پاک‌سازی پیام‌های صرافی برای پوشاندن اطلاعات حساس وجود دارند. ciphertext داخل SQLite با «کلید خام داخل DB» متفاوت است.
- روش مشتق‌سازی کلیدِ backend فایل/DB هم‌سطح حفاظت سیستم‌عاملی DPAPI نیست؛ بهبود آن جزو کارهای باز بررسی امنیتی است.
- پشتیبان می‌تواند **رکورد رمزنگاری‌شدهٔ کلیدها در DB** را داشته باشد؛ از ۲٫۲٫۲ در manifest حساس علامت می‌خورد. فایل keyring و `.secret_store.bin` داخل آن نیست. پشتیبان شخصی را عمومی منتشر نکنید.

---

## ساختار پروژه

</div>

```
CryptoAITrader/
├── main.py                  نقطه ورود
├── app/                     هسته: تنظیمات، پایگاه داده، امنیت، لاگ، DI
│   └── application.py       تنها جایی که اجزا به هم وصل می‌شوند
├── market/                  داده بازار
│   └── providers/lbank/     پیاده‌سازی LBank (قابل تعویض)
├── indicators/              ۲۴ اندیکاتور
├── signals/                 موتور سیگنال، موتور ریسک، استراتژی‌ها
├── ai/                      عامل هوش مصنوعی، ارائه‌دهنده‌ها، پرامپت‌ها
├── reports/                 ساخت و خروجی گزارش
├── backup/                  پشتیبان‌گیری و بازیابی
├── localization/{fa,en}/    ترجمه‌ها
├── ui/                      رابط گرافیکی
│   ├── pages/               ۱۱ صفحه
│   ├── charts/              نمودار شمعی
│   └── controllers/         اتصال رابط به موتورها
├── migrations/              مهاجرت‌های Alembic
├── tests/                   ۸۴ فایل آزمون (۲٬۲۷۰ آزمون)
├── scripts/                 اسکریپت‌های ساخت و اجرا
└── docs/                    مستندات
```

<div dir="rtl">

---

## مستندات

| سند | موضوع |
|---|---|
| [`docs/ADD_EXCHANGE_FA.md`](docs/ADD_EXCHANGE_FA.md) | افزودن صرافی جدید |
| [`docs/ADD_INDICATOR_FA.md`](docs/ADD_INDICATOR_FA.md) | افزودن اندیکاتور جدید |
| [`docs/AI_SETUP_FA.md`](docs/AI_SETUP_FA.md) | **فعال‌سازی هوش مصنوعی، مدل‌های رایگان، و بخش چت** |
| [`docs/ADD_AI_PROVIDER_FA.md`](docs/ADD_AI_PROVIDER_FA.md) | افزودن ارائه‌دهنده هوش مصنوعی |
| [`docs/LBANK_API_FA.md`](docs/LBANK_API_FA.md) | جزئیات API صرافی LBank |
| [`docs/DATABASE_FA.md`](docs/DATABASE_FA.md) | ساختار پایگاه داده و مهاجرت‌ها |
| [`docs/ARCHITECTURE_FA.md`](docs/ARCHITECTURE_FA.md) | معماری کلی و تصمیم‌های طراحی |
| [`docs/TROUBLESHOOTING_FA.md`](docs/TROUBLESHOOTING_FA.md) | رفع مشکلات رایج |

---

## آزمون‌ها

</div>

```bash
scripts/run_tests.sh          # لینوکس و مک
python -m pytest tests -q     # ویندوز
```

<div dir="rtl">

۲٬۲۷۰ آزمون در ۸۴ فایل شامل اندیکاتورها، موتور ریسک، موتور سیگنال، اعتبارسنج خروجی هوش مصنوعی، تایم‌فریم‌ها، بومی‌سازی، پشتیبان‌گیری، گزارش‌گیری، اجرای ناهمگام و ترمینال معاملهٔ خودکار.

---

## داده‌های شما کجاست؟

</div>

```
%LOCALAPPDATA%\CryptoAITrader\        (ویندوز)
~/.local/share/CryptoAITrader/        (لینوکس)
├── crypto_ai_trader.db    پایگاه داده
├── backups/               نسخه‌های پشتیبان
├── exports/               گزارش‌های خروجی
└── logs/                  فایل‌های لاگ
```

<div dir="rtl">

برای تغییر این مسیر، متغیر محیطی `CAT_DATA_DIR` را تنظیم کنید.

---

## مجوز

این نرم‌افزار «همان‌گونه که هست» ارائه می‌شود، بدون هیچ تضمینی. استفاده از آن به‌معنای پذیرش مسئولیت کامل تصمیم‌های معاملاتی شماست.

</div>
