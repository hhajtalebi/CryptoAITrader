"""
لایه رابط کاربری (PySide6).

قواعد این لایه:
    • هیچ رشته‌ای Hard-Code نمی‌شود؛ همه متن‌ها از `localization` می‌آیند
    • هیچ منطق تجاری اینجا نوشته نمی‌شود؛ رابط فقط نمایش‌دهنده است
    • جهت چیدمان (RTL/LTR) با زبان تغییر می‌کند
"""
