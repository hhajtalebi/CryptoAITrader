# assets

آیکون برنامه را با نام `icon.ico` در همین پوشه بگذارید.

فایل `CryptoAITrader.spec` وجود آن را بررسی می‌کند: اگر باشد، در فایل اجرایی
تعبیه می‌شود و اگر نباشد، ساخت بدون خطا با آیکون پیش‌فرض ادامه پیدا می‌کند.

اندازه پیشنهادی: چند لایه در یک فایل ico (۱۶، ۳۲، ۴۸، ۲۵۶ پیکسل).
