# پیشنهاد قابلیت‌ها و ابزارهای پیشرفته‌تر

این‌ها پیشنهادند، نه قابلیت پیاده‌شده. هیچ‌کدام نباید بدون خواست صریح
کاربر ساخته شود، و هیچ‌کدام مجوز معاملهٔ واقعی یا وعدهٔ سود نیست.

نسخهٔ فعلی برنامه `1.9.22` است. معامله هنوز پیش‌فرض کاغذی است.

## معامله و ریسک

1. **نمایش قیمت لیکویید شدن.** با اهرم ۲۰۰، کاربر باید پیش از شروع ببیند
   چند درصد حرکت مخالف مارجین را می‌سوزاند. این عدد باید کنار هدف سود
   باشد، نه مخفی در راهنما.
2. **حد ضرر دنباله‌دار برای اسکالپ.** بعد از رسیدن به سود خالص، حد ضرر
   به نقطهٔ ورود به‌علاوهٔ کارمزد بیاید تا معاملهٔ سبز، قرمز بسته نشود.
3. **بستن جزئی.** مثلاً نیمی در ۳ دلار و باقی در ۵ دلار. الان فقط یک هدف
   وجود دارد.
4. **سقف زیان ساعتی، نه فقط روزانه.** اسکالپ سریع می‌تواند در چند دقیقه
   سقف روز را پر کند. یک مکث اجباری بعد از چند باخت پیاپی لازم است.
5. **نرخ تأمین مالی (funding).** اگر نگهداری از زمان تأمین مالی رد شود،
   سود خالص ۳ دلاری ممکن است پیش از کارمزد صرافی تمام شود. برای نگهداری
   ۹۰ ثانیه‌ای معمولاً مهم نیست، ولی باید هشدار داده شود نه نادیده.
6. **دفتر تطبیق کاغذی و واقعی.** وقتی اجراکنندهٔ واقعی آمد، هر سفارش
   کاغذی و واقعی باید شناسه، حجم، قیمت پر شدن و کارمزد واقعی را کنار هم
   نشان دهد. اختلاف باید قرمز باشد.

## داده و سرعت

7. **کندل زندهٔ واقعی، نه فقط قیمت.** الان قیمت بدون redraw کامل می‌آید.
   گام بعدی این است که فقط آخرین شمع از سوکت وصله شود، نه ۳۰۰ کندل.
8. **صف نماد بر اساس تازگی.** اگر سهمیهٔ سوکت پر شد، نماد معاملهٔ باز و
   نماد صفحهٔ تحلیل باید همیشه مقدم بر فهرست پیگیری باشند.
9. **سنجش تأخیر.** یک نشانگر «قیمت چند میلی‌ثانیه پیش به‌روز شد» بهتر از
   فقط آنلاین/آفلاین است. کاربر lag را می‌بیند، نه حدس می‌زند.
10. **حالت کم‌داده.** روی اینترنت ضعیف، فقط نماد باز و نماد تحلیل پول
    شود و جدول بازار هر دقیقه یک بار.

## هوش مصنوعی

11. **انتخاب خودکار مدل از روی حافظهٔ کارت.** کاربر کارت ضعیف دارد.
    پروفایل سریع هست، ولی هنوز مدل کوچک‌تر را خودش انتخاب نمی‌کند.
    تشخیص VRAM و پیشنهاد مدل باید جدا از بازنویسی تنظیم ذخیره‌شده باشد.
12. **بازبینی بعد از هر اسکالپ، نه فقط سیگنال.** یک جمله: چرا بسته شد و
    آیا کارمزد هدف را خورد. بدون این، دفترچه فقط آمار است.
13. **رد کردن ورود وقتی کارمزد از هدف بزرگ‌تر است.** اگر کسی هدف ۱ دلار
    با حجم بالا بگذارد، موتور باید پیش از باز کردن بگوید این معامله بعد
    از کارمزد سود خالص ندارد.
14. **حافظهٔ گفتگو هنگام عوض کردن پروفایل سرعت.** الان `reset_ai` حافظه
    را پاک می‌کند. بهتر است تاریخچه بماند و فقط سقف ابزار عوض شود.

## سنجش و ابزار

15. **بک‌تست روی کندل ذخیره‌شده.** بدون سفارش و بدون وعده. فقط بگوید این
    قانون در گذشته چند بار به هدف خالص رسیده و چند بار کارمزد را باخته.
16. **بازارگردانی کاغذی دفتر سفارش.** اگر عمق بازار از حجم موقعیت کمتر
    باشد، اسکالپ روی کاغذ سبز است و در واقعیت پر نمی‌شود. شبیه‌سازی لغزش
    باید قبل از اهرم ۲۰۰ نشان داده شود.
17. **خروجی گزارش کارمزد.** جمع کارمزد روز، سهم صرافی، و سود خالص جدا از
    سود ناخالص. الان ستون `fee` هست؛ گزارش جدا لازم است.
18. **هشدار صوتی فقط برای بستن معامله.** هشدار قیمت هست. بستن خودکار
    اسکالپ باید حتی اگر کاربر در صفحهٔ دیگری است شنیده شود.
19. **حالت تمرین با سرعت ضبط‌شده.** پخش دوبارهٔ یک روز بازار، تا کاربر
    قانون ۳/۴/۵ دلار را بدون پول و بدون انتظار زنده ببیند.
20. **سازندهٔ صرافی فقط با سند.** ابزار بعدی نباید فرم سفارش حدسی بسازد.
    باید سند رسمی را به فیلدهای اجباری تبدیل کند و اگر فیلدی نبود،
    همان صرافی را «غیرقابل معاملهٔ واقعی» علامت بزند.

## چیزی که عمداً پیشنهاد نمی‌شود

- ارسال سفارش با payload حدسی LBank یا Toobit.
- روشن کردن معاملهٔ واقعی به‌صورت پیش‌فرض.
- بازنویسی اهرم ذخیره‌شدهٔ کاربر به ۲۰۰ بدون فشردن دکمهٔ پروفایل سریع.
- وعدهٔ سود. هدف ۳ دلار یک قانون بستن است، نه پیش‌بینی بازار.
